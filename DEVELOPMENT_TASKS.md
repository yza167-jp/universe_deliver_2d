# Development Tasks — M1 Four-Planet Demo

本文件只管理当前里程碑 M1。M0 的最终状态与完整任务历史保留在 Git 里程碑提交和 `Docs/Archive/M0/README.md` 中。

## 状态约定

- `Blocked`：依赖未完成。
- `Ready`：依赖已满足，可以由主 Agent 领取。
- `In Progress`：当前唯一正在开发的任务。
- `Human Check`：自动检查已通过，等待玩家试玩。
- `Done`：实现、检查和必要试玩均已完成。
- `Deferred`：明确移出 M1。
- `Stretch`：只在必做内容稳定且预算允许时解锁。

主 Agent 一次只允许一个 `In Progress` 任务。

## M1 强制 Human Check

- T-119 / Gate E：共用系统、v2 存档、赤砂回访与白噪星资格。
- T-129 / Gate F：白噪星完整订单。
- T-159 / Gate G：穹林星、武装选择与低空投放。
- T-179 / Gate H：群潮星与 Demo 高潮。
- T-199 / Gate I：四星 Demo 完整闭环。

其他任务默认在自动验收与必要烟雾测试通过后标记 `Done`；不要仅因为包含 UI、对白或占位美术就自动要求 Human Check。

## M1 进度

| 阶段 | 目标 | 状态 |
|---|---|---|
| A | M1 立项、v2 存档与多星球共用基础 | Done |
| B | 赤砂星回访与白噪星航行资格 | Human Check |
| C | 白噪星完整订单 | Blocked |
| D | 多星球成长、支线与回访共用系统 | Blocked |
| E | 穹林星完整订单与低空投放 | Blocked |
| F | 群潮星完整订单与主线高潮 | Blocked |
| G | 四星整合、导出与最终 Demo 验收 | Blocked |

---

# A. M1 立项、v2 存档与多星球共用基础

## T-100 — 建立 M1 文档、内容包与任务基线

**State:** `Done`  
**Priority:** P0  
**Goal:** 让 Codex 能在不误读 M0 文档的情况下安全进入 Four-Planet Demo 开发。

### 范围

- 将根目录当前目标切换到 M1。
- 冻结 M0 回归基线并建立归档入口。
- 建立 M1 Goal、任务队列、系统增量、存档 v2、内容预算和 Gate E–I。
- 建立赤砂回访、白噪、穹林、群潮 Planet Packet。
- 建立主线/支线 Order Packet 与角色候选。

### 明确不做

- 不修改游戏代码、场景或资产。
- 不把候选角色名和货物名升级为 LOCKED 正史。

### 验收标准

- 根目录文档不再把 M0 称为当前里程碑。
- M1 任务从 T-100 开始，T-101 成为唯一最早 Ready 任务。
- 三份必做支线与一份 Stretch 支线均进入计划。
- M0 精确快照可通过归档入口和里程碑 commit 找回。

### 依赖

M0 T-055 Done。

---

## T-101 — Save schema v2 与 v1 安全迁移

**State:** `Done`
**Priority:** P0  
**Goal:** 已完成 M0 的玩家无需重开即可继续 M1，多星球进度也能安全保存。

### 范围

- 将存档 schema 升级为 v2。
- 新增：解锁星球、章节、关系、许可、图鉴、纪念品、支线、站点状态、飞船升级、回访和 Demo 结尾变量。
- 实现 v1→v2 逐版本迁移。
- v1 已完成赤砂首单时，迁移后进入 M1 序章稳定站点状态。
- 主档/备份/坏档保留规则继续有效。

### 明确不做

- 不做多存档槽、云存档或飞行中节点恢复。
- 不删除或静默覆盖 v1 存档。

### 验收标准

- v1 完成档、v1 未完成档、v2 档和坏档均有自动测试。
- 迁移只执行一次，重复加载不重复奖励。
- 设置文件不被剧情迁移覆盖。
- M0 新游戏、保存和继续流程无回归。

### 依赖

T-100。

### 实现结果（2026-07-25）

- `GameProgressData` 升级为 schema v2，并完整保存章节、星球解锁、关系、许可、
  图鉴、纪念品、支线结果、站点等级、飞船升级、回访、结尾变量和稳定返站状态。
- schema 0 先进入既有 v1 解析与一致性验证，再执行 v1→v2 内存迁移；完成档保留
  `order_red_sand_m0`，同时补入 M1 Packet 的
  `order_red_sand_cooling_core` canonical 完成别名。
- 完成档进入赤砂回访章节并恢复已有赤砂、伊娅、旧中继铭牌和首单站点记录；
  未完成档保留既有 M0 状态，`main_story_chapter` 使用空 ID，不发放 M1 奖励。
- 读取旧档不改写原文件；下一次稳定保存才提交 v2，并由现有安全轮换保留原有效
  v1 备份。重复继续不会重复发奖或产生重复集合项。
- 50 套自动测试、完成/未完成 v1 迁移、坏档/备份恢复、主菜单继续、设置隔离、
  M0 新游戏→首单→返站→重建 App→继续游戏，以及完整项目检查全部通过。
- T-101 只建立持久化合同；多星球章节推进和运行时解锁接口仍由 T-102 实现。

---

## T-102 — 多星球章节、解锁、关系与许可运行时模型

**State:** `Done`
**Priority:** P0  
**Goal:** 主线顺序、局部关系和星球准入由数据驱动，而不是散落在场景脚本中。

### 范围

- 建立章节进度与星球解锁服务/模型。
- 建立星球关系值、许可 ID、图鉴条目、纪念品与回访状态的最小接口。
- 支持赤砂、白噪、穹林、群潮稳定 ID。
- 为主线顺序与局部选择建立白名单条件。

### 明确不做

- 不做复杂声望商店、派系模拟或多主线分支图。

### 验收标准

- 非法越级解锁被拒绝并给出原因。
- 关系与许可变化可保存、加载和测试。
- M0 赤砂星状态能映射到 M1 章节起点。

### 依赖

T-101。

### 实现结果（2026-07-25）

- `GameStateModel` 现为章节、四星解锁、关系、许可、图鉴、纪念品与回访状态的
  唯一运行时入口；所有变更返回带 `success / changed / reason_key / previous /
  current` 的类型化结果，只有真实变化才发出一次持久化信号。
- `M1ProgressRules` 集中保存六段主线顺序、四颗星球、三项许可、`-2…+3` 关系
  范围和星球准入白名单。白噪星要求白噪章节、赤砂已解锁及已取得/安装特高压电
  屏蔽罩；穹林与群潮要求对应章节和前一颗星球，跳级与倒退均被拒绝。
- 关系变化使用唯一事件 ID，并将消费标记写入既有 `story_flags`；重复事件、重复
  解锁、重复许可、重复图鉴/纪念品及重复回访状态均幂等，不重复保存或发奖。
- schema v2 读取新增章节、星球、许可、关系范围和稳定收藏/回访 ID 验证；
  `SaveService` 往返直接恢复同一份状态，不把加载重新解释为奖励事件。
- 新增 T-102 纯逻辑覆盖并扩展 SaveService 测试；51 套测试、v1→v2 迁移、
  主档/备份、M0 新游戏→首单→返站→继续和完整项目检查通过。
- 本任务仅落地运行时与规则合同；订单状态扩展、内容 Resource、导航/UI 和星球
  场景仍分别属于 T-103 及后续任务。

---

## T-103 — 主线/支线订单模型与一次一单状态流

**State:** `Done`
**Priority:** P0  
**Goal:** 在保持“一次一单”的叙事聚焦下支持主线、支线、部分成功和回访订单。

### 范围

- 扩展 OrderDefinition：章节、主/支线、是否加急、关系/许可奖励、重复策略、回访状态。
- 支持可用、已接取、完成、失败、放弃和已归档状态。
- 一次仍只允许一份 active order。
- 主线不可误放弃；支线可放弃且不阻塞主线。

### 明确不做

- 不做多单并行、随机刷新、无限委托板或路线优化。

### 验收标准

- 主线与支线状态流有纯逻辑测试。
- 不能同时接取两单。
- 支线失败不会撤销主线章节与必需模块。
- 订单 ID 及引用验证覆盖所有 M1 Packet。

### 依赖

T-102。

### 实现结果（2026-07-25）

- `OrderDefinition` 统一支持 `MAIN / SIDE / REVISIT`、`LANDING /
  LOW_ALTITUDE_DROP`、章节/星球/许可/模块解锁条件、五类奖励、三种重复策略与轻量
  加急参数；M0 赤砂首单已迁移到新字段但保持 UNIQUE、非加急、信用点奖励和着陆
  交付不变。
- `GameStateModel` 建立 `AVAILABLE / ACCEPTED / COMPLETED / FAILED / ABANDONED /
  ARCHIVED` 状态流，并继续以 `current_order_id` 作为唯一 active order 槽位。
  主线/回访不可放弃且失败后保持 active 进入既有重试；支线失败/放弃释放槽位，
  只有显式 `REPEATABLE` 才能重接，完成态永不重新开放。
- 统一完成入口在变更前验证整份奖励，单次应用信用点、关系、许可、图鉴、纪念品、
  完成标记和可选站点升级；`reward_applied_order_ids` 与完成状态共同阻止重复调用、
  重复加载或结算重入再次发奖。
- 加急订单只在 active 且未打开对话/帮助、游戏未暂停时累计计时；超时在宽限窗口
  内线性降至最低报酬比例，不直接失败，检查点与存档恢复保留计时。
- schema v2 增补订单状态图与奖励账本，并为缺少新字段的既有 v2/v1 档从
  current/completed/failed 记录安全补齐；验证拒绝多 active、未知状态和不一致
  终态。订单注册表同时验证重复 ID、章节/星球/模块、交付方式和全部奖励引用。
- 新增 T-103 运行时与数据验证覆盖；53 套测试、Godot 4.7.1 全脚本解析、v1→v2
  迁移、SaveService、M0 首单结算/完整闭环、订单终端及完整项目检查全部通过。
- 已知限制：本任务不落地 M1 Packet 内容、不重做订单/导航 UI、不显示加急倒计时，
  也不实现低空投放；这些仍属于 T-104 及后续任务。

---

## T-104 — M1 内容注册表、Packet 数据与引用验证

**State:** `Done`
**Priority:** P0  
**Goal:** 三颗新星球和全部订单从一致数据入口加载，Codex 不通过硬编码扩写内容。

### 范围

- 建立/扩展 M1 Planet、Order、Cargo、Module、Character 与 Codex Resource。
- 将 Packet 中稳定 ID 落地为占位数据。
- 候选显示名只使用 PROVISIONAL 本地化 Key。
- 验证主线顺序、模块获取、交付方式、角色和场景引用。

### 明确不做

- 不制作关卡或正式对白。
- 不锁定候选姓名。

### 验收标准

- 缺失/重复 ID、非法依赖与未定义本地化 Key 会使测试失败。
- 所有 M1 主线与必做支线能从注册表读取。

### 依赖

T-103。

### 实现结果（2026-07-25）

- 新增独立 `m1_four_planet_demo` 注册表，在不切换 M0 运行时入口的前提下统一注册
  4 颗星球、8 份实际订单、8 类货物、7 个模块、11 个角色、19 条类型化图鉴和
  4 件类型化纪念品；完整稳定 ID 清单见 `Docs/M1/07_M1_CONTENT_IDS.md`。
- 白噪星、穹林星和群潮星使用 `REGISTERED_ONLY` 内容就绪状态、空场景引用及独立
  占位飞行环境参数；验证器会拒绝将尚未制作的路线误报为可玩内容。
- M1 Packet 中的主线、赤砂回访和三份必做支线已落地为类型化 Resource，覆盖章节、
  解锁、角色、货物、交付方式、必须/推荐模块、奖励、重复策略与加急参数；Stretch
  赤砂支线未进入注册表。
- M0 首单继续以 `order_red_sand_m0` 作为唯一实际定义；
  `order_red_sand_cooling_core` 只作为兼容别名解析，避免维护两份可漂移订单。
- 新增类型化图鉴与纪念品定义；候选角色名、货物名及新收藏显示名均使用带
  `PROVISIONAL` 标记的本地化 Key，不把 Packet 候选值升级为 LOCKED 正史。
- 注册表验证现覆盖跨类型重复 ID、别名、场景/星球/角色/货物/模块/收藏引用、
  本地化 Key、三颗新星球的占位状态及三份必做支线合同；验证过程保持只读。
- Godot 4.7.1 headless import、54 套自动测试、M0 v1→v2 迁移、继续游戏、完整首单
  闭环和全部项目烟雾检查通过。
- 已知限制：本任务只建立内容数据和校验合同，不实现新星球路线、玩家可见多星球
  导航、图鉴 UI、低空投放或加急结算界面；这些仍属于后续任务。

---

## T-105 — 多星球导航与订单终端信息架构

**State:** `Done`
**Priority:** P0  
**Goal:** 玩家能理解下一颗主线星球、可选支线、配置门槛和回访机会。

### 范围

- 驾驶舱导航显示已解锁/未解锁星球及原因。
- 订单终端区分主线、支线和回访，不变成滚动任务海洋。
- 展示目的地、风险、交付、必须/推荐模块、客户历史和关系收益。
- 维持 640×360 可读性和键鼠焦点。

### 明确不做

- 不制作自由星图驾驶或任意星球访问。
- 不实现任务刷新。

### 验收标准

- 当前可接任务层级清楚。
- 未满足许可/模块时无法出发，并显示确定获取路径。
- M0 单订单显示无回归。

### 依赖

T-104。

### 实现结果（2026-07-25）

- 新增无状态 `M1CatalogModel` 与本地化提示解析器，统一投影当前主线、可选支线、
  下一步线索、历史订单及已知星球；active order 固定置顶，同一时刻最多一个当前
  主线，未发现支线不泄露名称，M0 历史 alias 不产生重复订单。
- 订单终端改为适配 640×360 的“紧凑分类目录 + 单项详情 + 固定操作区”，展示
  目的地、风险、交付方式、报酬、关系收益、货物、必须/推荐模块、客户历史和明确
  锁定路径；鼠标与键盘焦点只改变选中项，只有显式接取动作才修改订单状态。
- 驾驶舱新增独立导航面板，使用同一目录投影显示当前订单、唯一目的地、章节/星球/
  模块门槛、当前配置和航线状态；无订单时不提供任意星球旅行，也没有建立自由星图
  或第二份导航状态。
- `OrderDefinition` 增加 `REGISTERED_ONLY / PLAYABLE` 内容就绪合同；M0 首单保持
  可玩，7 份尚未落地剧情和路线的 M1 订单保持只登记。接单、配置确认和旅行开始
  均在运行时重新验证订单、目的星球与场景路径，不能只依赖 UI 禁用状态。
- 正常订单终端和驾驶舱切换为 `m1_data_registry.tres`；冻结的 M0 注册表仅保留为
  显式回归夹具。新增中英文目录、导航、锁定原因和模块状态本地化 Key，界面不暴露
  内部 ID。
- Godot 4.7.1 headless import、55 套自动测试、订单终端与导航专项烟雾、M0 完整
  闭环、v1→v2 迁移、继续游戏、Gate A、驾驶舱旅行及全部项目检查通过；5 个
  640×360 目录/导航录帧已人工检查布局和焦点可读性。
- 已知限制：本任务只建立信息架构与防误入守卫；M1 回访和新星球仍无可玩路线，
  因此对应订单继续显示为只登记线索。图鉴、低空投放、加急结算和内容调试入口分别
  留给 T-106～T-109。

---

## T-106 — 图鉴、纪念品墙与站点状态基础

**State:** `Done`
**Priority:** P1  
**Goal:** 配送结果在快递站留下可见痕迹，而不是只增加数字。

### 范围

- 建立人物、星球、货物、异常信号和纪念品的基础图鉴。
- 扩展纪念品墙为多槽位数据驱动显示。
- 建立三个 M1 站点状态：档案终端、生态角、中继观测室。
- 未解锁内容使用清晰占位而非泄露剧透。

### 明确不做

- 不做复杂收藏评分、100% 强迫或独立基地经营。

### 验收标准

- 条目和纪念品可保存与恢复。
- 站点变化由稳定 ID 驱动。
- M0 旧中继铭牌继续正确显示。

### 依赖

T-102、T-104。

### 实现结果（2026-07-25）

- 新增无状态 `CodexCatalogModel` 与可复用 `CodexBrowserUI`，按星球、人物、货物、
  异常和纪念品投影 `m1_data_registry.tres`；已解锁内容显示本地化资料，
  隐藏条目不出现，非隐藏锁定条目只显示通用占位，查询不写入进度。
- 纪念品墙改为注册表驱动的四槽灰盒界面；已获得内容显示名称和说明，未获得槽位
  不泄露后续奖励。键鼠共用选择路径，打开时通过站点模态协调器锁定世界输入并
  隐藏交互提示，关闭后恢复原焦点、移动和提示状态。
- M0 首单继续在统一结算中只产生一件旧中继铭牌及其关联图鉴；v1→v2 迁移、
  直接完成首单和旧内存展示标记均保持兼容，不产生重复收藏或重复信号。
- 建立 `station_state_archive_terminal`、`station_state_ecology_corner` 和
  `station_state_relay_observatory` 三个稳定状态 ID、公开幂等解锁接口及隐藏场景
  根节点。精确 ID 是展示权威，`station_state_level` 仅作为单调递增摘要，读取
  存档不会重放解锁事件。
- 本任务未把图鉴接入普通站点入口，也未制作三个设施的正式内容；这些根节点只为
  后续任务保留最小锚点，没有新增全局管理器、第三方依赖或基地经营系统。
- Godot 4.7.1 headless import、56 套自动测试、T-106 专项烟雾、v1→v2 迁移、
  继续游戏、M0 完整闭环、Gate A 和全部项目检查通过；三张 640×360 中文录帧已
  检查图鉴分类、锁定信息遮蔽、四槽布局和焦点可读性。

---

## T-107 — 低空投放交付原型

**State:** `Done`
**Priority:** P0  
**Goal:** 为穹林星支线建立第二种正式交付方式，但先在独立测试场景验证。

### 范围

- 实现固定、宽容接收区的低空投放。
- 判定高度、速度、水平位置、释放动作和货物落点。
- 支持成功、偏差部分成功和失败/重试。
- 鼠标/键盘使用同一 Input Map action。
- 独立 Delivery Lab 验证。

### 明确不做

- 不做复杂抛体模拟、移动敌对目标或空投战斗。
- 不直接制作穹林星关卡。

### 验收标准

- 判定参数集中配置。
- 释放后不会重复投放同一货物。
- 检查点重试恢复货物和投放状态。
- 自动测试覆盖窗口边界与结果。

### 依赖

T-103、T-104。

### 实现结果（2026-07-25）

- 新增集中配置的 `LowAltitudeDropProfile` 与确定性 `LowAltitudeDropModel`：
  以释放高度、固定下降速度和水平速度继承计算预计落点；覆盖 `PENDING`、核心
  成功、外圈部分成功、漏投和非法释放，不使用刚体投射物、随机风场或复杂碰撞。
- Delivery Lab 原型窗口为高度 `90–220 m`、水平速度 `90–240 m/s`；核心区
  `±110 m`、外圈 `±220 m`，下降速度 `120 m/s`、水平继承 `65%`，外圈质量/
  报酬比例为 `75% / 65%`。所有数值只存在于可调 Resource。
- 新增独立 `--delivery-lab` 入口，复用 M0 `CharacterBody2D` 飞船、显式积分和
  穹林占位环境。HUD 同时显示窗口、实时高度/速度、预计落点、单件货物状态及
  核心/外圈/漏投结果；`E` 与鼠标右键共用 `delivery_drop` Input Map action。
- 非法高度/速度不会消耗货物；首次合法释放立即关闭单货物门闩，重复输入不会
  生成第二件货物、覆盖结果或重复发出结算。`R` 从稳定检查点一起恢复飞船、
  货物、预测标记和 `PENDING` 结果。
- 场景不接取或结算穹林支线、不写信用点，也未把穹林星/订单提升为可玩。正式
  穹林路线、剧情和订单接入仍留给后续任务。
- Godot 4.7.1 headless import、58 套自动测试、Delivery Lab 直达/专项烟雾、
  全部 M0/Gate C 回归和完整项目检查通过；四张 640×360 中文 Metal 录帧确认
  预测线、核心/外圈、货物落点和三类结果反馈清楚且不被 HUD 遮挡。

---

## T-108 — 加急支线计时与超时减报酬

**State:** `Done`
**Priority:** P1  
**Goal:** 支线可提供速度压力，但不会把叙事游戏变成硬失败竞速。

### 范围

- 支线可配置加急目标时间、宽限和报酬曲线。
- 超时降低信用点/关系奖励，不直接判主线失败。
- 暂停、对话和帮助面板按明确规则暂停计时。
- 结算显示时间修正。

### 明确不做

- 不为所有订单增加倒计时。
- 不做排行榜或在线成绩。

### 验收标准

- 时间曲线可测试且不产生负报酬。
- 中途重试、暂停和加载不会重复计时。
- 非加急订单不显示压力 UI。

### 依赖

T-103。

### 实现结果（2026-07-25）

- `App/PersistentUI/ExpressOrderHUD` 同时作为跨场景唯一计时驱动和紧凑反馈层；
  只有当前 `ACCEPTED + is_express` 订单累计，组内权威实例防止重复 delta，
  非加急订单不累计且整块隐藏。
- 对话、飞行帮助和 `SceneTree.paused` 分别冻结计时；模态界面内显示本地化冻结
  提示，单纯系统暂停保留两行 HUD。场景切换、检查点、`R` 重试与存档恢复保留
  `elapsed_time`，不根据离线时钟补算。
- HUD 在 640×360 左下安全区显示 `mm:ss`、全额/宽限/保底/暂停及当前信用点
  比例；订单终端仅对加急订单展示目标、宽限、最低比例和非硬失败说明。
- 统一结算顺序为基础信用点 → 货物完整度 → 加急时间 → 最终信用点，并记录完整
  时间结果。`GameStateModel` 只提交权威结算器给出的最终值，奖励账本阻止时间
  倍率、信用点和准时关系奖励重复应用。
- 群潮支线继续保持 `REGISTERED_ONLY`；测试只使用深拷贝的可玩夹具，没有写入
  正式内容状态或玩家存档，也未实现群潮路线与剧情。
- Godot 4.7.1 headless import、59 套自动测试、T-108 专项烟雾、保存/暂停、
  订单终端、M0 首单结算与完整闭环及 `./scripts/check_project.sh` 全部通过；
  六张 640×360 中文 Metal 录帧已检查 HUD、模态提示、终端和结算布局。

---

## T-109 — M1 基础回归包与内容调试入口

**State:** `Done`
**Priority:** P0  
**Goal:** 新星球开发可以直达目标内容，同时每次提交仍保护 M0。

### 范围

- 增加按星球/订单/章节直达的开发入口。
- 建立 v2 存档、导航、订单、低空投放和 M0 闭环回归烟雾。
- 提供测试数据隔离，不污染玩家存档。

### 明确不做

- 不让调试入口出现在普通玩家菜单。

### 验收标准

- `./scripts/check_project.sh` 包含 M1 基础测试和 M0 回归。
- 调试状态可重置且不会写入正常存档。

### 依赖

T-105、T-106、T-107、T-108。

### 实现结果（2026-07-26）

- 建立集中声明并校验的 M1 调试场景目录，提供赤砂回访、白噪星、穹林星、
  群潮星、低空投放与加急订单六个直达入口；普通玩家菜单不暴露入口。
- 调试入口使用经过验证的内存进度快照；加急订单与低空投放使用隔离夹具，
  不把正式 `REGISTERED_ONLY` 内容升级为可玩状态。
- 调试会话关闭存档和设置写入，状态条明确显示场景、章节、订单、星球与
  `autosave=off`；按 F6 可重新应用同一份确定性快照并重载目标场景。
- `./scripts/check_m1_foundation.sh` 汇总 v1→v2 迁移、多星球目录与导航、
  图鉴/纪念品/站点状态、低空投放、加急订单、六个调试直达和 M0 完整闭环；
  `./scripts/check_project.sh` 已纳入该回归包。
- Godot 4.7.1 headless import、60 套自动测试、T-109 专项、六个直达启动、
  M1 基础聚合回归、M0 完整闭环、全部项目烟雾与空白检查通过。
- 已知限制：本任务只提供隔离的开发直达与回归基础，不实现 T-110 的赤砂
  回访内容，也不制作任何新增星球路线。

---

# B. 赤砂星回访与白噪星航行资格

## T-110 — 赤砂星回访订单数据与剧情骨架

**State:** `Done`
**Priority:** P0  
**Goal:** 让首单产生真实后果，并把伊娅与旧中继铭牌带入 M1 主线。

### 范围

- 落地 Packet 中的回访主线订单、材料货物、伊娅后续状态和对白骨架。
- 使用已有赤砂路线的短变化版或后半段入口。
- 明确屏蔽罩获得路径。

### 明确不做

- 不制作第二条同规模赤砂完整路线。
- 不解释中继网最终真相。

### 验收标准

- 已完成 M0 的档案可解锁回访。
- 未完成首单时不可越级。
- 主线模块获取不依赖刷钱。

### 依赖

T-109。

### 实现结果（2026-07-26）

- 回访订单继续保持 `REGISTERED_ONLY`，并同时要求已完成
  `order_red_sand_m0` 与首单剧情标记；测试夹具证明未完成首单不能越级。
- 新增赤砂回访合同：复用 M0 路线 `26000–38000 m` 的短服务航道，
  名义距离 `12000 m`、名义时长 `48 s`，不制作第二条同规模完整路线。
- 新增伊娅与老皮的双分支抵达对白骨架；玩家只决定完整上传或本地保留改装记录，
  两条分支均回收至同一主线，不解释中继网最终真相。
- 订单完成合同原子发放 `140` 信用点、赤砂关系 `+1`、材料图鉴、免费且唯一的
  特高压电屏蔽罩所有权、档案终端状态、白噪章节与赤砂回访完成状态；重复完成
  在任何奖励变更前被拒绝。
- `red_sand_revisit` 隔离调试快照明确停在“首单已完成、回访可用、屏蔽罩未获得”
  的开发节点。Godot 4.7.1 解析、61 套自动测试、T-110 专项烟雾、M1 聚合回归、
  M0 完整闭环与 `./scripts/check_project.sh` 均通过。
- 已知限制：本任务只落地数据、剧情与路线技术骨架；屏蔽罩能力/外观由 T-111
  实现，可玩回访路线、维修场变化、交付与实际结算由 T-112 实现。

---

## T-111 — 特高压电屏蔽罩模块与资格门槛

**State:** `Done`
**Priority:** P0  
**Goal:** 白噪星第一次证明主线模块既改变玩法，又有确定的剧情获取路径。

### 范围

- 新增 `module_high_voltage_shielding`。
- 模块影响电磁/高压危险，不替代普通护盾资源。
- 伊娅与老皮完成改装后解锁。
- 飞船外观提供最小可见变化。

### 明确不做

- 不做复杂模块升级树或随机品质。

### 验收标准

- 未安装时白噪星出发被阻止并说明路径。
- 安装后资格成立、存档保留。
- 模块效果由能力标签/配置读取。

### 依赖

T-110、T-104。

### 实现结果（2026-07-26）

- 将公司基础配发与剧情模块所有权集中到 `ShipLoadoutRules`；未通过赤砂回访奖励
  取得的特高压电屏蔽罩不能写入飞船配置，获得后可在 Defense 槽安装，卸下会
  立即使既有出发确认失效。
- 白噪可玩测试夹具证明“已拥有未安装”仍被出发前检查阻止，安装后资格成立；
  schema v2 序列化往返同时保留模块所有权、槽位配置和能力标签。
- 新增无状态电磁防护模型。当前集中可调基线为高压资源伤害乘数 `0.60`、
  电磁干扰乘数 `0.45`；高压伤害缩放后继续进入普通护盾优先入口，不建立第二条
  特殊护盾资源。
- 配置工作台会按当前 active order 展示模块，区分“尚未获得 / 已拥有未安装 /
  已安装”，并显示固定剧情获取说明；屏蔽罩安装后，配置预览与横版飞船均出现
  青色屏蔽轮廓。
- 正式白噪星与订单继续保持 `REGISTERED_ONLY`，未借用赤砂路线开放玩家出发。
  Godot 4.7.1 导入与解析、62 套自动测试、T-111 专项烟雾、640×360 可见录帧、
  M1 聚合回归、M0 完整闭环及 `./scripts/check_project.sh` 均通过。
- 已知限制：本任务只提供白噪危险可消费的能力与数值接口；正式电磁暴雪关卡和
  实际干扰表现仍由白噪星路线任务实现。

---

## T-112 — 赤砂星回访场景、路线变化与伊娅后续

**State:** `Done`
**Priority:** P0  
**Goal:** 玩家看见冷却设备、居民区和伊娅因首单发生变化。

### 范围

- 更新维修场视觉和互动。
- 复用路线的短回访版本，加入至少一个新景观/设施状态。
- 完成材料交付、屏蔽罩安装和一段角色对话。
- 写入回访、模块和关系结果。

### 明确不做

- 不扩展成完整城镇或新战斗。

### 验收标准

- 回访不是原场景原对白重复。
- 旧中继铭牌进入剧情但不揭示终极答案。
- 返回站点后可见变化。

### 依赖

T-111。

### 实现结果（2026-07-26）

- 将正式赤砂回访订单升级为 `PLAYABLE`，精确复用 M0 的
  `26000–38000 m` 后半段；专用入口检查点、三段本地 HUD 和 `48 s` 名义时长
  由同一 `RedSandRevisitContract` 驱动，没有复制第二条完整路线。
- 回访安全航道关闭原低空雷达、固定障碍和重复的 Gate C 危险组合；`28600 m`
  处新增无碰撞的净水/冷却设施、管线、蒸汽、灯光与居民活动，服务航道和最终
  进场均可从安全检查点重试。
- 更新抵达维修场并完成伊娅/老皮主对话、旧铭牌校准、材料交付和“完整上传 /
  本地保留”二选一；未走到最终完成标记或未选择时不能返站结算，取消对白不会
  伪造完成。
- 结算通过统一奖励入口原子写入信用点、关系、记录分支、材料图鉴、屏蔽罩所有权
  与自动安装、档案终端、白噪章节和回访完成状态；非法附加奖励在任何 mutation
  前整体拒绝。
- 返站后显示档案终端的最低可见灰盒；测试专用临时存档证明清空运行时状态后，
  继续载入可恢复选择分支、模块配置、回访状态和站点变化。
- Godot 4.7.1 导入、63 套自动测试、T-109/T-110 回归、T-112 完整闭环专项、
  三张 640×360 可见录帧、M1 基础聚合、M0 完整闭环与
  `./scripts/check_project.sh` 均通过。
- 已知限制：正式档案终端互动与浏览属于 T-113；订单标题和材料显示名继续保持
  PROVISIONAL；回访节奏、伊娅持续角色感与旧铭牌揭示尺度留到 Gate E 判断。

---

## T-113 — 档案终端与第一次 M1 站点变化

**State:** `Done`
**Priority:** P1  
**Goal:** 将赤砂回访、中继线索和后续星球信息汇入玩家的家。

### 范围

- 在快递站解锁档案终端。
- 显示已知星球、人物、货物和异常线索。
- 增加老皮关于白噪星的一段新内容。

### 明确不做

- 不展示未解锁角色或最终秘密。

### 验收标准

- 条目由 v2 图鉴数据驱动。
- 640×360 可读。
- 继续游戏后状态保持。

### 依赖

T-112、T-106。

### 实现结果（2026-07-26）

- 将 T-106 的 `CodexBrowserUI` 正式接入快递站；交互区、设备根节点和浏览入口只由
  精确 `station_state_archive_terminal` 状态启用，新档中保持隐藏且不可交互。
- 档案终端直接投影 v2 图鉴真相，完成赤砂回访后可查看已知的赤砂/白噪星、伊娅、
  两批剧情货物、旧中继铭牌与旧铭牌异常回波；未解锁角色和秘密条目不进入目录，
  “档案中已知白噪星”不等同于“已取得白噪出发许可”。
- 新增老皮的一次性三句简报，只把白噪公开维护日志中的相似断续噪声作为可核对
  线索，并要求先确认特高压电屏蔽罩；没有把来源、含义或中继网终局写成定论。
- 浏览器通过 `StationModalCoordinator` 统一锁定世界输入；鼠标与键盘可切换分类和
  条目，关闭后恢复玩家控制、交互提示与原焦点。终端位置经过可见录帧调整，避免
  设备在大厅相机安全边缘被裁切。
- 为已经完成 T-112、但保存于新增异常记录之前的 schema v2 存档增加幂等内容兼容：
  继续游戏时补齐白噪星与旧铭牌异常记录，不改变 schema，也不提前解锁导航。
- Godot 4.7.1 导入与解析、64 套自动测试、T-113 专项、站点/T-106/T-112 回归、
  三张 640×360 可见录帧、M1 基础聚合、M0 完整闭环与
  `./scripts/check_project.sh` 均通过。
- 已知限制：档案终端仍是 M1 灰盒视觉，内容成长感与老皮语气留到 Gate E 人工
  判断；白噪正式订单、出发资格与路线仍未开放。

---

## T-114 — 白噪星导航解锁与出发准备

**State:** `Done`
**Priority:** P0  
**Goal:** 玩家能理解为什么现在可以前往白噪星，以及需要怎样的飞船配置。

### 范围

- 解锁白噪星导航节点、主线订单占位和风险说明。
- 驾驶舱增加白噪星目的地预览。
- 检查特高压电屏蔽罩与主线章节。

### 明确不做

- 不实现白噪星正式路线。

### 验收标准

- 未完成回访/未装模块时不可出发。
- 完成后导航、订单和驾驶舱状态一致。

### 依赖

T-113。

### 实现结果（2026-07-26）

- 建立唯一的白噪星出发资格投影，明确区分“前置主线未完成”“模块未取得”
  “模块未安装”“资格已满足但正式航路待开放”和未来可出发状态；订单终端、
  飞船配置台与驾驶舱导航读取同一结果，不各自复制条件。
- 赤砂回访结算会原子解锁白噪星导航；已完成精确回访订单、但保存于本任务之前的
  schema v2 存档会幂等补齐该导航解锁。缺少回访完成标记、章节或屏蔽罩的档案
  不会被误解锁。
- 白噪星预览清楚显示约 1.28g 重力、电磁暴雪、低能见度与特高压电屏蔽罩要求；
  玩家拥有但未安装模块时会获得明确配置提示，安装后显示航行资格已经满足。
- 白噪星球与主线订单继续保持 `REGISTERED_ONLY`，没有
  `flight_scene_path`；订单不可接取、驾驶舱不可启动旅行，因此本任务没有借用
  赤砂路线或提前实现 T-120。
- Godot 4.7.1 导入与解析、65 套自动测试、T-114 专项场景烟雾、三张
  640×360 可见录帧、M1 基础聚合、M0 完整闭环和
  `./scripts/check_project.sh` 均通过。
- 已知限制：白噪星正式订单、航路和环境仍待 T-120；资格说明的主观清晰度与
  M1 成长感集中交由 Gate E / T-119 人工验收。

---

## T-119 — Gate E：M1 基础、赤砂回访与白噪星资格人工验收

**State:** `Human Check`
**Priority:** P0  
**Goal:** 在制作第二颗完整星球前，确认 M1 成长、回访和迁移基础成立。

### 人工重点

- M0 v1 存档迁移是否可靠。
- 赤砂回访是否让首单显得有后果。
- 屏蔽罩获取是否自然且不刷取。
- 档案终端和站点变化是否有成长感。
- 白噪星出发门槛是否清楚。

### 验收标准

- 自动检查、M0 回归和 v2 迁移通过。
- 进入 `Human Check` 后停止。
- 用户通过后 T-120 解锁。

### 自动预检结果（2026-07-26）

- 已完成 M0 首单的 v1 与较早 schema v2 存档会把历史完成订单作为权威事实，
  幂等补齐站点教程完成和 `story_red_sand_order_completed`；继续游戏进入稳定
  大厅后，目标明确指向赤砂星屏蔽改装回访，且正常订单条件允许接取。
- 兼容步骤不发放信用点、关系、模块或白噪星资格；未完成首单的新档仍不能越级
  进入回访。白噪星与其主线订单继续为 `REGISTERED_ONLY`。
- 新增 `--m1-debug=gate_e` 隔离入口，从首单完成、回访未接取的大厅开始；自动
  存档、玩家剧情档和玩家设置均被隔离，`F6` 可恢复完全一致的初始快照。
- 66 套自动测试、完成/未完成 v1→v2 迁移、较早 v2 兼容、赤砂回访完整闭环、
  屏蔽罩、档案终端、白噪资格、七个 M1 调试入口、Metal 可见启动、M1 基础聚合、
  M0 新游戏完整闭环和 `./scripts/check_project.sh` 均通过。
- Gate E Round 1 的 20–40 分钟试玩命令、正式流程、白噪驾驶舱预览与已知限制
  已记录在 `Docs/M1/05_M1_HUMAN_CHECKS.md`。T-120 在用户通过 Gate E 前保持
  `Blocked`。

### Gate E Round 1 人工反馈（2026-07-26）

- Result：`Rework required`。
- 飞船配置槽把“本次订单不使用”覆盖到真实模块名称与说明上，且订单角色与
  拥有/安装状态没有清楚分层。
- 档案终端应当从开局就保留可见旧机柜，精确状态只负责通电、交互和浏览；回访
  后还需要一次性接通剧情，已有解锁存档不能重放。
- 订单终端目前只统计历史数量，必须让 `COMPLETED / ARCHIVED` 订单逐条可选择并
  查看只读详情，同时保持 M0 兼容别名只显示一次。
- 赤砂回访的驾驶舱、旅行、短路线、着陆和结算仍混入首单语境；需以回访合同
  提供独立稳定文案与已读标记。
- 三段短路线本身继续保留，但必须在出发前和路线第一段明确说明维修服务航道会
  跳过已备案的外层进近，只进入验证后的后段安全走廊。
- 赤砂路线 HUD 主题缺少既有中文系统字体栈，Gate E 所有可见界面需统一验证
  中文字形覆盖，不得用删中文或改英文规避。
- 本轮只修复以上六项 Gate E 阻塞；T-110 至 T-114 保持 `Done`，T-120 保持
  `Blocked`，不得开始白噪星路线。

### Gate E Round 2 自动预检（2026-07-26）

- 配置槽已将模块身份、订单角色和拥有/安装状态拆分；陨石激光炮即使本单不要求
  仍显示真实名称与说明，并可正常装卸，必需模块检查未改变。
- 旧档案机柜外壳从开局可见但断电、不可浏览；回访结算后由一次性待处理标记触发
  老皮接通剧情，精确状态负责通电与交互，读取已解锁存档不会重播。
- `COMPLETED / ARCHIVED` 历史现可逐条选择并查看只读订单、双方、目的地、货物、
  基础报酬与已知剧情结果；未保存的飞行结算明确标注，M0 alias 仍只生成一条。
- `RedSandRevisitContract` 已集中提供回访驾驶舱、手动热点、广播、货舱、公司
  说明、旅行、三段路线、着陆、抵达和结算文案，并使用独立已读标记，不复用 M0
  首次配送语境。
- 三段 HUD 明确为“维修服务航道 → 屏蔽材料与维修场观察 → 变化后的维修场
  着陆”；出发前和首段均说明完整进近已备案、本次从验证后的后半段安全走廊切入。
- Gate E 可见场景共用中文字体资源；专项测试逐字验证全部实际本地化字符，
  11 张 640×360 Metal 录帧未发现十六进制缺字框、空白方块或乱码。
- Godot 4.7.1 headless import、67 套自动测试、T-111～T-114、v1→v2 迁移、
  M1 基础聚合、M0 完整闭环和 `./scripts/check_project.sh` 全部通过。
- T-119 与阶段 B 已返回 `Human Check`；T-110 至 T-114 保持 `Done`，T-120
  保持 `Blocked`，等待 Gate E Round 2 人工试玩。

### 依赖

T-114。

---

# C. 白噪星完整订单

## T-120 — 白噪星路线灰盒与环境配置

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 第二颗星球从结构和手感上证明不是赤砂换皮。

### 范围

- 完整进入/配送路线灰盒。
- 冰原、冰洞、极光、电磁暴雪与地下档案入口。
- 较强重力工作值 1.2–1.35。
- 检查点、快速/常规/观景路径。

### 明确不做

- 不复刻赤砂雷达和沙漠阶段。
- 不做最终美术。

### 验收标准

- 路线目标约 1–3 分钟。
- 地形与碰撞清楚。
- M0 飞船基线在较强重力下仍可控。

### 依赖

T-119。

---

## T-121 — 电磁暴雪、屏蔽与低能见度机制

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 白噪星的危险同时兑现环境、文明与模块要求。

### 范围

- 电磁干扰影响导航/HUD/控制提示，但不随机夺取玩家控制。
- 屏蔽罩降低干扰和资源风险。
- 低能见度提供可访问路线提示。
- 危险参数数据化。

### 明确不做

- 不做控制反转、永久失明或不可读噪声。

### 验收标准

- 有/无屏蔽罩差异清楚。
- 高对比、路线提示和慢动作辅助有效。
- 干扰不会破坏存档/输入状态。

### 依赖

T-120、T-111。

---

## T-122 — 白噪星主线订单、货物与驾驶舱内容

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 用一件记忆档案货物让玩家理解当地文明，而不是只读百科。

### 范围

- 使用稳定 ID 与候选显示名建立主线货物和客户历史。
- 驾驶舱加入出发、暴雪风险和记忆伦理对话。
- 公司描述与当地实际用途存在具体差异。

### 明确不做

- 不锁定候选 NPC/货物显示名。
- 不揭示中继网创造者。

### 验收标准

- 订单信息在出发前可理解。
- 对话符合老皮和当地角色规则。
- 选择只写入局部变量。

### 依赖

T-121、T-104。

---

## T-123 — 白噪星背景、VFX、音频与星球进入表现

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 冰原、极光与暴雪形成独立的进入记忆。

### 范围

- Production placeholder 色板和多层背景。
- 极光、高压辉光、雪粒、电磁干扰 VFX。
- 独立环境音和短主题动机。
- 保持碰撞层可读。

### 明确不做

- 不承诺最终商业美术或完整配乐。

### 验收标准

- 与赤砂星截图可一眼区分。
- 闪烁/噪声受辅助设置控制。
- 资产来源有记录。

### 依赖

T-120、T-121。

---

## T-124 — 白噪星目的地区域、核心 NPC 与局部选择

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 让“保存、删除或公开记忆”的冲突落在具体人物身上。

### 范围

- 场景式交付 + 1–2 屏冰下档案区。
- 1 名核心 NPC、1 名可选角色/终端。
- 局部选择改变关系、图鉴与后续对白。
- 获得中继网历史记录片段。

### 明确不做

- 不做大型地下城、复杂解谜或多结局关卡。

### 验收标准

- NPC 有具体目标、日常细节和公司立场。
- 选择不会阻塞主线。
- 场景模态与可读性沿用 M0 规则。

### 依赖

T-122、T-123。

---

## T-125 — 白噪星结算、许可、图鉴与回访种子

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 把白噪星结果连接到后续成长与穹林星线索。

### 范围

- 关系、许可、纪念品、图鉴和站点档案变化。
- 记录记忆选择。
- 解锁白噪回访种子与穹林前置信息。

### 验收标准

- 结果保存和继续正确。
- 差结果不造成主线死档。

### 依赖

T-124。

---

## T-126 — 白噪星必做支线：被退回的记忆盒

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 用较短支线展示个人记忆与公共档案之间的温度，而不是重复主线。

### 范围

- 一份短路线/复用分支支线。
- 将私人记忆盒送还原持有人或档案代理。
- 玩家决定公开索引、保持私密或仅归还副本。
- 关系/图鉴/信用点结果。

### 明确不做

- 不新增完整第二条长路线。

### 验收标准

- 可失败或减报酬但不阻塞主线。
- 选择至少产生一处后续反馈。
- 不复用主线同一伦理反转原句。

### 依赖

T-124、T-103。

---

## T-129 — Gate F：白噪星完整订单人工验收

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 确认第二颗完整星球在玩法、文明与氛围上真正成立。

### 人工重点

- 较强重力和屏蔽罩是否有意义。
- 电磁暴雪是否紧张但可读。
- 记忆档案文明是否通过人物而非百科成立。
- 白噪与赤砂是否明显不同。
- 支线是否像自愿探索而非清单劳动。

### 依赖

T-125、T-126。

---

# D. 多星球成长、支线与回访共用系统

## T-140 — 支线终端、报酬与失败/放弃 UX

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 玩家能在不被任务列表淹没的情况下理解支线的角色价值和风险。

### 范围

- 支线入口、推荐配置、加急标记、关系/图鉴收益和放弃确认。
- 结算显示时间、货物和选择修正。

### 明确不做

- 不做每日任务、无限刷新或稀有度系统。

### 依赖

T-129、T-108。

---

## T-141 — 赤砂星扩展支线：配额之外的滤芯

**State:** `Stretch`  
**Priority:** P2  
**Goal:** 在预算允许时让赤砂星继续变化，并补充普通居民视角。

### 范围

- 短配送或回访互动。
- 送达净水滤芯，改变维修场一处可见状态。
- 不提供主线必需资源。

### 明确不做

- 不影响 T-159/T-179/T-199 依赖；预算不足可 Deferred。

### 依赖

T-140、T-112。

---

## T-142 — 飞船模块成长与配置可读性

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 三个剧情模块产生清晰取舍，而不是配置界面堆满被动百分比。

### 范围

- 屏蔽罩、生物信号隔离舱、横风稳定器的数据和槽位规则。
- 显示当前星球必须/推荐模块。
- 保留激光炮与护盾备用电源。
- 配置不足时提供确定获取线索。

### 明确不做

- 不做随机装备、品质、拆解或复杂科技树。

### 依赖

T-129、T-104。

---

## T-143 — 关系、许可与玩家方式反馈整合

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 武装、选择、支线和货物结果被记住，但不会制造庞大分支。

### 范围

- 统一写入/读取关系、许可、武装倾向和局部选择。
- 提供对话白名单条件和调试查看。
- 关系变化有少量可见解释。

### 明确不做

- 不做复杂派系模拟或可刷声望条。

### 依赖

T-140、T-142。

---

## T-149 — M1 中段回归与穹林星资格

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 在制作第三颗完整星球前，确认主线、支线、成长和存档可持续扩展。

### 范围

- 从 M0/v2 迁移到白噪完成的整合烟雾。
- 解锁穹林星前置、模块和导航占位。
- 检查支线结果、关系和图鉴。

### 依赖

T-142、T-143；T-141 不阻塞。

---

# E. 穹林星完整订单与低空投放

## T-150 — 穹林星路线灰盒与树冠空间

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 用巨树、树洞和垂直通道建立轻盈但空间受限的飞行身份。

### 范围

- 完整进入/配送路线。
- 较低重力、树冠通道、树洞与孢子云。
- 路线总体向右，允许上下通道与短距离回退。

### 明确不做

- 不做开放森林或程序生成树洞。

### 依赖

T-149。

---

## T-151 — 武器封存、信任与双路线便利

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 让和平/武装倾向产生实际表达，但不分裂主线制作量。

### 范围

- 进入核心树冠前提供封存激光炮选择。
- 封存：更高信任和较安全核心路线。
- 拒绝：保留激光，走较危险外缘路线并减少部分角色便利。
- 两条路径回到同一主线节点。

### 明确不做

- 不加入敌人战斗或永久没收模块。

### 验收标准

- 选择可撤销/恢复到后续配置。
- 关系和对白正确记录。
- 两条路径内容量受控。

### 依赖

T-150、T-143。

---

## T-152 — 货物信号、生物吸引与生态危险

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 让货物限制有生态原因，而不是把飞行生物做成传统敌人。

### 范围

- 货物信号吸引移动生物群/路线压力。
- 生物信号隔离舱降低吸引范围或频率。
- 生物不使用血条、射击或敌人 AI 战斗循环。

### 明确不做

- 不允许激光伤害当地生物。

### 验收标准

- 有/无模块差异清楚。
- 危险可通过速度、路线和模块应对。
- 辅助提示可读。

### 依赖

T-150、T-142。

---

## T-153 — 穹林星主线订单、角色与驾驶舱内容

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 将运输需求、生态边界和武装态度落到具体人物与货物上。

### 范围

- 主线货物、客户历史、老皮对话和核心 NPC。
- 角色回应封存选择、模块与飞行方式。
- 到达区域 1–2 屏。

### 依赖

T-151、T-152、T-104。

---

## T-154 — 穹林星 Production Placeholder 美术、VFX 与音频

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 巨树、孢子与生态文明形成独立的可分享视觉段落。

### 范围

- 独立色板、树冠背景、建筑轮廓、生态 VFX 与主题动机。
- 飞行生物表现为生态存在，不是红色敌人标记。

### 依赖

T-150、T-152。

---

## T-155 — 穹林星目的地剧情、关系与站点生态角

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 让封存武器与生态选择在人物关系和快递站中留下反馈。

### 范围

- 完成主线对话、局部选择、纪念品、图鉴和关系结果。
- 解锁站点生态角与后续老皮内容。
- 获得/升级生物信号隔离舱。

### 依赖

T-153、T-154。

---

## T-156 — 穹林星必做支线：孢子季急投

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 将低空投放作为有叙事意义的第二种正式交付方式。

### 范围

- 向固定、宽容的树冠接收环低空投放生态稳定剂/医疗孢囊。
- 使用高度、速度和位置窗口。
- 可部分成功、失败和重试。
- 支线角色与关系结果。

### 明确不做

- 不做移动目标、战斗投弹或复杂弹道。

### 依赖

T-155、T-107、T-140。

---

## T-159 — Gate G：穹林星与低空投放人工验收

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 确认武装态度、生态飞行和第二交付方式共同成立。

### 人工重点

- 封存/保留武器是否是有意义但不沉重的选择。
- 生物压力是否像生态而非战斗。
- 树冠路线是否可读且不同于前两星。
- 低空投放是否清楚、宽容并值得复用。
- 生态角和角色反馈是否让结果有余韵。

### 依赖

T-156。

---

# F. 群潮星完整订单与主线高潮

## T-170 — 群潮星路线灰盒、海面与岛屿结构

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 用海洋、岛屿和天气塔建立 M1 最具动势的进入路线。

### 范围

- 完整星球进入、海面飞行、岛屿通道与天气塔阶段。
- 海浪/岛屿只在可见时启用对应碰撞。
- 最终进场包含有限移动/浮动平台，但不做复杂物理船。

### 依赖

T-159。

---

## T-171 — 横风、天气塔与稳定器机制

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 让群潮星由风与航路控制主导，而不是再做一套雷暴。

### 范围

- 数据化横风、阵风预警和天气塔影响区。
- 横风稳定器降低扰动但不自动驾驶。
- 风向/强度在 HUD 和环境中可读。

### 明确不做

- 不做完整气象模拟或随机不可预期风暴。

### 依赖

T-170、T-142。

---

## T-172 — 群潮星主线订单、天气控制货物与驾驶舱内容

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 将天气与航路控制权冲突转化为玩家亲手送达的一件关键货物。

### 范围

- 主线货物、三个利益方、客户历史和老皮对话。
- 记录此前星球关系与中继线索。
- 解锁横风稳定器的确定路径。

### 依赖

T-171、T-104。

---

## T-173 — 群潮星 Production Placeholder 美术、VFX 与音频

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 海面、风墙、天气塔和城邦灯光形成 Demo 高潮视觉。

### 范围

- 岛屿、云墙、海浪、雨幕、塔信标、主题动机与危险音频。
- 保持平台和碰撞轮廓清楚。

### 依赖

T-170、T-171。

---

## T-174 — 群潮星目的地区域与控制权局部选择

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 让玩家决定天气塔如何被管理，但不制作三套主线结局。

### 范围

- 1–2 名核心 NPC 与小型天气塔区域。
- 选择：单一城邦、城邦联盟、公司临时托管。
- 三种选择改变关系、对白、结尾变量和图鉴。
- 所有选择都进入同一中继揭示主线节点。

### 明确不做

- 不制作三套不同终章关卡。

### 依赖

T-172、T-173、T-143。

---

## T-175 — 中继网第一层揭示与站点观测室

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 在回答足够少的前提下给 Demo 一个明确高潮和继续游玩的理由。

### 范围

- 明确中继网早于公司、四星都曾接触它。
- 老皮听见普通设备无法识别的残缺讯息。
- 快递站解锁中继观测室。
- 显示公司星图以外的收件坐标。

### 明确不做

- 不揭示创造者、真实用途、老皮完整关系或最终结局。

### 依赖

T-174。

---

## T-176 — 群潮星必做支线：风眼前的灯

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 用加急支线验证时间奖励和岛屿日常，而不影响主线高潮。

### 范围

- 向天气维护队送达导航信标电池。
- 有宽松目标时间；超时减报酬和关系，不硬失败。
- 复用路线分支和横风机制。

### 依赖

T-174、T-108、T-140。

---

## T-179 — Gate H：群潮星与 Demo 高潮人工验收

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 确认第四颗星球、局部选择和中继揭示构成真正高潮。

### 人工重点

- 横风与稳定器是否有意义且公平。
- 群潮是否与前三星明显不同。
- 控制权选择是否有反馈但不显得虚假。
- 中继揭示是否清楚又保留神秘。
- 加急支线是否增加节奏而非压力劳动。

### 依赖

T-175、T-176。

---

# G. 四星整合、导出与最终 Demo 验收

## T-190 — M1 主线章节、解锁与回访整合

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 将五份主线订单连接为一段清楚的序章、发展、高潮和悬念结尾。

### 范围

- 章节推进、导航解锁、站点变化、驾驶舱内容和回访顺序。
- 不允许越级、重复奖励或死档。

### 依赖

T-179。

---

## T-191 — 三份必做支线与部分成功整合

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 支线在完整 Demo 中可发现、可选择、可失败并产生持续反馈。

### 范围

- 白噪、穹林、群潮支线完整状态。
- 时间、货损、放弃、关系、图鉴和结算。
- 赤砂 Stretch 支线仅在已完成时纳入。

### 依赖

T-190、T-126、T-156、T-176。

---

## T-192 — v2 存档迁移、章节恢复与坏档回归

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 玩家可在 2–4 小时 Demo 中安全退出、继续和迁移旧档。

### 范围

- 覆盖所有主线阶段、支线、关系、许可、图鉴、模块和站点状态。
- v1→v2、v2 备份、坏档和中途稳定节点恢复。

### 明确不做

- 不恢复飞行中场景节点。

### 依赖

T-190、T-191。

---

## T-193 — 多星球图鉴、纪念品、关系与站点变化收敛

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 玩家完成 Demo 后能够看见这段旅程在家中留下的全部痕迹。

### 依赖

T-192。

---

## T-194 — 四星视觉、音频、本地化与可访问性一致性

**State:** `Blocked`  
**Priority:** P1  
**Goal:** M1 达到可分享 production placeholder，而不是四套不一致灰盒。

### 范围

- 色板、像素密度、UI、环境音、主题动机、VFX 和中文换行检查。
- 英文 Key/占位列完整。
- 闪光、震动、路线提示和高对比模式跨星球有效。

### 依赖

T-193。

---

## T-195 — M0/M1 全量测试、性能与生命周期回归

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 四星内容不造成状态泄漏、音频叠加、性能恶化或旧流程回归。

### 范围

- 完整自动测试、关键场景实例化、全章节烟雾、存档、失败重试和资源生命周期。
- macOS 60 FPS 目标和长流程内存观察。

### 依赖

T-194。

---

## T-196 — macOS Demo 打包与 Windows 导出启动验证

**State:** `Blocked`  
**Priority:** P1  
**Goal:** M1 能以可分享构建运行，而不仅存在于编辑器。

### 范围

- macOS 可分享包。
- Windows 导出模板/构建的启动级验证。
- 版本、保存路径、Credits/Attribution 与基本退出流程。

### 明确不做

- 不接入 Steam SDK、签名公证承诺或自动更新。

### 依赖

T-195。

---

## T-197 — Demo 文本、节奏与已知限制打磨

**State:** `Blocked`  
**Priority:** P1  
**Goal:** 清理会破坏第一印象的占位说明、重复对白和节奏断点。

### 明确不做

- 不以“打磨”为名增加新系统、新星球或大支线。

### 依赖

T-196。

---

## T-198 — 四星 Demo 完整流程烟雾与试玩包

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 为最终人工验收准备从新游戏到群潮结尾的可重复路径。

### 范围

- 新游戏、M0 首单、迁移档、五份主线、三份支线、失败重试、结算、存档、继续和结尾。
- 提供 2–4 小时试玩说明与分段继续点。

### 依赖

T-197。

---

## T-199 — Gate I：M1 Four-Planet Demo 最终人工验收

**State:** `Blocked`  
**Priority:** P0  
**Goal:** 判断 Demo 是否具备对外分享资格，并决定是否进入 M2+。

### 最终验收问题

- 四颗星球是否像四个不同地点与文明，而不是换皮关卡？
- 主线、支线、回访、模块和站点成长是否形成连续旅程？
- 老皮与核心 NPC 是否让玩家愿意继续了解？
- 飞行、危险、交付和失败重试是否公平且有变化？
- 群潮高潮与中继网悬念是否足以支撑 Demo 结尾？
- 2–4 小时内容是否密度合理、没有明显刷取和空白拖时？
- 构建、存档、继续、可访问性和性能是否可接受？

### 完成条件

- 用户明确接受，或只剩有限非阻塞发布打磨项。
- Gate E–I 全部通过。
- 所有必做任务 Done；T-141 可 Done 或 Deferred。
- 创建 M1 里程碑提交/标签。
- 未经新规划不直接进入 M2+。

### 依赖

T-198。

---

## 任务卡维护规则

- 新任务必须写明玩家价值、范围、明确不做、验收和依赖。
- M0 回归问题在造成它的当前任务内处理，不重新打开 M0 任务表。
- 候选姓名/货物名在用户确认前保持 PROVISIONAL，代码使用稳定角色/订单 ID。
- Stretch 任务不能阻塞任何强制 Gate。
- 实际实现历史由 Git 表达，任务卡只保留当前合同和重要验收结果。
