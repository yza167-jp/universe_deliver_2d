# Development Tasks — M0 Playable Spine

本文件只管理当前里程碑 M0。M1 的另外三颗星球只在 `Docs/04_DEMO_SCOPE.md` 中定义，不进入当前任务队列。

## 状态约定

- `Blocked`：依赖未完成。
- `Ready`：依赖已满足，可以由主 Agent 领取。
- `In Progress`：当前唯一正在开发的任务。
- `Human Check`：自动检查已通过，等待玩家试玩。
- `Done`：实现、检查和必要试玩均已完成。
- `Deferred`：明确移出 M0。

主 Agent 一次只允许一个 `In Progress` 任务。完成后更新本文件，再提交。

### 预定人工验收节点

M0 的强制 Human Check 任务只有：

- T-015：大厅接单闭环人工试玩
- T-036：飞行手感人工试玩
- T-045：赤砂星完整路线人工试玩
- T-055：M0 完整闭环人工验收

到达以上任务后，主 Agent 必须停止继续开发并等待用户试玩。

其他任务默认在自动验收与必要烟雾测试通过后标记为 Done。
只有当该任务存在无法由自动测试判断的关键视觉、可读性、声音、
叙事或操作风险时，才临时标记为 Human Check。

不要仅因为任务包含 UI、动画或占位美术，就自动要求 Human Check。
飞行单项手感集中在 T-036 验收，赤砂星单项关卡表现集中在 T-045 验收。

## M0 进度

| 阶段 | 目标 | 状态 |
|---|---|---|
| A | 项目、测试与数据基础 | Done |
| B | 快递站、老皮与接单闭环 | Done |
| C | 第一人称驾驶舱与旅行 | Done |
| D | 飞行实验室与核心手感 | Done |
| E | 赤砂星完整飞行路线 | Done |
| F | 落地剧情、结算、存档与 M0 打磨 | In Progress（T-050 Done；T-051 Ready） |

---

# A. 项目、测试与数据基础

## T-001 — 初始化 Godot 项目与像素画基线

**State:** `Done`\
**Priority:** P0\
**Goal:** 仓库可以被 Godot 4.7.1 正常打开，并从第一天使用一致的 2D 像素显示设置。

### 玩家价值

所有后续场景建立在稳定、清晰、不产生像素抖动的画面基线上。

### 范围

- 在仓库根目录创建 Godot 项目。
- 使用 Compatibility renderer。
- 内部视口 640×360，16:9。
- 使用 `viewport` 拉伸、`keep` 宽高比和整数倍缩放。
- 默认 2D 纹理过滤为 nearest。
- 创建最小 `Main` 场景，显示标题、版本和“项目初始化成功”。
- 配置基础 Input Map action 名称，但暂不实现完整控制。
- 创建适合 Godot 的 `.gitignore`。

### 明确不做

- 不制作主菜单美术。
- 不实现大厅、飞船或剧情。
- 不引入插件。

### 验收标准

- Godot 4.7.1 可以从仓库根目录导入项目。
- 项目运行后显示最小 Main 场景，无脚本错误。
- 1280×720 或 1920×1080 窗口下像素保持整数缩放。
- Compatibility renderer 和内部视口配置已提交到项目设置。
- `.godot/` 等本机生成目录不进入 Git。

### 检查

- 使用 Godot headless 方式导入/解析项目。
- `git diff --check`。

### 依赖

无。

### 建议提交

`初始化 Godot 2D 项目与像素画显示基线`

---

## T-002 — 建立目录结构、检查脚本与最小测试运行器

**State:** `Done`\
**Priority:** P0\
**Goal:** 每个后续提交都能通过一个快速、统一的仓库检查入口验证。

### 范围

- 建立 `scenes/`、`scripts/`、`data/`、`assets/`、`tests/` 等基础目录。
- 创建 `scripts/check_project.sh`，支持 `GODOT_BIN` 覆盖 Godot 可执行文件。
- 创建不依赖第三方插件的轻量测试运行器。
- 检查至少包括：项目 headless 导入、GDScript 解析、测试脚本、`git diff --check`。
- 创建一个必定通过的示例测试和一个场景实例化烟雾测试。
- 在 README 中补充实际可用的运行命令。

### 明确不做

- 不引入 GUT 或其他测试插件。
- 不搭建 CI 云服务。

### 验收标准

- `./scripts/check_project.sh` 在项目根目录可执行。
- 检查失败时返回非零退出码并给出可读错误。
- 测试运行器可发现并运行测试。
- Main 场景可以 headless 实例化。

### 依赖

T-001。

### 建议提交

`建立 Godot 项目检查脚本与测试骨架`

---

## T-003 — 建立最小应用场景流与全局状态

**State:** `Done`\
**Priority:** P0\
**Goal:** 大厅、驾驶舱、飞行和目的地场景能通过清晰状态流连接，而不是相互硬编码。

### 范围

- 创建应用根场景和 Scene Router。
- 定义 M0 所需阶段：`MAIN_MENU`、`STATION`、`COCKPIT`、`FLIGHT`、`ARRIVAL`、`RESULTS`。
- 建立最小 `GameState`，保存当前订单、目的地、货物、飞船配置和剧情标记的运行时状态。
- 创建空白占位场景并允许开发模式快速切换。
- 场景切换时清理旧场景，不泄漏重复节点。

### 明确不做

- 不实现正式 UI。
- 不把所有业务逻辑塞进 GameState。
- 不实现完整存档。

### 验收标准

- 可以按顺序在六个占位场景间切换。
- 非法阶段切换会被拒绝或记录清晰错误。
- 场景引用不使用易失效的绝对文件系统路径。
- 有自动测试覆盖合法与非法状态切换。

### 依赖

T-002。

### 建议提交

`建立 M0 应用场景流与运行时状态`

---

## T-004 — 定义核心数据 Resource 与引用验证

**State:** `Done`\
**Priority:** P0\
**Goal:** 星球、订单、货物和模块由数据驱动，剧情内容不散落在场景脚本里。

### 范围

创建静态类型 Resource：

- `PlanetDefinition`
- `OrderDefinition`
- `CargoDefinition`
- `ShipModuleDefinition`
- `CharacterDefinition`

定义稳定字符串 ID、显示文本 Key、资源引用和 M0 必需字段。创建数据注册表与验证器，加入最小赤砂星占位数据。

### 明确不做

- 不写完整四星内容。
- 不实现通用数据库或编辑器插件。
- 不在此任务中实现订单 UI。

### 验收标准

- M0 数据能在 headless 测试中加载。
- 重复 ID、空 ID、缺失引用和非法数值会导致测试失败。
- 订单可以引用目的星球、货物、要求模块和交付方式。
- 货物可以声明 Boost 限制、碰撞容忍和吸引风险标签。

### 依赖

T-002。

### 建议提交

`定义星球订单货物与飞船模块数据资源`

---

## T-005 — 建立本地化与基础对话系统

**State:** `Done`\
**Priority:** P0\
**Goal:** 老皮和星球 NPC 的完整剧情能够以数据形式播放，并从一开始避免硬编码文本。

### 范围

- 建立中文源语言本地化表和英文占位列。
- 创建基础对话数据结构：发言者、文本 Key、表情/姿态、可选条件、下一行和简单选择。
- 创建通用对话 UI，支持继续、快速显示、历史回看和跳过已读文本的基础接口。
- 支持设置剧情标记和触发简单信号。
- 创建一段老皮测试对话。

### 明确不做

- 不做复杂可视化剧情编辑器。
- 不做配音、嘴型同步或完全分叉剧情。
- 不加入外部对话插件。

### 验收标准

- 中文文本正常显示，不出现缺字或溢出。
- 对话可以由 Resource/结构化数据启动并结束。
- 缺失本地化 Key 会在测试中报告。
- 简单选择可以写入一个剧情标记，但不产生独立主线分支。

### 后续扩展说明

- 通用对话的“整段跳过”能力由 T-023 扩展；T-005 保持已完成的基础播放、继续、快速显示、历史与已读接口范围。

### 依赖

T-002、T-004。

### 建议提交

`建立本地化与基础剧情对话系统`

---

## T-006 — 建立设置、辅助选项与输入重映射骨架

**State:** `Done`\
**Priority:** P1\
**Goal:** 飞行辅助和可读性设置在系统早期就有稳定落点。

### 范围

- 设置数据：主音量、音乐、音效、屏幕震动、文字速度。
- 辅助选项：慢动作辅助、路线提示、高对比度地形。
- 飞行辅助驾驶强度预留 0%–100%，默认 75%。
- 建立 Input Map action 与键鼠默认绑定。
- 设置仅存本地，不与剧情存档耦合。

### 明确不做

- 不制作最终设置界面美术。
- 不承诺手柄完整支持。

### 验收标准

- 设置可保存并在重启后恢复。
- 输入业务代码只读取 action。
- 三项辅助选项可以从调试设置界面切换并发出状态信号。

### 依赖

T-002、T-003。

### 建议提交

`建立输入映射与辅助选项设置骨架`

---

# B. 快递站、老皮与接单闭环

## T-010 — 灰盒快递站大厅

**State:** `Done`\
**Priority:** P0\
**Goal:** 玩家拥有一个可辨认、可返回、未来能逐步升级的家。

### 范围

- 2D 俯视单层大厅，约 2–3 个屏幕面积。
- 包含订单终端、工作台/机库入口、驾驶舱入口、老皮休息区、纪念品墙占位。
- 使用 TileMapLayer、简单碰撞和占位像素素材。
- 预留后续场景状态变化点。

### 明确不做

- 不制作完整空间站建设。
- 不制作多个楼层或大型地图。

### 验收标准

- 场景布局清晰，关键交互点可从入口快速抵达。
- 不存在卡住玩家的碰撞死角。
- 640×360 下仍能看清交互对象。

### 依赖

T-003。

### 建议提交

`搭建边境快递站大厅灰盒场景`

---

## T-011 — 玩家俯视移动与通用互动

**State:** `Done`\
**Priority:** P0\
**Goal:** 玩家能自然地在大厅和小型目的地区域中行走、观察并交互。

### 范围

- CharacterBody2D 八方向移动。
- 可调加速度、减速度和移动速度。
- 交互探测、提示和优先级。
- 基础待机/行走占位动画。
- 通用 `Interactable` 接口或小型组件。

### 明确不做

- 不做战斗、翻滚、背包或复杂导航。

### 验收标准

- 键盘移动稳定，无对角线加速。
- 靠近可交互物时显示清晰提示。
- 同时重叠多个对象时选择规则稳定。
- 自动测试覆盖交互候选选择的纯逻辑。

### 依赖

T-010、T-006。

### 建议提交

`实现大厅玩家移动与通用互动`

---

## T-012 — 老皮角色、互动与前期教程

**State:** `Done`\
**Priority:** P0\
**Goal:** 从游戏第一分钟建立老皮作为伙伴、同事和黑色幽默来源的存在感。

### 范围

- 老皮占位像素 Sprite：直立、会走、可说话。
- 大厅中的固定初始位置和简单面向/动作。
- 教程对话依次引导移动、互动、查看订单终端。
- 对话明确同声传译器存在，但不长篇解释设定。
- 教程结束后可重复进行日常短对话。

### 明确不做

- 不揭示老皮与终极秘密的关系。
- 不做复杂跟随 AI。
- 不把老皮写成纯提示机器人。

### 验收标准

- 新游戏时教程顺序不会因乱点而永久卡住。
- 老皮至少有一段温暖、一段公司黑色幽默台词。
- 已完成教程的存档不会强制重复完整教程。

### 依赖

T-005、T-011。

### 建议提交

`实现老皮角色与大厅新手教程`

---

## T-013 — 订单终端与赤砂星主线订单

**State:** `Done`\
**Priority:** P0\
**Goal:** 玩家理解“接单就是进入一个星球故事”，而不是抽取随机任务。

### 范围

- 订单终端 UI。
- 展示：货物信息、报酬、距离、星球环境、风险等级、必须配置、客户历史订单。
- 只显示一份可接取的赤砂星主线订单和未来订单占位。
- 接受订单后写入 GameState，并更新老皮台词。
- 主线订单不可因误操作永久放弃。

### 明确不做

- 不做刷新、随机生成、多单并行或任务列表管理。
- 不实现支线加急计时。

### 验收标准

- 订单数据全部来自 Resource。
- 无订单、已接取、已完成三种状态表现清楚。
- 缺少必须数据时按钮不可用并报告原因。
- 自动测试覆盖订单状态流转。

### 依赖

T-004、T-012。

### 建议提交

`实现赤砂星主线订单终端与接单流程`

---

## T-014 — 飞船配置与唯一飞船模块界面

**State:** `Done`\
**Priority:** P0\
**Goal:** 玩家在出发前理解飞船是一艘长期成长的载具，并能做一个简单配置选择。

### 范围

- 一艘固定飞船。
- 显示基础数值：船体、护盾、燃料、Boost、货舱。
- 三类模块槽位：动力、防护、功能。
- M0 可选功能模块：激光炮。
- 显示赤砂星订单的必须/推荐配置。
- 允许确认出发，未满足主线必须配置时给出明确提示。

### 明确不做

- 不购买多艘飞船。
- 不随机掉落模块。
- 不实现完整升级树。

### 验收标准

- 配置变化写入 GameState。
- 激光炮装卸可见且会影响后续飞行能力。
- 主线所需基础模块有确定获得方式，不要求刷钱。

### 依赖

T-013。

### 建议提交

`实现唯一飞船的基础模块配置界面`

---

## T-015 — 大厅接单闭环人工试玩

**State:** `Done`\
**Priority:** P0\
**Goal:** 在继续开发驾驶舱前，确认“家、伙伴、订单、配置”的节奏成立。

### 范围

- 将 T-010 至 T-014 连成可试玩路径。
- 修复阻塞与高优先级可用性问题。
- 提供从新游戏到进入驾驶舱前的试玩说明。

### 验收标准

- 自动检查全部通过。
- 新玩家无需开发者解释即可完成大厅教程和接单。
- 进入 `Human Check`，主 Agent停止扩展并请求试玩。
- 试玩结论记录到本任务和 `Docs/08_DECISION_LOG.md`。

### 依赖

T-010 至 T-014。

### 人工重点

- 老皮是否像伙伴而非 UI。
- 大厅是否太大或太空。
- 订单信息是否让人想去赤砂星。
- 配置是否清楚但不过度复杂。

### Gate A 验收记录（2026-07-16）

- `./scripts/check_project.sh` 与主菜单到驾驶舱入口端到端烟雾测试通过。
- 640×360 Compatibility 录帧确认主菜单、目标引导与出发准备面板可读且未越界。
- 人工试玩结论待反馈；收到后更新本任务与 `Docs/08_DECISION_LOG.md`。

### Gate A Round 1 试玩反馈（2026-07-17）

**Result:** `Rework required`

- 对话打开后，场景交互提示仍显示在对话框上方。
- 最大化窗口仍受整数缩放限制而出现可避免的大面积黑边，订单终端信息过密、字号偏小。
- 左上角下一目标条过宽，遮挡大厅中央路线与关键目标。
- 本轮仅返工模态界面优先级、显示适配、订单终端可读性与紧凑目标提示；完成后返回 `Human Check`，不继续 T-020。

### Gate A Round 2 预检（2026-07-17）

**Result:** `Human Check`

- `./scripts/check_project.sh` 通过：Godot 4.7.1 导入、全部 GDScript 解析、25 个测试套件、全部专项烟雾测试与空白检查均通过。
- 自动覆盖对话/订单终端/工作台/出发确认四类模态：世界输入、交互提示和目标条会统一隐藏并正确恢复；同一次 `E` 交互不会跳过对话首行。
- 1280×720 可见检查确认对话无遮挡，目标条宽 232 px（约占安全宽度 38%）、最多两行且避开中央路线；订单终端主摘要与固定接单动作清晰，长信息可单列滚动查看。
- 真全屏诊断：屏幕 3024×1964，普通可用区 3024×1888，Godot 真全屏窗口 3024×1890；`viewport + keep + fractional` 选择 4.725×，内容为 3024×1701，仅保留比例必需的上下各约 94.5 px 黑边。旧整数 4× 会产生左右各 232 px、上下各约 225 px 的可避免黑边。
- F11 往返真全屏与窗口成功，未发现裁切、拉伸变形或模态 HUD 回归；等待玩家进行 Gate A Round 2 试玩。

### Gate A Round 2 试玩结论（2026-07-17）

**Result:** `Passed`

- 玩家确认四类模态打开后交互提示与任务追踪 HUD 正确隐藏，关闭后相关 HUD 正确恢复。
- 同一次 `E` 输入不会同时打开并推进对话。
- 当前 Mac 真全屏模式下的显示区域与文字可读性可以接受，订单终端在 1280×720 下仍可正常阅读。
- 任务目标栏的尺寸和遮挡情况可以接受。
- Gate A 正式通过；T-015 标记 `Done`，第一人称驾驶舱任务 T-020 解锁为 `Ready`。

### 建议提交

`打通快递站接单与出发前流程`

---

# C. 第一人称驾驶舱与旅行

## T-020 — 可交互第一人称驾驶舱灰盒

**State:** `Done`\
**Priority:** P0\
**Goal:** 星际旅行成为有陪伴感的宁静空间，而不是一张加载界面。

### 范围

- 固定 2D 第一人称驾驶舱画面。
- 可点击导航屏、窗外、老皮座位、公司终端、收音机和货舱指示器。
- 对可交互热点提供鼠标悬停与键盘焦点。
- 视窗外使用多层星空占位动画。

### 明确不做

- 不让玩家在巡航阶段自由驾驶方向。
- 不做 3D 驾驶舱。
- 不做复杂解谜。

### 验收标准

- 640×360 下热点清楚，不依赖像素级点击。
- 鼠标和键盘均可完成主要交互。
- 场景无热点重叠导致的误触。

### 依赖

T-015。

### 自动预检（2026-07-17）

- 已实现固定 2D 第一人称驾驶舱、六个无重叠热点、鼠标/键盘交互与三层星空占位动画。
- Godot 4.7.1 全量检查和 640×360 原生帧预检通过；构图、热点辨识与宁静氛围等待玩家确认。

### Human Check Round 1 试玩反馈（2026-07-17）

**Result:** `Rework required`

- 热点激活后只有描述文本，缺少可辨认的对话、设备面板或状态切换行为。
- 老皮热点的工具提示与底部详情栏重复显示同一段第三人称描述。
- Hover、Focused 与 Activated 三种状态语义不清，玩家无法确认点击是否真正触发。
- 本轮只返工统一激活入口、六个最小真实行为、模态输入锁与重复反馈；不进入 T-021。

### Human Check Round 2 自动预检（2026-07-17）

**Result:** `Human Check`

- Hover 只改变热点高亮；Focused 使用明确边框并在底栏显示“热点名 + `[E / Click]` 操作”；Activated 才触发真实结果。
- 鼠标点击与 Input Map `interact` 共用 `activate_hotspot()` 入口；导航、公司终端与货舱指示器打开各自内容的只读设备面板，老皮进入正式对话，收音机切换 `ON/OFF`，观察窗显示独立短暂观察反馈。
- 设备面板与对话期间锁住背景热点；`Escape` 或关闭按钮退出后焦点返回原热点。所有热点工具提示已清空，底栏不再重复详情文本。
- Godot 4.7.1 针对性测试、完整项目检查与 640×360 实际窗口键鼠烟雾测试均通过；等待玩家复测三态辨识、六类反馈和老皮陪伴感。
- 本轮仍未实现目的地确认、旅行状态或进入 `FLIGHT`；T-021 保持 `Blocked`。

### Human Check Round 2 试玩结论（2026-07-17）

**Result:** `Passed`

- 玩家接受当前第一人称驾驶舱作为 M0 灰盒：空间关系成立，导航、前窗、老皮座位、公司终端、收音机和货舱指示器均可清楚识别。
- 鼠标与键盘都能选择并激活主要热点；点击区域足够大，未发现明显热点重叠、误触或像素级点击要求。
- 当前星空、设备面板、老皮座位和占位反馈足以支撑后续旅行任务；T-020 正式通过并标记 `Done`。
- T-003 与 T-020 均已完成，T-021 解锁为 `Ready`；本轮不开始实现 T-021。

### 已知限制与后续打磨

- 部分热点激活后的中央临时文本可能与底部状态栏语义相近；这是非阻塞灰盒限制，不影响 T-020 验收。
- 对应设备获得正式面板、对话或实际功能时，中央区域应表达激活结果，底栏继续承担选中状态与操作提示，避免重复显示相同信息。
- 不为关闭 T-020 立即重构驾驶舱 UI，也不创建庞大的全局 UI 系统；在后续相关任务中做局部、可逆的收敛。

### 建议提交

`通过 T-020 驾驶舱灰盒人工验收`

---

## T-021 — 目的地设定与星际旅行状态流

**State:** `Done`\
**Priority:** P0\
**Goal:** 玩家亲手确认目的地，并感受到从站点出发到抵达目标星系的时间与距离。

### 范围

- 导航屏显示当前订单允许的目的地。
- 设定赤砂星后开始一段短旅行。
- 旅行有开始、巡航、接近三个阶段。
- 通过视窗、音频和 UI 逐步表现目标星系接近。
- 可跳过已看过的非关键旅行段，但首次主线不默认跳过。

### 明确不做

- 不制作自由星图探索。
- 不允许选择未解锁主线星球。

### 验收标准

- 未接订单不能错误出发。
- 旅行结束后进入 FLIGHT 场景并保留订单/配置状态。
- 返回大厅时状态不会重复触发出发。

### 依赖

T-020、T-003。

### 自动预检（2026-07-17）

**Result:** `Human Check`

- 导航屏只显示当前订单允许的赤砂星目的地；没有订单、未完成起飞确认、目的地不匹配、旅行进行中或旅行已完成时均会阻止出发并显示对应状态。
- 本地 `TravelSequenceController` 以可调时长驱动离站、巡航和接近三阶段；首次主线旅行不显示跳过按钮，恢复“已看过”标记后可跳过并正常完成状态交接。
- 视窗星速、赤砂星远近、阶段进度 UI 与三组轻量合成提示音同步变化；旅行完成后进入 `FLIGHT`，订单、目的地、货物和飞船配置保持不变，重新进入已完成驾驶舱不会重复出发。
- Godot 4.7.1 全量检查通过：27 个单元/场景测试套件、驾驶舱旅行专项烟雾、Gate A 端到端烟雾、全部脚本解析与空白检查均通过。
- 640×360 实际窗口预检确认导航确认面板、离站状态条、接近阶段行星放大与 `FLIGHT` 交接无明显遮挡或越界；旅行节奏、阶段音效和距离感仍需玩家确认。

### Human Check Round 1 试玩反馈（2026-07-17）

**Result:** `Rework required`

- 已通过：离站、巡航和接近三个阶段可辨认；赤砂星接近效果成立；约 12 秒总时长可接受；首次主线旅行不可跳过；进入 `FLIGHT` 后订单、目的地、货物和飞船配置保持正确。
- 需要返工：旅行状态信息不得覆盖前窗，改为复用底部状态栏；离站、巡航、接近及交接期间前窗必须完全被动；收音机开关需要可辨认的动态视觉与低音量循环声音反馈。
- 本轮只修复上述三个问题并补充回归测试；不重做已通过的旅行节奏与接近效果，不开始 T-022、T-023 功能或横版飞行。

### Human Check Round 2 自动预检（2026-07-17）

**Result:** `Human Check`

- 旅行阶段、简述与紧凑进度已复用底部状态栏；640×360、1280×720 和真全屏实际窗口预检均确认状态栏不与前窗相交，文本最多两行且不拦截鼠标。
- 离站、巡航、接近和完成交接状态会隐藏前窗交互覆盖层，清除遗留焦点与选中态，并拒绝悬停、聚焦和激活；回到空闲状态后前窗视觉与输入恢复。
- 旅行中导航仍可查看，但明确显示航程进行中且不能重复出发；收音机仍可用，ON/OFF 共用一个本地循环播放器，指示灯与波形同步启停，重复切换不叠加播放器或音源。
- Round 1 已通过的三阶段节奏、赤砂星接近、首次不可跳过和 `FLIGHT` 状态保留均保持不变。
- Godot 4.7.1 完整检查通过：27 个单元/场景测试套件、驾驶舱热点烟雾、旅行专项烟雾、Gate A 端到端烟雾、全部脚本解析与空白检查均通过。
- T-023 仅新增为后续通用对话任务，T-022 依赖已同步更新；本轮没有实现二者。

### 人工试玩步骤

1. 启动新游戏，完成大厅教程、接取赤砂星订单并确认飞船配置，进入驾驶舱；建议分别查看 640×360、1280×720 或真全屏中的任一常用尺寸。
2. 激活导航屏并出发，确认旅行阶段、短描述和进度只出现在底栏，不遮住前窗，首次旅行仍没有跳过按钮。
3. 旅行中把鼠标移到前窗并尝试点击，再用键盘切换焦点，确认前窗没有高亮、边框、选中、焦点或观察通知；星空和赤砂星接近动画仍清楚可见。
4. 旅行中再次打开导航屏，确认显示航程进行中且无法二次出发；关闭面板后，分别用鼠标和键盘切换收音机，确认指示灯/波形和低音量循环声音会随 ON/OFF 启停且没有叠音。
5. 完整经历约 12 秒的离站、巡航和接近阶段，确认结束后自动进入当前 `FLIGHT` 占位场景，状态保留正常。

### 人工重点与已知限制

- 重点确认三个阶段是否易辨认、总时长是否过快或拖沓、提示音是否清楚但不刺耳、赤砂星接近是否形成足够的距离感。
- `FLIGHT` 当前仍是既有占位场景；T-030 才会建立飞行实验室，本任务不提前实现横版飞行。
- 阶段提示音与赤砂星均为 M0 灰盒占位；“已看过旅程”已写入运行时故事标记，跨进程存档由后续存档任务接管。
- 本任务沿用 `Docs/05_TECHNICAL_DESIGN.md` 已确定的本地旅行控制器、`GameState` 会话状态和 `SceneRouter` 场景交接边界，没有新增旅行架构规则；通用角色对话的整段跳过规则另由 T-023 与 D-043 记录。
- Round 1 返工将旅行阶段、简述与紧凑进度收进底部状态栏；旅行结束或回到空闲状态后恢复普通热点状态与操作提示。
- 离站、巡航、接近和进入 `FLIGHT` 的交接期间，前窗只显示旅行视觉，不保留鼠标覆盖、键盘焦点、点击区域或选中态。
- 收音机使用单一低音量本地循环音源和输入被动的指示灯/波形占位反馈；关闭即停止，不叠加播放器或音源实例。

### Human Check Round 2 试玩结论（2026-07-18）

**Result:** `Passed`

- 玩家确认旅行状态信息不再遮挡前窗，离站、巡航和接近三个阶段仍清楚可辨，星空与赤砂星接近动画不会被热点高亮干扰。
- 旅行期间前窗不再响应悬停、键盘焦点或点击；导航不会重复启动航程，收音机 `ON/OFF` 已具备足够明确的最低限度视听反馈。
- 约 12 秒总时长可以接受；旅行结束后的 `FLIGHT` 交接以及订单、目的地、货物和飞船配置状态保留正常。
- T-021 正式通过并标记 `Done`；T-021 与 T-005 均已完成，因此 T-023 解锁为 `Ready`。T-022 在 T-023 完成前继续保持 `Blocked`，T-030 状态不变。

### 建议提交

`实现驾驶舱目的地设定与星际旅行流程`

---

## T-022 — 旅行中的老皮对话与可选互动

**State:** `Done`\
**Priority:** P1\
**Goal:** 用少量互动建立宁静、温暖和黑色幽默的旅行气质。

### 范围

- 一段主线必触发对话。
- 两个可选热点对话，例如收音机与货舱。
- 老皮对玩家是否安装激光炮作出不同评论。
- 旅行结束前提供简短控制提示。

### 明确不做

- 不写大量世界观百科。
- 不让对话阻塞无法跳过的长动画。

### 验收标准

- 必触发与可选对话不会相互覆盖。
- 已读状态可记录。
- 文本符合 Narrative Bible 的角色语气。

### 依赖

T-021、T-005、T-023。

### 自动验收（2026-07-18）

- 巡航阶段会自动触发一段老皮主线对话；对话期间航程计时暂停，关闭未完成的主线对话后会安全重新呈现，使用通用“跳过整段”可正常完成并恢复航程。
- 旅行中首次开启收音机或检查货舱会分别触发可选对话；任一对话显示期间，其他热点不会叠加开启。完成后已读行写入 `GameState`，再次操作恢复收音机开关或货舱面板的原有行为。
- 主线对话按 `module_asteroid_laser` 是否安装选择唯一分支；已安装与未安装两种评论均由自动测试覆盖，未采用的分支不会误记为已读。
- 接近阶段状态栏会给出横版飞行的速度与姿态控制提示；本任务没有改变首次旅行不可跳过、前窗被动展示或结束后进入 `FLIGHT` 的既有交接规则。
- Godot 4.7.1 完整检查通过：28 个单元/场景测试套件、驾驶舱旅行、驾驶舱交互、对话 UI、Gate A 等烟雾测试、全脚本解析、项目导入和空白检查均通过。
- 本任务不是预定强制人工验收节点，但老皮语气、对话插入时机和约 12 秒基础航程被对话暂停后的实际节奏无法由自动测试可靠判断，因此暂标记为 `Human Check`。

### Human Check 试玩步骤

1. 从新游戏完成大厅教程、接取赤砂星订单和飞船配置，进入驾驶舱后从导航屏启动首次旅行。
2. 离站阶段首次开启收音机并检查货舱：确认两个可选对话各只出现一次、不会互相覆盖，对话后热点恢复原有功能。
3. 进入巡航后确认老皮主线对话自动出现；关闭一次未完成的对话，确认它会重新呈现，再用“跳过整段”或正常阅读完成，确认航程继续且不会重复触发。
4. 分别以未安装和已安装激光炮的配置进入新一轮运行，确认老皮只说对应的一条装备评论。
5. 接近阶段确认控制提示清楚可读，随后正常进入 `FLIGHT` 占位场景。

人工重点：老皮是否仍显得放松、温暖且带克制的公司黑色幽默；主线对话的自动插入是否突兀；可选对话是否像自然探索而非待清任务；暂停与恢复是否破坏航程节奏；装备评论和进入提示是否清楚且不啰嗦。

### Human Check 试玩结论（2026-07-18）

**Result:** `Passed`

- 玩家确认巡航阶段主线对话正常触发，且不会与收音机、货舱可选对话重叠；对话期间暂停航程、结束后恢复航程的节奏可以接受。
- 使用 `Esc` 中断主线对话后会安全恢复，不会错误跳过；“跳过整段”能够正常结束对话并恢复航程。
- 安装与未安装陨石激光炮时会显示正确的老皮评论分支，已读状态及所选分支记录正常。
- 收音机和货舱对话具有自然探索感；接近阶段的横版飞行控制提示清楚且不过度啰嗦，最终能够正常进入 `FLIGHT` 占位场景。
- 当前对话文本、占位音频和视觉表现足以作为 M0 实现；后续打磨属于非阻塞改进，不影响 T-022 验收。
- T-022 正式通过并标记 `Done`；T-023 已实现且保持 `Done`。阶段 C 的 T-020、T-021、T-022、T-023 均已完成，因此阶段 C 标记 `Done`；T-030 保持 `Ready`，本轮不开始实现。

### 建议提交

`加入赤砂星航程中的老皮互动剧情`

---

## T-023 — 通用对话整段跳过与安全结束

**State:** `Done`\
**Priority:** P0\
**Goal:** 所有角色对话都能明确跳过整段，同时安全完成已读、剧情标记和输入恢复，不误选任何选项。

### 范围

- 为通用 `DialogueUI` 提供明确的“跳过整段”操作，所有使用该 UI 的角色对话自动获得能力。
- 跳过线性内容时，将被跳过内容按已读处理，并且只执行一次序列完成、剧情标记与输入恢复。
- 对话包含选择时，整段跳过最多前进到最近的选择节点并停下，等待玩家明确选择。
- 正常结束、整段跳过、遇到选择停止和重复触发共用可验证的安全收尾路径。

### 明确不做

- 不自动替玩家选择任何 `Choice`。
- 不引入外部对话插件或复杂快进系统。
- 不把 `Escape` 隐式改成整段跳过。
- 不改变驾驶舱星际旅行的跳过规则；首次主线旅行仍不可跳过。

### 验收标准

- 正常播放与整段跳过都会且只会完成一次序列、剧情标记和输入恢复。
- 线性跳过内容被记录为已读。
- 遇到选择时停在最近选择节点，不自动选择，玩家选择后可正常继续与结束。
- 重复请求跳过或结束不会重复发射完成信号、重复写入标记或遗留输入锁。
- 自动测试覆盖正常结束、线性整段跳过、选择停止与重复触发。

### 依赖

T-021、T-005。

### 自动验收（2026-07-18）

- 通用 `DialogueUI` 已提供本地化的“跳过整段”按钮；线性跳过会逐行记录已读并应用剧情效果，序列完成信号只发射一次。
- 遇到带 `Choice` 的行时会停在最近选择节点并立即显示选项，不会自动选择；玩家明确选择后仍可继续跳过并正常结束。
- 重复跳过、结束或取消不会重复完成序列；大厅教程与驾驶舱对话烟雾测试确认输入锁、模态状态和键盘焦点均能恢复。
- Godot 4.7.1 完整检查通过：27 个单元/场景测试套件、对话 UI、大厅教程、驾驶舱与 Gate A 等全部烟雾测试、脚本解析及空白检查均通过。
- 本任务不属于预定人工验收节点，关键状态与 640×360 布局已有自动测试覆盖，因此按约定直接标记 `Done`。

### 建议提交

`实现通用对话整段跳过与安全结束`

---

# D. 飞行实验室与核心手感

## T-030 — Flight Lab、相机与调试入口

**State:** `Done`\
**Priority:** P0\
**Goal:** 在不重跑整段剧情的情况下快速测试飞行手感。

### 范围

- 独立 Flight Lab 场景。
- 飞船占位 Sprite、相机、简单地形与空间背景。
- 开发模式可直接从 Main 进入。
- 调试 HUD 显示速度、垂直速度、俯仰、区域、重力、燃料、Boost、辅助驾驶和碰撞状态。
- 支持一键重置到稳定起点。

### 明确不做

- 不制作赤砂星最终关卡。
- 不做最终 HUD 美术。

### 验收标准

- 从启动到进入 Flight Lab 不超过几次操作。
- 调试 HUD 可开关。
- 重置不会残留旧速度或资源状态。

### 依赖

T-003、T-006。

### 自动验收（2026-07-18）

- 已建立可独立实例化的 Flight Lab：使用 `CharacterBody2D` 飞船占位 Sprite、跟随相机、空间背景、简单地形与碰撞体，并由既有 `FLIGHT` 阶段包装场景承载。
- Debug 构建可通过 `-- --flight-lab` 从 Main 直接进入；调试 HUD 使用本地化文本显示速度、垂直速度、俯仰、区域、重力、燃料、Boost、辅助驾驶和碰撞状态，`F3` 可开关显示。
- `R` 会恢复稳定起点，并清除位置、速度、姿态、燃料、Boost、重力与碰撞状态；自动烟雾测试同时验证相机同步和 640×360 HUD 边界。
- Godot 4.7.1 定向验证通过：29 个单元/场景测试套件、Flight Lab 专项烟雾和中文直达启动均通过；完整 `./scripts/check_project.sh` 通过全部现有路径。
- 本任务只建立后续飞行任务的可重复调试基线，未提前实现运动、重力、伤害或武器；飞行手感按约定集中在 T-036 人工验收，因此本任务标记 `Done`。

### 建议提交

`建立飞行实验室与核心调试 HUD`

---

## T-031 — 油门、刹车、俯仰与显式惯性模型

**State:** `Done`\
**Priority:** P0\
**Goal:** 飞船从低速悬停到高速飞行都具有重量，但仍可被玩家理解和掌控。

### 范围

- `CharacterBody2D` 飞船主体。
- 油门、刹车/反推、抬头、低头。
- 推力沿船头方向施加。
- 显式速度、角速度、阻力和速度限制。
- 无大气环境下保持明显惯性。
- 参数集中到 `FlightTuning` Resource。

### 明确不做

- 不实现真实轨道力学。
- 不直接使用 RigidBody2D 作为核心手感来源。
- 不加入武器、碰撞伤害或天气。

### 验收标准

- 可从缓慢移动加速到高速。
- 松开油门后不会立即停止。
- 刹车有清晰效果但不是瞬间归零。
- 参数变化无需改动运动公式代码。
- 纯数学部分有自动测试。

### 依赖

T-030。

### 自动验收（2026-07-18）

- `FlightLabShip` 继续使用 `CharacterBody2D`，通过 `flight_throttle`、`flight_brake`、`flight_pitch_up` 和 `flight_pitch_down` Input Map action 读取控制，并在 `_physics_process()` 中显式积分线速度、角速度和旋转后调用 `move_and_slide()`。
- 新增独立 `FlightMotionModel` 纯计算层：主推力沿船头方向，轻微空间阻力保留滑行惯性，制动沿当前速度逐步趋零且不会自动倒飞，同时应用可调前向速度、总速度和最大俯仰保护。
- 推力、制动、空间阻力、前向/总速度上限、制动死区、角速度、角加速度、角阻尼与俯仰范围均集中在 `FlightTuning` Resource；替换调参资源即可改变结果，无需修改运动公式。
- Flight Lab 增加世界空间速度参照、推进器亮度反馈和角速度 HUD；`R` 重置同步清除线速度、角速度和全部当前输入状态。
- Godot 4.7.1 自动验证通过：30 个单元/场景测试套件全部通过；纯数学测试覆盖推力方向、长时加速、惯性、制动不越零、速度限制、俯仰响应/阻尼和资源替换；Flight Lab 专项烟雾实际驱动四个 Input action；完整 `./scripts/check_project.sh` 通过。
- 本任务未实现 Boost、重力、大气、伤害、武器或天气；飞行手感继续按约定集中在 T-036 人工验收，因此本任务标记 `Done`。

### 建议提交

`实现可调的横版飞船推力与惯性模型`

---

## T-032 — 大气层重力、阻力与辅助驾驶

**State:** `Done`\
**Priority:** P0\
**Goal:** 松手后飞船会因星球引力下降，下降速度先增加后趋近终端速度，同时玩家可以调节辅助程度。

### 范围

- 通过环境配置逐步引入重力和空气阻力。
- 默认辅助驾驶抵消约 75% 重力。
- 0%–100% 可调；100% 可悬停但产生明确资源成本或限制。
- 水平与垂直阻力可分别调整。
- 下降速度有终端上限，不无限增长。
- 支持不同星球重力配置。

### 明确不做

- 不模拟完整空气动力学、升力面或失速曲线。
- 不为四颗星球提前调最终参数。

### 验收标准

- 在大气层中松手后垂直速度逐渐增加，随后稳定接近终端速度。
- 水平速度因阻力逐渐下降。
- 俯冲明显加速，拉起与刹车可以恢复控制。
- 辅助驾驶 0%、75%、100% 的差异清楚。
- 自动测试覆盖终端速度和辅助重力计算。

### 依赖

T-031。

### 自动验收（2026-07-18）

- 新增可复用的 `FlightEnvironmentProfile` 与纯计算 `FlightEnvironmentModel`：深空和赤砂星大气使用独立 Resource，重力混合、空气密度、水平/垂直阻力、自然终端速度与安全下降上限均可调；赤砂星 `PlanetDefinition` 显式引用并验证自己的环境配置，未提前建立另外三颗星球数据。
- `FlightLabShip` 继续使用 `CharacterBody2D` 和显式速度积分；环境值按配置平滑过渡，分轴线性阻力使用稳定的解析积分，松手下降会先加速再趋近自然终端速度，病态向下速度另受安全上限保护。
- 辅助驾驶沿用设置中的 0%–100% 与默认 75%；Flight Lab 可用 `F4` 切换深空/赤砂星大气、`F5` 比较 0%/75%/100% 预设。100% 在有燃料时抵消重力并按当前灰盒参数消耗燃料，燃料耗尽后回落到免费辅助阈值；最终成本仍留待 T-036 Gate B 调整。
- 调试 HUD 增加空气密度、重力混合、有效重力、自然/安全终端速度、请求/有效辅助与辅助燃料成本；大气密度同时驱动最低限度色调反馈。640×360 中文录帧确认新增信息与 F4/F5 提示均在视口内可读。
- Godot 4.7.1 自动验证通过：31 个单元/场景测试套件全部通过；纯数学测试覆盖平滑环境过渡、0%/75%/100% 有效重力、分轴阻力、自然终端速度、安全上限、不同星球配置替换、俯冲与拉起/刹车恢复，以及 100% 辅助成本和燃料耗尽限制；Flight Lab 专项烟雾与完整 `./scripts/check_project.sh` 均通过。
- 本任务未实现完整空气动力学、另外三颗星球最终参数、Boost、碰撞/失败、武器或进入风格识别；T-032 不属于预定强制人工验收节点，因此按自动验收结果标记 `Done`。

### 建议提交

`实现大气重力阻力与可调辅助驾驶`

---

## T-033 — 飞行资源、碰撞、失败与一键重开

**State:** `Done`\
**Priority:** P0\
**Goal:** 危险可读、失败可快速恢复，并且不会形成主线经济死档。

### 范围

- 船体、护盾、燃料、Boost 能量、货物完整度。
- 小擦碰只产生火花、震动和少量护盾变化。
- 高速碰撞触发失败；M0 主线从最近检查点重试。
- Boost 用于突破重力或风暴，消耗 Boost 能量并影响燃料。
- 一键重开当前飞行段。
- 货物完整度 UI 与公司管理层警告触发接口。

### 明确不做

- 不扣强制维修费。
- 不做永久飞船损毁。
- 不做支线报酬公式。

### 验收标准

- 不同碰撞速度产生至少两档结果。
- 失败到重新获得控制的等待尽量短。
- 重开恢复检查点定义的资源和状态。
- 主线永远不会因资源归零而无法继续。

### 依赖

T-032。

### 自动验收（2026-07-18）

- 新增独立的 `FlightResources`、`FlightCollisionResolver`、`FlightCollisionResult` 与 `FlightCheckpointState`：船体、护盾、燃料、Boost 能量和货物完整度均可快照恢复；碰撞按法线速度分为忽略、擦碰、重撞和致命四档，阈值、伤害、货损、震动及重试延迟集中在 `FlightTuning` 和 M0 调参资源中。
- Boost 通过现有 Input Map 的 `flight_boost` 读取，提供独立尾焰反馈，同时消耗 Boost 能量和额外燃料；限定货物会压低有效 Boost，100% 付费悬停会阻止自然恢复。燃料归零后仍保留可调的应急推进，Boost、护盾或货物归零也不会锁死主线。
- Flight Lab 以真实 `CharacterBody2D` 滑动碰撞验证擦碰、重撞和高速失败：擦碰仅产生火花、相机震动和少量护盾损失；重撞可造成护盾/船体与货损并触发一次性公司警告；致命撞击锁定当前输入后约 0.35 秒自动恢复最近检查点，也可按 `R` 立即恢复位置、运动、环境和全部资源。
- 调试 HUD 增加五项资源、当前消耗、碰撞分档/法线速度与检查点信息；状态提示支持中文换行。640×360 Compatibility 实际录帧确认顶部操作提示、左侧遥测和右下失败提示均在视口内且互不重叠。
- Godot 4.7.1 自动验证通过：33 个单元/场景测试套件全部通过；专项烟雾覆盖 Boost 消耗与恢复、货物策略、真实地形高速碰撞、警告去重、自动快速重试、`R` 一键恢复和中文 HUD 布局；完整 `./scripts/check_project.sh` 与 `git diff --check` 均通过。
- 本任务未实现维修费、永久飞船损毁、支线报酬、激光炮、正式赤砂星关卡或进入风格分类；T-033 不属于预定强制人工验收节点，飞行主观手感与失败节奏仍集中到 T-036 Gate B 验收，因此按自动结果标记 `Done`。

### Gate C 后续调整（2026-07-19）

- T-045 返工确认 M0 赤砂星主线以 `100` 满燃料出发，不加入中途补给；普通
  推进、Boost 额外消耗和无限重力补偿分别为 `0.32 / 1.10 / 2.00` 燃料/秒。
  八个阶段的检查点各自定义安全燃料下限，重试只向上保底、不伪造 HUD 数字；
  燃料归零后继续保留 `0.30` 倍应急普通推力并禁用 Boost。T-033 保持 `Done`。

### 建议提交

`实现飞行资源碰撞反馈与快速重试`

---

## T-034 — 激光炮与可破坏陨石

**State:** `Done`\
**Priority:** P1\
**Goal:** 兑现“武装运输者”的第一步，同时严格保持武器只服务于太空环境通路。

### 范围

- 仅安装激光炮模块时可发射。
- 激光使用连续可见光柱、独立伤害 Tick，并有清晰视觉/音效反馈。
- 陨石分为不可破坏背景、可破坏小型和耐久较高大型障碍。
- 武器碰撞层只作用于明确的环境目标。

### 明确不做

- 不制作敌人、伤害 NPC 或地表战斗。
- 不做弹药库存、武器商店或多武器系统。

### 验收标准

- 未安装模块时输入给出非阻塞反馈。
- 激光能击碎指定陨石并生成简化碎片/粒子。
- 激光不能影响地形、NPC 或剧情交互对象。
- 自动测试覆盖模块能力门控与碰撞层规则。

### 依赖

T-014、T-033。

### 自动验收（2026-07-18）

- 新增独立 `FlightWeaponRules`，从既有 `GameState` 飞船配置和 M0 模块目录解析 `capability_break_asteroids`；默认未安装时，`F`/鼠标左键开火只显示非阻塞中文反馈。Flight Lab 的 `F6` 仅在 Debug 构建临时切换装载状态，不写回正式配置。
- 新增可复用 `FlightLaserWeapon`：射程、伤害、冷却和光束持续时间集中在 `FlightTuning`；发射提供短光束、炮口闪光和运行时合成音效，连续输入在冷却期间不会重复造成伤害。
- 新增可复用 `DestructibleAsteroid` 场景：背景陨石保持纯视觉，小型目标耐久 1、大型障碍耐久 3；命中有闪光与剩余耐久反馈，击碎后隐藏碰撞并发射简化碎片，`R` 或自动检查点重试会恢复目标状态。
- 世界地形使用命名 `World` 层，可破坏陨石使用独立 `DestructibleAsteroid` 层；激光射线掩码只包含后者，飞船仍同时碰撞地形和陨石，因此 NPC、剧情对象与普通地形不会成为武器目标。
- Godot 4.7.1 自动验证通过：34 个单元/场景测试套件覆盖模块能力门控、命名碰撞层、耐久/重置、键鼠输入与场景结构；Flight Lab 专项烟雾实际验证未安装拒绝、冷却、小型一发、大型三发、清空后落空和检查点恢复；完整 `./scripts/check_project.sh` 通过。640×360 中文录帧确认 HUD、光束和击碎反馈可读且不重叠。
- 本任务未实现敌人、NPC/地表伤害、战斗 AI、弹药、武器商店、多武器、正式赤砂星路线或最终视听素材；T-034 不属于预定强制人工验收节点，主观武器节奏与飞行手感继续集中到 T-036，因此按自动验收结果标记 `Done`。

### Gate C 后续调整（2026-07-19）

- T-045 返工以持续 Beam 取代 Gate B 的按冷却连续脉冲表现：键盘与鼠标仍
  共用 `flight_fire` held 状态，按住期间光柱每帧可见，伤害按 `0.10 s` Tick
  独立结算，松开、暂停、失败、重试、卸载或离场会停止光柱与循环音。射线仍
  只命中可破坏陨石层，不增加弹药、过热或其他武器。T-034 保持 `Done`。

### 建议提交

`实现飞船激光炮与可破坏陨石`

---

## T-035 — 俯冲、滑翔与常规进入风格识别

**State:** `Done`\
**Priority:** P1\
**Goal:** 玩家不同的进入策略得到叙事反馈，而不是街机分数。

### 范围

- 记录进入耗时、最大下降速度、最大热/风险值、观景区触发和最后拉起。
- 以可调阈值分类 `DIVE`、`GLIDE`、`BALANCED`。
- 调试 HUD 显示实时候选风格。
- 将最终风格写入订单运行结果供对白使用。

### 明确不做

- 不显示分数、连击或排行榜。
- 不让风格分类决定主线成败。

### 验收标准

- 明显激进与明显缓慢的测试轨迹会得到不同分类。
- 临界值来自配置而非硬编码。
- 分类结果可被对话条件读取。

### 依赖

T-032、T-033。

### 自动验收（2026-07-18）

- 新增轻量 `OrderRunState` 与独立 `FlightStyleTracker`：一次大气进入会记录耗时、最大下降/总速度、归一化风险、去重观景触发、最后拉起和碰撞次数；重试只清空本次进入尝试，最终结果保留给订单结算阶段。
- `DIVE`、`GLIDE`、`BALANCED` 的最小采样时长、速度、风险、耗时、观景数量和拉起阈值全部集中在 `FlightTuning` 与 M0 调参 Resource；明显快速高风险轨迹、长时间低风险观景轨迹和中间轨迹分别获得三种结果，替换阈值无需修改分类公式。
- Flight Lab 在切入赤砂星大气时开始追踪，检查点重试会开始新尝试，切回深空时把最终风格及当前资源写入活动订单运行结果；两个无阻挡灰盒观景门提供真实触发路径，调试 HUD 以本地化文本实时显示候选风格和关键指标。
- 对话白名单新增 `ENTRY_STYLE_EQUALS`，只接受三种稳定风格 ID；运行时可据最终订单结果保留或跳过对应对白，不执行任意表达式，也不影响主线成败。
- Godot 4.7.1 自动验证通过：36 个单元/场景测试套件覆盖三种分类、阈值替换、观景去重、最后拉起、重试、订单结果保留、对白读取与校验；中文 Flight Lab 专项烟雾覆盖大气启停、实时 GLIDE 候选、最终写入、HUD 边界及既有飞行/激光/碰撞回归；完整 `./scripts/check_project.sh` 通过。640×360 Compatibility 实际录帧确认新增遥测行完整可读且不与状态面板重叠。
- 本任务未加入分数、连击、排行榜或成功判定，也未开始正式赤砂星路线；当前阈值与观景门仍是 M0 灰盒默认值，飞行手感和三种风格的主观辨识集中到 T-036 Gate B 人工验收，因此本任务按自动结果标记 `Done`。

### 建议提交

`加入大气进入驾驶风格识别`

---

## T-036 — 飞行手感人工试玩

**State:** `Done`\
**Priority:** P0\
**Goal:** 在制作完整星球关卡前确认基础飞行本身值得继续玩。

### 范围

- 汇总 T-030 至 T-035。
- 提供 0%、75%、100% 辅助驾驶预设。
- 提供低速悬停、高速俯冲、刹车拉起、碰撞和激光测试路线。
- 修复阻塞问题并进入 `Human Check`。

### 验收标准

- 自动检查通过。
- 主 Agent停止扩展并提供试玩说明。
- 用户至少评价：重量感、俯仰、刹车、重力、默认辅助、速度感、相机和失败节奏。
- 试玩结论记录到 Decision Log。

### 依赖

T-030 至 T-035。

### Gate B Round 1 自动预检（2026-07-18）

**Result:** `Human Check`

- Flight Lab 新增五项紧凑试玩路线：辅助预设与低速悬停、高速俯冲、刹车拉起、碰撞与快速重试、激光开路；路线只记录“已体验”，不自动评价手感或判定通过。
- `F5` 继续提供 0%、75%、100% 三种辅助预设；`Tab` 可隐藏/恢复路线卡，`R` 与高速撞击后的自动重试不会清空本次路线进度，重新启动 Flight Lab 才会重置清单。
- 路线要求实际体验一发小陨石与三发大陨石、至少一次非致命碰撞和一次致命失败；现有 0.35 秒自动重试、检查点资源恢复、进入风格追踪与调试遥测均保持不变。
- Godot 4.7.1 完整检查通过：37 个单元/场景测试套件、中文 Flight Lab 专项烟雾、全部既有场景烟雾、脚本解析、项目导入与空白检查均通过。
- 640×360 Compatibility 实际窗口预检确认紧凑路线卡、遥测面板与状态栏不重叠，飞船和前方测试目标仍可见；路线卡也不拦截鼠标输入。
- 本轮没有预先改动推力、重力、阻力、辅助成本、相机或碰撞阈值；这些主观默认值等待玩家 Gate B 反馈。T-040 在 T-036 人工通过前继续保持 `Blocked`。

### Human Check Round 1 试玩反馈（2026-07-19）

**Result:** `Rework required`

- 已接受：飞船整体重量感，以及油门、俯仰、刹车、拉起的基础手感；0%、75%、100% 三档重力补偿差异与默认 75% 参数；碰撞失败和快速重试节奏；当前 M0 灰盒相机跟随。
- 本轮不重新调节基础前进推力、满重力、空气阻力、碰撞阈值、相机参数或现有资源消耗。
- Gate B 返工限定为五类：将默认 HUD 收敛为 Essential Flight HUD 并用 `H` 单独展开 Full Diagnostics；将主要调试键改为 `H/V/G/L`；让激光支持键鼠按住连续脉冲发射；统一三档玩家可见重力补偿名称；加入有限倒车并在倒车输入或负前向速度时禁用 Boost。
- 实现与自动检查期间 T-036 和阶段 D 回退为 `In Progress`；通过后只能重新进入 `Human Check`，T-040 继续保持 `Blocked`。

### Gate B Round 2 自动预检（2026-07-19）

**Result:** `Human Check`

- 默认 HUD 收敛为顶部三组：256×70 的运动/环境组、120×70 的资源/完整度组，以及 232×70（640 宽度的 36.25%）的紧凑 Gate B 卡；中央飞船与首个陨石目标保持可见。Full Diagnostics 默认隐藏，`H` 独立开关；紧凑路线只显示进度、当前名称和一行提示，`Tab` 展开后仍不覆盖初始飞船或首个目标。底部状态只在真实事件后显示一行 2.4 秒临时反馈。
- Input Map 默认键改为 `H/V/G/L/Tab/R`，开火继续由 `F` 与鼠标左键共用 `flight_fire`；精确旧 F3–F6 默认绑定会迁移，玩家可见提示不再出现这些功能键。
- 三档统一通过 `FlightAssistMode` 映射为“关闭重力补偿”“有限重力补偿”“无限重力补偿”；0.0/0.75/1.0、本轮前已有的重力差异、100% 燃料成本和 Boost 恢复门控均未改变。
- 激光在按下时立即发射，按住时复用 `laser_cooldown_seconds = 0.22` 的现有脉冲冷却；松开、重置、失败、暂停、失焦、卸载模块或离开场景会清除 held-fire，不增加弹药、持续光束或重复武器节点。
- 有限倒车参数集中为进入阈值 6 px/s、推力倍率 0.40、最大正向速度比例 0.30（当前 132 px/s）和制动力倍率 1.00；`S` 先制动再反推，倒车输入或负前向速度会在资源扣除前禁用 Boost。俯仰方向、角度/角速度上限和相机逻辑未分叉。
- Godot 4.7.1 完整检查通过：headless import、全部脚本解析、38 个单元/场景测试套件、Flight Lab/Gate B 专项及全部既有烟雾、工作树/索引空白检查均通过。640×360 中文真实窗口预检确认 H/Tab/V/G/L/R/F 和三种 HUD 状态可辨；桌面控制接口无法保持单键按下，因此长按连发、连续 S、倒车俯仰与 Boost 资源门控以逐物理帧专项烟雾结果为自动证据，仍留给玩家 Round 2 真实手感确认。
- 本轮没有改动已接受的基础前进推力、满重力、空气阻力、碰撞阈值、相机参数或既有资源消耗。T-036 和阶段 D 重新进入 `Human Check`；T-040 继续保持 `Blocked`。

### Human Check Round 2 试玩步骤

1. 使用 `--language zh_CN -- --flight-lab` 中文直达 Flight Lab；预计 8–12 分钟。先确认默认只显示顶部 Essential HUD、紧凑 Gate B 卡和极简快捷键提示，中央飞行区没有常驻大面板。
2. 按 `H` 展开 Full Diagnostics，再按 `H` 关闭；确认关闭后不留空背景或鼠标拦截区域。按 `Tab` 展开/折叠完整五项路线，确认两种状态都不遮挡飞船和首个陨石目标。
3. 按 `V` 切入赤砂星大气；按 `G` 依次确认“无限重力补偿 → 关闭重力补偿 → 有限重力补偿”，在无限档按 `R` 从静止起点观察悬停、燃料消耗与 Boost 停止恢复。确认三档实际差异没有回归。
4. 按 `L` 临时装载激光。分别短按 `F`、短点鼠标左键，确认每次只发射一发；随后分别按住其中一种输入，确认按约 0.22 秒间隔持续发射，松开立即停止。击碎小型和大型陨石后确认普通地形不受影响。
5. 按 `R` 重置后向前加速，再持续按 `S`：确认先平滑制动到零，之后才进入明显慢于正向飞行的有限倒车；Essential HUD 应显示负号和“倒车”。
6. 倒车输入期间以及已经处于负前向速度时尝试 `Shift`，确认 Boost 不生效、燃料和 Boost 能量不因该输入下降，并且每次按住只出现一次“倒车时无法使用 Boost”提示。
7. 保持倒车时使用 `↑/↓`，确认俯仰方向与正向飞行一致、角度范围不变、飞船不自动掉头且相机不翻到船后。随后按 `W`，确认先消除负速度再恢复向前。
8. 按 `R` 确认负速度、倒车反馈和 held-fire 均已清除；再按路线卡依次快速体验高速俯冲、刹车拉起、擦碰/致命撞击与快速重试，最终完成五项清单。

### 人工重点与已知限制

- Round 2 只需重点评价：Essential HUD 是否清楚且不遮挡；H/Tab 分层是否可靠；H/V/G/L 是否易记；长按激光节奏与停止时机；刹车到有限倒车的过渡、倒车上限、Boost 门控、俯仰和相机是否自然。
- Round 1 已接受的重量感、油门、基础俯仰、刹车/拉起、三档实际差异、默认 75%、碰撞重试与灰盒相机原则上不重新评分，只需报告是否出现回归。
- 当前相机仍是 Flight Lab 的简单双轴居中跟随，测试路线和视觉均为 M0 灰盒；本次需要判断它们是否足以进入 T-040，而不是评价最终关卡美术。
- 路线完成标记只证明对应动作被尝试，不代表动作正确或参数合适；最终通过以下方玩家结论为准。
- T-036 是强制人工验收节点；本次已经收到明确通过结论，因此只关闭本任务并解锁 T-040，不在同一轮开始后续实现。

### Human Check Round 2 试玩结论（2026-07-19）

**Result:** `Passed`

- 玩家确认默认 Essential HUD 已明显收敛且飞行区域可读，Full Diagnostics 可正常显示和隐藏，`H/V/G/L/Tab/R` 新键位清楚可用。
- 激光短按单发、长按连续发射且松开停止的行为通过；“关闭重力补偿 / 有限重力补偿 / 无限重力补偿”三档名称清楚，既有重力补偿效果与资源代价没有异常变化。
- `S` 可平滑地从前进制动到零并进入有限倒车；倒车适合短距离修正，不取代正常前进。倒车时 Boost 正确禁用且不消耗资源，俯仰方向和角度范围正常。
- 当前重量感、推力、刹车、拉起、重力、相机、碰撞和失败重试节奏获准作为 M0 飞行基线继续使用。
- T-030 至 T-036 与阶段 D 均为 `Done`；T-021 也已为 `Done`，因此 T-040 和阶段 E 解锁为 `Ready`。T-041 至 T-045 继续保持 `Blocked`，本轮不开始 T-040。

### 建议提交

`完成首轮横版飞行手感试玩版本`

---

# E. 赤砂星完整飞行路线

## T-040 — 赤砂星关卡灰盒与阶段编排

**State:** `Done`\
**Priority:** P0\
**Goal:** 把“飞入星系到降落地表”的仪式感做成一条可连续操控的电影化路线。

### 范围

关卡按压缩尺度连续表现：

1. 星系外缘与远处星球。
2. 陨石带。
3. 近轨道接近。
4. 大气边缘。
5. 雷暴中层。
6. 低层云与沙漠远景。
7. 峡谷/设施低飞。
8. 降落进场。

使用分段环境配置、检查点和视差背景实现，不模拟真实天文距离。

### 明确不做

- 不制作无缝开放星球。
- 不提前追求最终美术。
- 不允许长期向左反向飞回关卡开头。

### 验收标准

- 全程可操控，阶段切换不直接瞬移飞船状态。
- 星球视觉尺度逐步增加。
- 玩家可刹车、悬停、短距离后退，但路线总体向右推进。
- 主线关卡自身目标约 1–3 分钟：快速 60–100 秒、常规 90–150 秒、观景
  120–180 秒；主动悬停或倒车可超过该范围。

### 依赖

T-036、T-021。

### 自动验收（2026-07-19）

- 标准 `FLIGHT` 流程已接入赤砂星八阶段连续灰盒；独立 Flight Lab 仍可通过调试入口启动，没有把关卡编排混入手感实验场。
- 路线由可调数据定义 8 个连续区段，总长 `150000 px`、目标时长 `480 s`；环境参数在区段边界平滑切换，检查点按阶段推进，切换时不重写飞船位置、速度或旋转。
- 飞船可在当前阶段附近短距离后退，但不能长期逆行返回关卡开头；路线终点保留有限缓冲区，整体仍向右推进。
- 灰盒已提供连续地形、三层视差、逐步放大的赤砂星、阶段地标、双语阶段说明和 Essential HUD；陨石伤害、雷暴压力、峡谷雷达、正式着陆判定及最终视听继续留给 T-041 至 T-044。
- Godot `4.7.1-stable` 完整检查通过：40 个自动测试套件全部通过，赤砂星路线专项烟雾验证了八阶段顺序、平滑环境交接、星球尺度、检查点、有限倒车、HUD 与目标时长；640×360 中文录帧确认初始灰盒信息可读。
- T-040 不是预定强制人工验收节点，当前关键风险可由自动验收与必要烟雾测试判断，因此标记 `Done`。T-040 与 T-034 均已完成，T-041 解锁为 `Ready`；T-042 至 T-045 继续保持 `Blocked`。

### Gate C 后续调整（2026-07-19）

- 初版 `150000 px / 480 s` 灰盒基线已由 T-045 试玩判定过长。八阶段保持原
  顺序，但压缩为 `38000 px`：`0–4000 / 4000–9000 / 9000–13500 /
  13500–18000 / 18000–23000 / 23000–27500 / 27500–33000 /
  33000–38000`。名义快速/常规/观景时长集中配置为 `82 / 120 / 165 s`，
  不提高 T-036 已验收的普通推力。T-040 保持 `Done`。

### 建议提交

`搭建赤砂星从星系接近到降落的关卡灰盒`

---

## T-041 — 陨石、雷暴与环境分区

**State:** `Done`\
**Priority:** P0\
**Goal:** 让空间、上层大气和中层大气具有不同操作压力与感官特征。

### 范围

- 陨石区固定编排，支持绕行或激光开路。
- 雷暴区加入固定闪电、风力扰动和可读预警。
- 分区切换调整重力、阻力、音频和屏幕反馈。
- 辅助选项能降低雷暴控制压力或启用慢动作窗口。

### 明确不做

- 不做完全随机天气。
- 不做敌对单位。

### 验收标准

- 玩家能从视觉和操控上辨认所处环境层。
- 危险在命中前有足够预警。
- 慢动作辅助不改变剧情结果，只改善反应窗口。

### 依赖

T-040、T-034。

### 自动验收（2026-07-19）

- 陨石带已加入 8 个固定、可复现的可破坏障碍，包含小型单发目标和大型多发目标；玩家可选择上下绕行或用已安装激光开路，检查点重试会恢复障碍状态。
- 雷暴层已加入 4 个固定雷击区，每次均先显示目标框、倒计时与警告音，再按固定范围结算命中；环境伤害沿用护盾优先于船体的规则，并保留快速重试。
- 雷暴风力由纯数学模型按时间确定性生成，辅助驾驶的 `0% / 75% / 100%` 强度依次降低控制压力；慢动作辅助只延长真实反应时间，不改变命中范围、伤害、路线阶段、检查点或剧情状态，并会在命中、重开、离开雷暴和退出场景时恢复原时间倍率。
- 八个路线阶段继续沿用各自的重力与阻力配置，并新增局部环境音签名、屏幕色调、雷暴风带、雷击闪光与双语状态反馈；没有加入随机天气、敌对单位、峡谷雷达、着陆判定或 T-044 的最终视听资源。
- Godot `4.7.1-stable` 完整检查通过：41 个自动测试套件全部通过；赤砂星路线烟雾与危险层专项烟雾验证了固定陨石、激光开路、检查点恢复、确定性风力、辅助减压、可见雷击预警、慢动作边界和环境反馈。640×360 中文录帧确认目标框、倒计时、命中闪光与 HUD 可读且不拦截鼠标。
- T-041 不是预定强制人工验收节点，当前关键风险已由自动验收、专项烟雾测试与渲染帧检查覆盖，因此标记 `Done`。T-042 解锁为 `Ready`；T-043 至 T-045 继续保持 `Blocked`，完整路线节奏与手感集中在 T-045 人工试玩。

### Gate C 后续调整（2026-07-19）

- 固定路线触发仍保持可重复，但单次雷击改为 `TRACKING → LOCKED → STRIKE →
  COOLDOWN`：预警先追踪并限制在相机安全区，随后固定目标给出最后躲避窗口，
  只在短暂落雷阶段启用命中。重试会清空未完成状态；不加入随机天气或敌人。
  T-041 保持 `Done`。

### 建议提交

`实现赤砂星陨石带与大气雷暴分区`

---

## T-042 — 峡谷设施、路线提示与雷达灰盒

**State:** `Done`\
**Priority:** P0\
**Goal:** 用当地公司殖民设施建立可读的飞行管制区，不用抽象得分门强迫玩家。

### 范围

- 手工灰盒峡谷、拱洞和工业设施障碍。
- 固定雷达扫描扇区；低于最低安全高度会提高锁定风险。
- 高于最低安全高度可安全通过；M0 不把贴地、洞穴或地形遮挡作为断锁规则。
- 被扫描后触发公司/老皮警告和更紧张的环境反馈，但不进入战斗。
- 路线提示和高对比度地形辅助生效。

### 明确不做

- 不做导弹、防空炮战斗或潜行 AI。
- 不做贴地连击分数。

### 验收标准

- 高位路线能完全避免惩罚，低飞会得到明确爬升警告。
- 玩家能理解锁定与惩罚来自“高度过低”。
- 无辅助时可读；开启辅助后地形边缘和建议路线更明显。

### 依赖

T-041。

### 历史自动验收（2026-07-19，雷达规则已由 T-045 Round 3 替换）

- 地表段已加入 6 组手工固定的峡谷、岩檐、管线和设施门障碍；高空直线路径保持开放，低空青色引导线则穿过需要减速修正的遮挡区，形成风险更高的快速路线与更安全的低飞路线倾向。
- 3 个固定雷达扇区按高度显式积分锁定风险，3 个与地形对应的遮挡区会立即切断锁定并使残余风险衰减；锁定只触发 HUD、公司/老皮提示和屏幕压力反馈，不造成伤害，也不改变路线、订单、检查点或剧情结果。
- 既有路线提示开关控制安全路线和遮挡轮廓，高对比度地形开关强化全部碰撞边缘；中文与英文状态文本均通过本地化 Key 提供，HUD 保持在 640×360 安全区域内且不拦截输入。
- Godot `4.7.1-stable` 完整检查通过：42 个自动测试套件全部通过；赤砂星路线、危险层和低飞专项烟雾测试验证了固定障碍、风险积分、地形断锁、检查点清理、辅助可视化和无战斗后果。640×360 中文录帧确认默认引导、锁定压力与断锁说明可读且不遮挡飞船。
- 未加入导弹、防空炮、敌人、潜行 AI、贴地评分、正式着陆或 T-044 的最终美术、音频和粒子。T-042 不是预定强制人工验收节点，因此按自动验收结果标记 `Done`；T-043 解锁为 `Ready`，T-044、T-045 继续保持 `Blocked`，完整路线节奏与雷达压力集中在 T-045 人工试玩。

### Gate C Round 3 规则替换（2026-07-22）

- 上述 2026-07-19 自动验收记录保留为历史，不再是当前玩法规范。T-045 Round 3
  已移除地形遮挡断锁和“安全低飞”，将雷达改为 `高度 ≥ 300 m` 安全、低飞
  警告并产生清楚脉冲后果；T-042 继续保持 `Done`，不重新打开。

### 建议提交

`实现赤砂星峡谷低飞与雷达扫描压力`

---

## T-043 — 着陆交付与检查点

**State:** `Done`\
**Priority:** P0\
**Goal:** 将整段飞行以明确、宽容但仍需要减速的降落动作收束。

### 范围

- 大型可读着陆区域。
- 进入区域后根据速度、姿态和接触强度判断平稳、勉强或失败。
- 平稳程度影响少量对白和货物完整度，不阻塞主线。
- 设置阶段检查点与一键重开。
- 落地成功切换到 ARRIVAL。

### 明确不做

- 不把精准对接做成核心硬核技能。
- 不做复杂起落架物理。
- M0 不实现空投正式任务。

### 验收标准

- 普通玩家可在几次尝试内落地。
- 高速正面撞击失败；小幅擦碰不会立刻失败。
- 重试回到最近合理阶段，而非重新走大厅和驾驶舱。

### 依赖

T-042、T-033。

### 自动验收（2026-07-19）

- 最终阶段已加入宽 `2400 px` 的居民维修场着陆台、青色表面/信标、路线提示与双语着陆 HUD；成功不再由飞过路线末端触发，玩家必须在着陆区完成真实接地。
- 着陆以独立纯逻辑按水平速度、下降速度、接触强度和俯仰判断平稳、勉强或失败，阈值、勉强着陆 `6` 点货损和 ARRIVAL 交接延时均集中在 `FlightTuning`，没有加入复杂起落架或刚体对接。
- 高速或过陡接地会进入既有快速失败流程，可按 `R` 立即重试或自动返回最终阶段的固定安全进场检查点；检查点保留进入时的环境、辅助驾驶和资源状态，不需要重走大厅、驾驶舱或整条航线。
- 平稳/勉强结果会写入当前 `OrderRunState`，勉强着陆只降低少量货物完整度并给出老皮反馈，不阻塞主线；成功后飞船停止并由标准 Scene Router 从 `FLIGHT` 切换到 `ARRIVAL`，抵达占位场景能读取该结果。
- Godot `4.7.1-stable` 完整检查通过：43 个自动测试套件全部通过；路线、危险、低飞及新增着陆专项烟雾验证了接地才完成、失败重试、货损持久化和 ARRIVAL 交接。640×360 中文录帧确认大型着陆区、两行提示与抵达结果可读。
- T-043 不是预定强制人工验收节点，当前关键风险已由纯逻辑测试、真实场景闭环烟雾与渲染帧检查覆盖，因此标记 `Done`。T-044 解锁为 `Ready`；T-045 继续保持 `Blocked`，完整路线的节奏、环境辨识与着陆体验集中在 T-045 人工试玩。

### 建议提交

`实现赤砂星宽容着陆与阶段检查点`

---

## T-044 — 赤砂星氛围、美术占位、音频与粒子首轮

**State:** `Done`\
**Priority:** P1\
**Goal:** 即使仍是占位素材，星球进入也具有可辨认的复古未来主义氛围。

### 范围

- 多层视差：星空、星球、云层、沙漠、峡谷、设施。
- 推进、Boost、擦碰、雷暴、入大气和着陆粒子。
- 引擎循环、Boost、碰撞、警告、雷暴、激光和环境基础音效。
- 氛围电子音乐，进入危险阶段动态增强。
- 颜色和轮廓遵守 `Docs/10_ART_DIRECTION.md`。

### 明确不做

- 不要求最终像素美术。
- 不引入许可证不明素材。
- 不用重度后处理破坏像素清晰度。

### 验收标准

- 关闭调试 HUD 后仍能感知速度、危险和环境变化。
- 所有外部素材记录到 `ATTRIBUTION.md`。
- 音量不压过关键对白和警告。

### 依赖

T-041、T-042、T-043。

### 自动验收（2026-07-19）

- 赤砂星路线现由星空、尘带、云层、远景沙漠/峡谷、近景设施五层
  `Parallax2D`，配合独立星球层、碰撞地形与阶段轮廓构成清晰纵深；颜色、
  暖琥珀/青色设备光和紫色雷暴均遵守 `Docs/10_ART_DIRECTION.md`。
- 飞船新增推进与 Boost 尾迹，局部反馈组件按速度、入大气、雷暴和着陆阶段
  切换稀疏粒子；既有擦碰火花、雷击闪光、激光与陨石碎片继续复用，没有加入
  模糊、Bloom 或重度后处理。
- 引擎循环、Boost、碰撞、警告、雷暴、激光、环境循环和四拍氛围电子层均由
  本地 GDScript 运行时合成；音乐随阶段和雷达压力在 `-30 dB` 至 `-17 dB`
  平滑增强，并为雷击预警与关键提示保留至少 `6 dB` 余量。未引入第三方依赖
  或外部素材，两个粒子 SVG 为本仓库新建的纯几何占位，不需要新增署名。
- 新增赤砂星氛围专项烟雾，验证五层视差、推进/阶段粒子、合成音频、危险音乐
  和警告余量；既有路线、危险、峡谷低飞、着陆与 ARRIVAL 烟雾全部回归通过，
  飞行和着陆参数没有变化。
- 640×360 中文无 HUD 风暴与着陆录帧通过视觉判定 `91/100`：速度、危险、
  赤砂环境、设施和着陆区可直接辨认，像素轮廓保持清晰。参考图与录帧仅作为
  `.omx/` 本地 QA 证据，不进入提交。
- Godot `4.7.1-stable` 完整检查通过：43 个自动测试套件、全部专项烟雾、脚本
  解析、项目导入和 whitespace 检查均通过。T-044 不是预定强制人工验收节点，
  当前主观风险集中到 T-045 完整路线试玩，因此标记 `Done` 并将 T-045 解锁为
  `Ready`；阶段 E 保持未完成。

### Gate C 后续调整（2026-07-19）

- 完整星球圆盘现在只存在于阶段 1–3；阶段 4 用 `0.55 s` 平滑离场，阶段
  5–8 仅保留天空、云层、地平线、地形和设施视差。普通与 Boost 尾迹参数
  直接配置在船体子级 `CPUParticles2D`，使用局部坐标、后向发射和零重力；
  Boost 另有更长更亮尾迹、速度线、音频和轻度镜头拉远。T-044 保持 `Done`。

### 建议提交

`完成赤砂星首轮视差音效与飞行反馈`

---

## T-045 — 赤砂星完整路线人工试玩

**State:** `Done`\
**Priority:** P0\
**Goal:** 在连接落地剧情前验证整段星球进入是否真正具有仪式感和节奏。

### 范围

- 从驾驶舱旅行结束开始，连续玩到着陆。
- 修复阻塞问题和明显节奏问题。
- 提供俯冲与缓降两种建议测试路径。
- 进入 `Human Check`。

### 验收标准

- 自动检查通过。
- 用户能明确区分空间、接近、大气层、雷暴、地表和着陆阶段。
- 用户试玩后确认或调整关卡总长度、默认重力、雷达压力和视角。
- 结论记录到 Decision Log。

### 依赖

T-040 至 T-044。

### Gate C 自动预检（2026-07-19）

- 驾驶舱旅行结束到赤砂星 `FLIGHT`、八阶段连续路线、平稳着陆和
  `ARRIVAL` 结果保留已由新增集成烟雾覆盖；既有路线、危险、低飞、着陆和
  氛围专项烟雾均回归通过。
- 正式路线补齐 2 个不显示、不阻挡飞船的观景事件，使浅角度缓降能够按既有
  阈值记录为 `GLIDE`；快速下沉且不经过观景区域的路线仍记录为 `DIVE`。
  擦碰也会写入既有进入风格统计。未修改 T-036 已验收的飞行参数，也未调整
  关卡长度、默认重力补偿、雷达压力或相机。
- Godot `4.7.1-stable` 项目导入、脚本解析、43 个既有自动测试套件和新增
  Gate C 烟雾均通过；640×360 中文无 HUD 风暴与着陆录帧保持可辨认。
- 自动预检只能证明完整链路和两种进入风格可达。路线节奏、阶段辨识、默认
  重力补偿、雷达压力和相机仍需玩家试玩，因此 T-045 与阶段 E 进入
  `Human Check`；T-050 保持 `Blocked`，本轮不开始落地剧情。

### Gate C Round 1 试玩反馈（2026-07-19）

**Result:** `Rework required`

- 约 8 分钟的基线过长；保留八阶段、危险和检查点，但通过压缩路线距离将
  快速、均衡、观景三种路线分别收敛到约 `60–100 / 90–150 / 120–180` 秒，
  不以全局倍增飞船速度或强迫竞速替代关卡压缩。
- 当前燃料会在路线中途耗尽；不加入中途补给，改为满油出发、检查点恢复
  预设安全燃料，并确保零燃料时仍保留应急基础推力但不能 Boost。
- 赤砂星接近感不足；统一由路线视觉控制器管理小行星盘、明显放大和下移离场，
  到大气层后隐藏完整星球盘，并保证重开和检查点恢复一致。
- 飞船尾焰、长按激光、雷击预警和 Boost 反馈需要重做：尾焰必须保持本地后向、
  激光改为持续光束与独立伤害 Tick、雷击采用追踪—锁定—落雷—冷却阶段、Boost
  需要明确速度差及视听反馈，同时保留倒车禁用与既有资源约束。
- 新增可复用飞行控制帮助面板：首次进入自动打开并冻结飞行，`C` 可再次查看，
  显示本路线实际操作与激光安装状态；持久 HUD 只保留简短帮助和诊断提示。
- 返工完成并通过自动检查后，T-045 与阶段 E 只能回到 `Human Check`；T-040 至
  T-044 保持 `Done`，T-050 保持 `Blocked`，本轮不开始任何后续任务。

### Gate C Round 2 自动预检（2026-07-19）

**Result:** `Human Check`

- 八阶段顺序保持不变，路线从初版 `150000 px / 480 s` 压缩为 `38000 px`；
  分段距离为 `4000 / 5000 / 4500 / 4500 / 5000 / 4500 / 5500 / 5000 px`，
  名义快速/常规/观景时长为 `82 / 120 / 165 s`。基于真实 `FlightLabShip`
  积分的三条脚本路线实测为：快速 `80.5 s`、常规 `98.4 s`、观景
  `121.3 s`，没有提高 T-036 已通过的 `240 px/s²` 普通推力。
- 三条测试均以 `100` 燃料开始：约 45% 时间 Boost 的快速路线结束 `53.0`，
  约 25% 时间 Boost 的常规路线结束 `72.2`，无 Boost 的观景路线结束
  `88.8`。普通/Boost 额外燃料消耗为 `0.32 / 1.10` 每秒，无限重力补偿为
  `2.00` 每秒；检查点燃料保底、零燃料应急推力和 Boost 禁用均通过烟雾。
- 星球圆盘从阶段 1 的 `0.36` 倍、屏幕坐标约 `(580, 102)`，经阶段 2
  `0.48→0.72`，到阶段 3 `0.72→1.28` 并下移至约 `(455, 160)`；阶段 4
  向 `(370, 390)` 离场并在 `0.55 s` 内淡出，阶段 5–8 强制隐藏。重开与
  检查点恢复复用同一进度控制器。640×360 中文无 HUD 阶段 1/3/5/8 可见
  录帧确认尺度对比、圆盘隐藏和地表参照成立。
- 下坠尾迹根因是 `CPUParticles2D` 运行时仍使用自身默认的世界向下重力与向前
  发射，而场景只给它挂了不会覆盖这些字段的粒子材质。现在普通/Boost 尾迹
  直接使用船体局部坐标、后向方向、零重力与 `0.26 / 0.34 s` 紧凑寿命；
  Boost 数量和初速度更高，重试会清空残留。
- 激光改为射程 `560 px`、宽 `3 px` 的持续 Beam，视觉每帧更新，伤害每
  `0.10 s` 结算 `1` 点，短按至少可见 `0.08 s`、释放淡出 `0.04 s`；命中
  陨石会截短光柱，未安装、松开、暂停、失败、重试和离场均正确停止 Beam 与
  循环音。雷暴使用 `0.80 s TRACKING + 0.55 s LOCKED + 0.18 s STRIKE +
  0.45 s COOLDOWN`，高/低位安全区、锁定后固定和只在落雷时命中均有专项覆盖。
- Boost 使用 `2.20×` 推力、`1.35×` 最大速度和 `0.12 s` Ramp；同起点一秒
  普通/Boost 前向速度增量实测 `234.6 / 435.0 px/s`。尾迹、速度线、发动机
  音高/音量、HUD、`18 px` 前视和 `0.96` 轻度拉远共同提供反馈，倒车与零
  燃料门控保持不变。
- 首次路线会显示并暂停本地化飞行帮助；真实窗口已确认 `E` 关闭、`C` 复开、
  `L` 更新激光“已安装/未安装”，以及 `H`/`Tab` HUD 分层。自动烟雾另覆盖
  计时/危险冻结、关闭恢复、核心/测试按键、重试不重复强制弹出和无鼠标拦截。
- Godot `4.7.1-stable` 可见录帧、headless import、全部脚本解析、43 个测试
  套件、Flight Lab、路线、危险、低飞、着陆、氛围、驾驶舱至着陆 Gate C 和
  新增 Round 2 专项烟雾、`git diff --check` 均通过。T-045 与阶段 E 返回
  `Human Check`；T-040 至 T-044 保持 `Done`，T-050 保持 `Blocked`。
- 自动结果不能代替对 1–3 分钟节奏、星球过渡自然度、尾迹/Beam/雷暴/Boost
  主观清晰度、燃料余量、相机和着陆可读性的玩家判断，因此等待 Round 2 试玩，
  不开始 T-050。

### Gate C Round 2 试玩反馈（2026-07-20）

**Result:** `Rework required`

- 近轨道阶段已经建立星球放大感，但进入大气边缘时完整圆盘直接消失，缺少
  飞船来到星球上方、掠过曲率并进入稀薄大气的连续空间关系；阶段 3→4 必须
  由统一背景控制器完成 0.5–1.5 秒连续过渡，并在阶段 5 后再完全退出圆盘。
- 峡谷低飞的雷达锁定目前只有提示与屏幕压力，没有明确玩法后果；需要加入
  可见、可理解且可重复验证的轻量惩罚，同时让安全低飞确实避免该惩罚。
  低飞结束后还需留出脱离雷达、抬升、减速和重新对准着陆台的准备缓冲区。
- 赤砂星 Essential HUD 的“前向 XX”语义不明，且路线中的 `H` 只展开两行
  小面板，未提供 Gate B 已通过的完整诊断层。本轮默认 HUD 必须明确显示带
  单位的距着陆点、速度和高度，同时保留按 `H` 可查看的独立 Full Diagnostics。
- 阶段 1–4 当前生成了类似地面的平直碰撞边界，导致大气边缘之前俯冲也会
  撞地。星系外缘、陨石带、近轨道和大气边缘必须完全禁用地表碰撞；真正地形
  只在更后面的低空/地表阶段逐步启用，并与背景、检查点和着陆阶段一致。
- 返工期间 T-045 与阶段 E 回退为 `In Progress`；上一轮已成立的 1–3 分钟
  时长方向、燃料预算、持续 Beam、雷暴追踪、Boost 反馈、控制帮助和完整流程
  原则上保留。T-040 至 T-044 保持 `Done`，T-050 保持 `Blocked`。

### Gate C Round 3 自动预检（2026-07-20）

**Result:** `Human Check`

- 阶段 3→4 现在由同一背景控制器在 `0.90 s` 内连续放大、下移和淡出完整
  星球圆盘，同时交给曲面地平线、大气色带和高层云；阶段 5 后完整圆盘保持
  隐藏。阶段 1、3、4 过渡中点/终点、5、7、8 的 640×360 可见录帧与
  检查点恢复烟雾确认空间关系连续。
- 星系外缘、陨石带、近轨道、大气边缘和雷暴层不再创建地表视觉或碰撞；
  低层云、地表航线和着陆阶段才同步启用真实地形。专项测试确认早期俯冲不会
  撞到隐形地面，后段可见地形与碰撞阶段一致。
- 雷达高位锁定在每个检查点至多造成一次 `12` 点护盾/船体与 `2` 点货物损伤，
  并提供本地化状态提示和轻微镜头反馈；沿低位安全路线不会受到该后果。
  雷达区在路线距离 `32650` 结束，着陆台碰撞约从 `35600` 开始，留出约
  `2950 px` 的脱离雷达、抬升、减速和对准缓冲区。
- Essential HUD 默认明确显示“距着陆点 / 速度 / 高度”及单位，并按当前风险
  显示重力补偿、雷达安全高度或着陆准备提示；`H` 独立切换 Full Diagnostics，
  再按一次关闭。中文 640×360 真实窗口确认完整诊断信息可见且不遮挡路线状态。
- T-036 已通过的飞行基线、持续 Beam、追踪雷暴、Boost 反馈、帮助面板、燃料
  预算和 1–3 分钟时长均保持：真实积分仍为快速 `80.5 s / 53.0` 燃料、常规
  `98.4 s / 72.2`、观景 `121.3 s / 88.8`；普通/Boost 一秒速度增量仍为
  `234.6 / 435.0 px/s`。
- Godot `4.7.1-stable` 项目导入、全脚本解析、43 个测试套件、所有赤砂星专项
  与完整交接烟雾、可见录帧、真实窗口 HUD 开关和视觉判定均通过；视觉判定
  得分 `93/100`。自动结果不能代替玩家对动态过渡、雷达压力、准备缓冲和整体
  节奏的判断，因此 T-045 与阶段 E 进入 `Human Check`；T-040 至 T-044 保持
  `Done`，T-050 保持 `Blocked`，本轮不开始任何后续任务。

### Gate C Human Check Round 3 试玩反馈（2026-07-22）

**Result:** `Rework required`

- 阶段 3→4 仍在边界处跳变：星球圆盘必须继承上一帧的变换并沿统一路线进度
  持续放大、下移，几何连续地转化为曲面地平线；不得用快速淡出掩盖重置。
- 原阶段 6、7 的低层沙漠与设施内容合并为阶段 6“低空管制雷达区”；规则反转
  为低飞受罚、高飞安全。阶段 7 改为雷达关闭后的高位着陆准备走廊，阶段 8
  才执行从高处下降的最终进场。
- 阶段 8 开始时的实际着陆距离约 1 km，不足以观察、减速、对准和下降；目标
  改为距平台接触区至少 2.5 km，优先 3–4 km，且不得通过加长平台本体满足。
- HUD 高度改为阶段感知语义：阶段 1–3 显示 `> 1 km`，大气进入阶段平滑转为
  数值高度，阶段 6–8 使用与低空雷达共享的当地地表 AGL；无有效地表参考时
  回退到 `> 1 km`，不得显示 0。
- 已接受的 1–3 分钟路线时长、燃料预算、持续 Beam、Boost、追踪雷暴、飞行
  帮助、两层 HUD、阶段 1–5 无真实地表碰撞和 T-036 飞行基线继续保留。
  T-040 至 T-044 保持 `Done`，T-050 保持 `Blocked`；本轮只返工上述四项。

### Gate C Round 4 自动预检（2026-07-22）

**Result:** `Human Check`

- 轨道—大气过渡由 `RedSandOrbitTransitionModel` 在路线 `12800–15000 m` 的
  `2200 m` 窗口统一驱动；以常规巡航 `316.67 m/s` 计算名义 `6.95 s`。阶段
  3/4 边界 `13500 m` 的位置约 `(437.57, 265.82)`、尺度 `2.295×`、Alpha
  `1.0`，边界前后 `0.001 m` 样本的位置/尺度/辉光差均低于单帧阈值。圆盘从
  `(465.11, 154.40) / 1.193×` 持续移动到 `(350, 620) / 5.8×`，在可见弧面
  只剩约 `32 px` 前保持不透明，随后才在自然移出屏幕时关闭；地平线、辉光和
  高层云共用同一进度。检查点和整段重开可精确恢复对应状态。
- 新阶段 6 为 `23000–30500 m`、长 `7500 m` 的低层沙漠设施与低空管制区。
  最低安全高度 `300 m`，状态流为 `CLEAR → LOW_ALTITUDE_WARNING (1.0 s) →
  LOCKED (0.45 s) → PULSE (0.12 s) → COOLDOWN (1.8 s)`；脉冲扣除 `12`
  点护盾/船体与 `2` 点货物，并有 HUD 数值、扫描/静电、音效和系统提示。高飞
  全程安全，地形遮挡不再断锁，阶段结束会彻底清理雷达。
- 阶段 7 为 `30500–33000 m`、长 `2500 m` 的无雷达着陆准备走廊；阶段 8 为
  `33000–38000 m` 的最终进场。着陆中心仍在 `37000 m`，平台宽仍为
  `2400 m`，因此阶段 8 起点距中心实测 `4000 m`、距平台前缘 `2800 m`，
  推荐高度 `600 m`；按 `220 m/s` 到中心实测 `18.2 s`。平台碰撞从距中心
  `2000 m`（距前缘 `800 m`）才参与，重试恢复到中心前 `4000 m / 600 m`。
- `FlightAltitudeReferenceProvider` 统一服务 HUD 与雷达：阶段 1–3 显示
  `> 1 km`；阶段 4–5 以 `1.8→1.2→1.05 km` 虚拟剖面平滑下降；阶段 6–8
  使用当地地表 AGL，探测无效回退 `> 1 km` 而不是 `0 m`，数值按 `km/m`
  自动格式化。Full Diagnostics 可查看模式、虚拟值、地形值与最终值。
- 路线回归实测为快速 `80.5 s / 52.9` 燃料、常规 `101.1 s / 71.2`、观景
  `127.8 s / 88.2`；普通/Boost 一秒速度增量仍为 `234.6 / 435.0 px/s`。
  Godot `4.7.1-stable` headless import、全脚本解析、45 个测试套件、全部赤砂星
  专项、驾驶舱至着陆集成、14 张 640×360 可见录帧和中文真实窗口帮助/H/G/L
  烟雾均通过。
- 自动结果不能代替玩家对连续入大气的空间感、雷达警告/惩罚清晰度、阶段 7
  高位准备节奏、阶段 8 下降与平台侧面风险的主观判断。因此 T-045 与阶段 E
  进入 `Human Check`；T-040 至 T-044 保持 `Done`，T-050 保持 `Blocked`，
  不开始任何后续任务。

### Gate C Round 4 人工试玩步骤

在仓库根目录以中文直达驾驶舱旅行结束后的赤砂星路线：

```bash
export GODOT_BIN="/Applications/Godot.app/Contents/MacOS/Godot"
"$GODOT_BIN" --path . --language zh_CN -- --red-sand-route
```

首次进入会自动打开并暂停“飞行控制”面板；`E`、`Enter` 或“开始飞行”
关闭，飞行中按 `C` 可重新打开。核心操作为 `W` 推进、`S` 制动/有限倒车、
`↑`/`↓` 俯仰、`Shift` Boost、`F`/鼠标左键持续激光、`R` 检查点重试；
直达测试模式另提供 `H` 诊断、`G` 重力补偿、`L` 激光模块与 `Tab` 路线详情。

1. 关闭帮助后观察阶段 1–3 的 Essential HUD：高度应始终显示 `> 1 km`，不得
   出现几百米或 `0 m`。按一次 `H` 打开 Full Diagnostics，确认能看到高度模式、
   虚拟高度、地形样本和最终高度；再按一次收起，飞行区不应被遮住。
2. 保持常规推进完整观看阶段 3 后半段到阶段 4：圆盘应在约 `2200 m`、名义
   `6.95 s` 的窗口内持续放大并下移，阶段边界没有瞬移；圆盘自然越出屏幕并
   转化为曲面地平线，而不是先淡没。短暂倒车不应让星球缩小或回弹。
3. 观察阶段 4–5 高度从 `> 1 km` 平滑转为约 `1.8 km → 1.05 km` 的数值；阶段
   1–5 继续没有平直地面或隐形地表碰撞。阶段 6 起高度才使用当地地表 AGL。
4. 进入阶段 6 后先保持 `≥ 300 m`，确认 HUD 显示安全且雷达不锁定、不扣资源；
   再主动降到安全线以下，确认约 `1.0 s` 警告、`0.45 s` 锁定、清楚的脉冲音效/
   静电和一次 `12` 点护盾/船体、`2` 点货物变化。确认冷却期间不会每帧扣除。
5. 进入阶段 7，确认雷达扫描、警告和声音彻底关闭；利用 `2500 m` 无雷达走廊
   抬升到约 `400–800 m`、降低速度、稳定姿态，并从远处辨认着陆信标。
6. 进入阶段 8 时直接查看 HUD：距着陆中心应约 `4.0 km`，推荐高度约 `600 m`；
   平台在远处可见但不会立即碰撞。前段观察对准，中段减速下降，最后约
   `2.0 km` 才启用平台碰撞，正常进场到中心约 `18 s`。
7. 故意在最终进场失败一次并按 `R`：重试后仍应位于中心前约 `4.0 km`、平台
   前缘前约 `2.8 km`，有足够高度和距离重新对准，不会出生在平台侧面。
8. 完成着陆并确认进入 `ARRIVAL`；同时确认持续 Beam、追踪雷暴、Boost、帮助
   面板、两层 HUD、燃料预算和 1–3 分钟总体节奏没有回归。

请重点判断：阶段边界的空间连续性是否可信；低飞为何危险是否能从 HUD、扫描、
声音和资源变化直接理解；阶段 7 是否足以建立高位、可控的进场姿态；阶段 8 的
距离、下降节奏和平台侧面风险是否合适；高度语义是否始终符合画面。既有尾迹、
Beam、雷暴、Boost、燃料与相机仅做回归观察，不在本轮重新调参。当前仍是灰盒
与运行时合成音频，直达命令不重播驾驶舱旅行，也不包含 T-050 目的地剧情。

### Gate C Human Check Round 4 试玩反馈（2026-07-22）

**Result:** `Rework required`

- 阶段 3→4 的数值变换虽连续，但画面仍明显像两个背景状态；需要查明视觉断层
  来源，并由一条跨阶段共享进度连续驱动圆盘、地平线、辉光、云层和星空。
- 阶段 6–8 的 AGL 在真实试玩中失效并回退为 `> 1 km`，导致 HUD 与低空雷达
  无法表达 `300 m / 150 m` 等可玩高度；必须修复唯一高度源及其失败诊断。
- 阶段 7 存在疑似不可见碰撞墙；必须定位具体碰撞体、层与启用条件，保证高、
  中、低三条可见路线均可穿越，且任何阻挡都具有匹配的可见几何。
- 屏幕底部出现与飞船无关的橙色粒子；必须定位具体节点、坐标空间和生命周期，
  修复推进、Boost、激光、碰撞、重试、切段与离场后的残留或错位。
- 返工期间 T-045 与阶段 E 为 `In Progress`；T-040 至 T-044 保持 `Done`，
  T-050 保持 `Blocked`，本轮不开始后续任务。

### Gate C Round 5 自动预检（2026-07-22）

**Result:** `Human Check`

- 阶段 3→4 的视觉断层根因不是圆盘数值跳变，而是圆盘使用移动/缩放的
  `PlanetAnchor`，旧曲面地平线却是另一套固定屏幕坐标多边形；阶段切换还会
  立即改色并启用入层粒子。现在圆盘、曲面地平线、大气辉光、高层云、星空和
  入层粒子全部消费同一个 `orbit_to_atmosphere_visual_progress`，地平线与圆盘
  共享位置和尺度，阶段标签只改变玩法与文本，不再重设背景。
- 连续视觉窗口扩为路线 `11800–15800 m`，共 `4000 m`；常规巡航
  `316.67 m/s` 的名义时长为 `12.63 s`。圆盘中心沿平滑曲线下移到
  `(320, 612)` 并放大到 `7.0×`，只有自然移出视口、可见弧面降至
  `112→84 px` 后才淡出。191 张连续采样帧与边界前后帧确认没有独立地平线、
  Alpha 重置或阶段硬切。
- 阶段 6–8 AGL 失效根因是唯一向下射线在地形分段接缝或飞船参考点已位于
  旧封闭地表多边形内部/下方时无法命中，旧逻辑随即把高度静默回退为高空。
  `FlightAltitudeReferenceProvider` 现在首选专用地表层射线，并使用与可见/
  碰撞地形相同的路线剖面回退；瞬时双源失效保留最近有效 AGL，持续双源失效
  标记错误并让自动检查失败。HUD 与雷达只读取同一个 Final AGL。
- 真实场景验证 `600 m`、阈值 `300 m`、`280 m`、`150 m` 和恢复后的
  `400 m` 均返回正确数值；`600/300/400 m` 安全、`280 m` 进入警告、
  `150 m` 产生一次脉冲。真实 Input Map 的推进/低头路径从 `600 m` 降到
  `277 m`，证明玩家控制链可越过雷达阈值，不依赖测试 Stub。
- 阶段 7 空气墙确认是
  `/root/RedSandFlight/World/RouteGeometry/FloorBody07`：路线灰盒用封闭
  `CollisionPolygon2D` 填充到屏幕下方，自动闭合出的分段起点竖边在
  `30500 m` 阶段边界挡住飞船。地表碰撞现改为仅沿可见上缘的
  `SegmentShape2D`，不再生成透明侧墙；阶段 7 地表剖面保持平直，高/中/低
  `600/280/150 m` 三条路线均可完整通过。Full Diagnostics 会记录最近碰撞
  名称、路径、层/掩码和法线。
- 底部橙色特效来自
  `/root/RedSandFlight/EnvironmentFeedback/LandingDustParticles`，它位于
  `CanvasLayer` 固定屏幕坐标 `(320, 346)`，并在整个阶段 8 持续发射，因此与
  飞船或接触点无关。该节点及同类 Burst 已移除；着陆尘埃现在属于世界空间的
  `RedSandLandingZone/TouchdownDustParticles` 与 `TouchdownBurstParticles`，
  只在真实接地位置一次性触发，重试、切段和离场都会清理。
- Godot `4.7.1-stable` headless import、全脚本解析、45 个测试套件、赤砂星路线/
  危险/雷达/着陆/氛围与 Gate C 集成烟雾、Round 4 专项真实窗口烟雾、20 张
  关键帧及 191 张连续入层录帧均通过。快速/常规/观景路线仍为约
  `80.5/101.1/127.8 s`，既有燃料、持续 Beam、Boost、雷暴、帮助、HUD、
  着陆与 `ARRIVAL` 交接保持。
- 自动结果不能替代玩家对动态空间连续性、实际雷达反馈可读性、阶段 7 可见
  障碍与阶段 8 整体落地节奏的主观判断。因此 T-045 与阶段 E 返回
  `Human Check（Gate C Round 5）`；T-040 至 T-044 保持 `Done`，T-050 保持
  `Blocked`，不开始任何后续任务。

### Gate C Round 5 人工试玩步骤

在仓库根目录以中文直达赤砂星路线：

```bash
export GODOT_BIN="/Applications/Godot.app/Contents/MacOS/Godot"
"$GODOT_BIN" --path . --language zh_CN -- --red-sand-route
```

首次进入按 `E`、`Enter` 或“开始飞行”关闭帮助。操作仍为 `W` 推进、`S`
制动/有限倒车、`↑`/`↓` 俯仰、`Shift` Boost、`F`/鼠标左键持续激光、`R`
检查点重试；`H` 切换 Full Diagnostics，`C` 复开帮助。

1. 阶段 1–3 确认 Essential HUD 始终为 `高度 > 1 km`；阶段 4–5 确认高度由
   约 `1.8 km` 连续下降到 `1.05 km`，没有 `0 m` 或异常高空回退。
2. 不使用 Boost，完整观察阶段 3 后半到阶段 4 前半约 `12.63 s`：阶段标签
   改变时背景不应切换；圆盘应持续放大、下移，自然成为同一曲率地平线，星空、
   色调、薄雾和入层粒子逐渐交接。
3. 进入阶段 6 后按 `H`：确认模式为 `AGL`，高度源为射线或剖面回退，Final
   AGL 与 Essential HUD 一致。先保持约 `600 m`，再持续俯冲经过 `300 m`
   降至约 `150–280 m`；高度必须连续变化，不能重新显示 `> 1 km`。
4. 在 `≥ 300 m` 确认雷达安全；低于阈值确认约 `1.0 s` 警告、`0.45 s`
   锁定及一次 `12` 点护盾/船体、`2` 点货物脉冲；重新爬到 `400 m` 左右后
   确认危险解除。
5. 进入阶段 7 后确认雷达彻底关闭。分别尝试高位、约 `280 m` 中位和约
   `150 m` 低位向前通过；所有真正阻挡都必须有清楚可见的地形或设施，不能再
   在阶段起点撞到透明竖墙。若碰撞，可用 `H` 记录最近碰撞节点与法线。
6. 全程观察屏幕底部；静止、推进、Boost、激光、碰撞后都不应出现固定在
   `(320, 346)` 的橙色尘埃。按 `R` 后确认旧特效清空，只有实际接地时尘埃才
   出现在平台接触点。
7. 阶段 8 仍从约 `4.0 km / 600 m` 开始；完成减速、对准与下降，故意失败并
   重试一次，再完成着陆并确认进入 `ARRIVAL`。

请重点判断：阶段 3→4 是否终于像同一空间中的连续飞入；实际操控下 AGL 与
雷达反馈是否清楚可信；阶段 7 是否只剩可见障碍；底部异常橙色特效是否彻底
消失；最终进场节奏是否仍合适。现有灰盒轮廓和运行时合成音频不作为最终美术，
本轮也未重调 T-036 飞行手感或开始 T-050。

### Gate C Human Check Round 5 试玩反馈（2026-07-22）

**Result:** `Rework required`

- 阶段 6 的玩家可见高度会偶发固定为 `1 m`，向上飞行后也不继续变化；三次
  试玩中有两次复现，导致低空管制雷达无法按真实高度可靠工作。
- 当前怀疑与阶段 5→6 边界的垂直坐标、地形剖面接管或不同进入轨迹有关，
  但该说法仅作为诊断假设；必须通过边界快照、重复轨迹和多帧率测试确认真实
  根因，不能直接以重置入口高度掩盖问题。
- 本轮只修复 canonical route vertical frame、高度有效性、阶段 5→6 连续接管
  与共享 HUD/雷达高度源；已通过的入大气视觉、阶段 6–8 职责、着陆节奏和
  T-036 飞行基线不得回退。实现与自动检查期间 T-045 与阶段 E 为
  `In Progress`；T-040 至 T-044 保持 `Done`，T-050 保持 `Blocked`。

### Gate C Round 6 自动预检（2026-07-22）

**Result:** `Human Check`

- 稳定复现条件为：阶段 5 的进入轨迹使船底高度参考点位于阶段 6 地表剖面
  下方。旧 `_sample_profile()` 把负 AGL 通过 `max(..., 1.0)` 强制变为 `1 m`
  且仍标为有效，`_apply_route_samples()` 再次 Clamp 并把 `1 m` 保存为最近有效
  值；飞船爬升但尚未越过剖面时每帧继续得到同一假有效值，因此 HUD 与雷达
  一起锁在 `1 m`。是否触发取决于阶段 5 的实际 Y、俯仰和速度，所以三次试玩
  并非每次发生。
- 路线现只使用 canonical route vertical frame：实际路线距离、船体碰撞盒底部
  中心 `AltitudeReferencePoint` 和当前阶段共享地表剖面决定 Final AGL；相机、
  屏幕坐标与单调最大进度不参与。阶段 5 后半 `60%`（进度 `40%` 起）已预热
  并平滑混入同一来源，
  阶段 6 不重设飞船 Y、速度、参考点或坐标原点。
- `0 m` 与 `1 m` 现在都是合法实测高度，不再承担失败哨兵。负 AGL、剖面缺失
  或 RayCast/剖面差值超过 `4 m` 会显式无效；最近有效值最多保持 `0.20 s`，
  持续无效进入 `INVALID` 并报错，雷达在当前源无效时不会处罚。平台顶面只在
  真实平台碰撞启用后通过同一地表查询覆盖。
- `11` 条进入轨迹在 `30/60/120 FPS` 下全部通过。60 FPS 的高/中/低三条
  阶段 6 初始高度分别为 `600/380/150 m`；爬升后为
  `625.1/405.1/175.1 m`，随后下降为 `580.7/360.7/130.7 m`。20 次不同
  高度与垂直速度重复进入为 `20/20`，未出现 `1 m` 卡死、长期无效或 HUD/雷达
  分歧。
- 真实 Input Map 路径覆盖高位上升、低位俯冲和 Boost 上升：阶段 6 首帧约为
  `597–600/279–281/503 m`，随后持续爬升约为
  `1725–1733/784–789/1740 m`，来源均为 `PROFILE`。阶段 6、阶段 8 检查点和
  R 重开保持同一高度坐标；阶段 8
  检查点仍为 `4.0 km / 600 m`，到中心名义 `18.2 s`。
- Godot `4.7.1-stable` headless import、全脚本解析、45 个测试套件、全部赤砂星
  专项、驾驶舱至着陆 Gate C 集成、Round 5 高度专项和完整项目检查均通过；
  非 headless 的 Gate C 完整集成烟雾连续 `5/5` 通过。日志未出现
  `Altitude invariant violated`、长期 `HOLD_LAST_VALID`、来源超阈值不一致、
  飞船位于地形内部或把高度初始化/Clamp 为 `1 m`。
- 自动结果不能代替玩家以不同阶段 5 轨迹连续操作时对高度变化和雷达反馈的
  判断。因此 T-045 与阶段 E 返回 `Human Check（Gate C Round 6）`；T-040 至
  T-044 保持 `Done`，T-050 保持 `Blocked`，不开始后续任务。

### Gate C Round 6 人工试玩步骤

在仓库根目录以中文启动：

```bash
export GODOT_BIN="/Applications/Godot.app/Contents/MacOS/Godot"
"$GODOT_BIN" --path . --language zh_CN -- --red-sand-route
```

首次进入按 `E`、`Enter` 或“开始飞行”关闭帮助。操作仍为 `W` 推进、`S`
制动/有限倒车、`↑`/`↓` 俯仰、`Shift` Boost、`R` 检查点重试；按 `H` 打开
Full Diagnostics，重点查看高度来源、有效性、船 Y、地面 Y、Final AGL、剖面
段、混合值、失效时间和射线/剖面差值。

1. 第 1 轮在阶段 5 保持高位平飞进入阶段 6，记录首帧高度；持续抬头爬升，
   确认高度连续增加，再短暂低头确认高度连续减少。
2. 退出并重新运行上述直达命令。第 2 轮在阶段 5 持续俯冲进入，确认首帧
   明显低于第 1 轮；继续降到 `300 m` 以下触发低空雷达，再爬回 `400 m`
   以上确认危险解除。
3. 再次退出并重新运行直达命令。第 3 轮在阶段边界附近同时 Boost 与持续
   上升，确认阶段 6 不重置进入高度，Final AGL 继续响应垂直运动。
4. 第 4 轮在边界前做一次短距离倒车后再进入阶段 6；进入后按 `R` 从阶段 6
   检查点恢复，确认高度来源仍为 `PROFILE` 且没有长期 `HOLD_LAST_VALID` 或
   `INVALID`。
5. 第 5 轮自由选择中位轨迹完整飞行；在阶段 8 按 `R` 重试一次，确认仍为
   `4.0 km / 600 m` 进场，随后完成着陆并进入 `ARRIVAL`。

请逐轮记录：阶段 5 进入策略、阶段 6 第一帧高度、向上后的高度、向下后的
高度、高度来源、雷达是否按 `300 m` 阈值触发、是否出现 `1 m` 卡死。五轮中
任何一次高度固定、长期无效、HUD 与雷达判断相反，均视为 Round 6 未通过。

### Gate C Human Check Round 6 试玩反馈（2026-07-22）

**Result:** `Rework required`

- 中文直达路线的真实连续试玩在阶段 6 首次启用 AGL 时报告
  `REFERENCE_BELOW_TERRAIN_PROFILE`：船底参考 Y 已约为 `1798`，固定地表剖面
  仍约为 `660`，因此不是合法的低空高度，而是飞船已处于尚未出现的地表之下。
- 首个阶段 6 AGL 失效使该阶段检查点被拒绝；随后高度源由短暂
  `HOLD_LAST_VALID` 进入 `INVALID`，阶段 7/8 继续拒绝无效检查点，形成玩家看到
  的“高度参考异常”连锁，而不是 7/8 各自独立的 HUD 故障。
- 本轮只修复阶段 5 建立地表坐标帧、阶段 6 锁定及其检查点/重试连续性；可见
  地形、碰撞、低空雷达、着陆台和 canonical 剖面必须共用同一垂直偏移。不得
  恢复 `1 m` Clamp、隐藏错误、在阶段边界移动飞船或清空其速度/姿态。
- 新回归必须从阶段 1 真实积分到阶段 6，而不是只在边界前放置测试飞船；并
  继续覆盖不同阶段 5 轨迹、阶段 6–8 有效高度、雷达/HUD 同值、检查点和完整
  着陆。T-045 与阶段 E 回退为 `In Progress`；T-040 至 T-044 保持 `Done`，
  T-050 保持 `Blocked`。

### Gate C Round 7 自动预检（2026-07-22）

**Result:** `Human Check`

- 根因确认是阶段 1–5 没有地表但飞船仍受重力并累计世界 Y；Round 6 只验证了
  边界附近放置的轨迹，错误地把固定 `floor_y` 当成完整路线中始终有效的地面
  基准。现在 `FlightSurfaceFrame` 于路线 `20000 m`（阶段 5 进度 `40%`）按
  当前船底参考点与虚拟高度获取统一垂直偏移，并在首个真实地表边界
  `23000 m` 锁定。
- 该偏移同时应用到阶段 6–8 的灰盒地形与 `StaticBody2D`、设施和阶段标识、
  低空雷达、着陆区/平台及 canonical 地表剖面。切段不移动飞船、不清空速度
  或姿态；高位进入保留额外高度，只有预测入口低于 `180 m + 当前向下速度 ×
  1.25 s` 时才把尚未出现的未来地表继续下移。
- 新全程回归使用真实 Input Map 油门和显式速度/位置积分，从阶段 1 连续飞到
  阶段 6。确定性样本在船底世界 Y `3383.36`、向下速度 `178.09` 时锁定地表
  偏移 `3133.98`，阶段 6 首个 Final AGL 仍为 `402.77 m`、预测值
  `402.61 m`、来源 `PROFILE`；该累计下沉比玩家 Round 6 日志中的 Y `1798`
  更极端，连续 `5/5` 独立运行结果一致且没有 `INVALID`、拒绝检查点或高度
  Clamp。
- PhysicsServer2D 对移动静态体存在脚本帧后的同步延迟；偏移变化后的最多四次
  Provider 更新暂以 canonical 剖面为权威，随后必须恢复 RayCast 交叉验证并
  保持差值不超过 `4 m`。该窗口不接受负 AGL，也不会掩盖长期来源不一致。
- 阶段 6、7、8 检查点和 R 重试均保留同一锁定地表帧；阶段 8 仍恢复为距中心
  `4.0 km / 600 m`，阶段 7 长 `2500 m`，平台碰撞与 `18.2 s` 名义进场均未
  改变。雷达安全线仍为 `300 m`，入大气视觉、燃料、Beam、Boost、雷暴、
  飞行帮助、T-036 手感和 T-040 至 T-044 均未重调或重新打开。
- Godot `4.7.1-stable` headless import、全脚本解析、`46/46` 测试套件、全部
  赤砂星专项、驾驶舱至着陆 Gate C 集成和 `./scripts/check_project.sh` 通过；
  非 headless 全程地表帧与 Gate C 集成通过，20 张 640×360 可见录帧确认阶段
  6–8 的地形、设施、HUD、雷达和着陆进场保持对齐。
- 自动结果不能替代玩家对连续真实操作、阶段 5→6 地表进入构图和阶段 6–8
  高度/碰撞语义的判断。因此 T-045 与阶段 E 返回
  `Human Check（Gate C Round 7）`；T-040 至 T-044 保持 `Done`，T-050 保持
  `Blocked`，不开始任何后续任务。

### Gate C Round 7 人工试玩步骤

在仓库根目录以中文启动：

```bash
export GODOT_BIN="/Applications/Godot.app/Contents/MacOS/Godot"
"$GODOT_BIN" --path . --language zh_CN -- --red-sand-route
```

首次进入按 `E`、`Enter` 或“开始飞行”关闭帮助。操作保持为 `W` 推进、`S`
制动/有限倒车、`↑`/`↓` 俯仰、`Shift` Boost、`R` 检查点重试、`H` 切换
Full Diagnostics。

1. 从阶段 1 连续飞行，不跳段；阶段 5 采取普通平飞或轻微下降进入阶段 6。
   切段瞬间不应跳动飞船、清空速度或改变姿态，也不应出现“高度参考异常”。
2. 阶段 6 打开 Full Diagnostics，确认模式为 `AGL`、来源持续为 `PROFILE`，
   `valid` 为真，船 Y 与地面 Y 可以很大但 Final AGL 合理；HUD 和雷达必须对
   `300 m` 安全线作出一致判断。
3. 在阶段 6 主动先爬高、再降到 `300 m` 以下：Final AGL 应连续响应；高位
   保持安全，低位出现警告/锁定/一次脉冲。不得固定 `1 m`、长期
   `HOLD_LAST_VALID` 或进入 `INVALID`。
4. 进入阶段 7，确认雷达彻底关闭且高度仍有效；从高、中、低位做短距离修正，
   检查可见地形与碰撞对齐，不应撞到因地表偏移产生的新隐形墙。
5. 进入阶段 8，确认 HUD 仍为约 `4.0 km / 600 m`。故意失败并按 `R` 一次，
   重试后高度来源仍有效、出生点不在平台侧面；随后完成着陆并进入 `ARRIVAL`。
6. 如出现异常，请保留 Full Diagnostics 中的阶段、路线距离、来源/有效性、
   船 Y、地面 Y、Final AGL、射线/剖面值与失败原因；本轮尤其关注阶段 6 首帧
   以及阶段 7/8 检查点是否仍会拒绝。

请判断：真实完整路线是否彻底消除阶段 6–8 高度参考异常；未来地表随进入轨迹
建立后是否仍有自然的阶段 5→6 构图；HUD、雷达、可见地形与碰撞是否始终使用
同一高度语义；阶段 8 重试是否可靠。当前仍是 M0 灰盒，本轮未开始 T-050。

### Gate C Human Check Round 7 试玩反馈（2026-07-22）

**Result:** `Rework required`

- 完整试玩在阶段 8 越过着陆台尾缘后仍会报告“高度参考异常”：路线距离约
  `38207–38216 m`，已超过平台中心 `37000 m` 加半宽 `1200 m` 得到的尾缘
  `38200 m`。此时高度射线仍可能命中 `PadBody`，canonical 剖面却已切回普通
  地表，二者出现约 `22 m` 差异；继续前飞还会让船底参考进入普通地表以下。
- 本轮只修复平台尾缘的高度采样一致性与漏过平台后的失败/重试闭环。高度剖面
  必须按与射线相同的船底参考点水平坐标采样；未完成着陆而越过真实平台尾缘时
  应明确判定进场失败并走既有检查点重试，不能继续飞入无定义的路线尾部。
- 不加长平台本体，不放宽着陆成功区，不改阶段 8 起点、进场距离、T-036 飞行
  手感或既有高度/地表坐标帧规则。T-045 与阶段 E 回退为 `In Progress`；
  T-040 至 T-044 保持 `Done`，T-050 保持 `Blocked`。

### Gate C Round 8 自动预检（2026-07-22）

**Result:** `Human Check`

- 根因是同一帧内两套高度采样使用了不同的水平坐标：剖面按船体中心 X 判断
  是否仍在平台上，RayCast 却从旋转后的船底中心发出。现在二者统一使用
  `AltitudeReferencePoint` 的路线空间 X；专项回归确认即使船体中心先越过
  尾缘、参考点仍在 `38199 m`，剖面与 `PadBody` 射线都得到约 `120 m` AGL，
  差值不超过 `4 m`，不会进入 `RAY_PROFILE_MISMATCH`。
- 平台中心仍为 `37000 m`、宽仍为 `2400 m`、真实尾缘仍为 `38200 m`。
  船底参考点越过尾缘且尚未接地时，现在立即显示“已错过着陆平台，正在返回
  进场检查点”，冻结失败期间的高度更新，并沿既有阶段 8 检查点自动重试；
  不再允许进入 `38200–38360 m` 的无定义尾部后切换地表剖面。
- 新着陆烟雾覆盖旋转姿态下的尾缘双源一致性、越界失败、连续失败帧无高度污染、
  本地化反馈、自动恢复到 `4.0 km / 600 m` 安全进场，以及随后失败接地、勉强
  着陆和 `ARRIVAL` 交接。平台长度、成功接触区、阶段 8 距离和飞行参数均未改。
- Godot `4.7.1-stable` headless import、全部脚本解析、`46/46` 测试套件、全部
  赤砂星专项、驾驶舱至着陆集成和 `./scripts/check_project.sh` 通过；非 headless
  Metal/Compatibility 着陆专项同样通过且终端无 `ERROR`。T-045 与阶段 E 返回
  `Human Check（Gate C Round 8）`；T-040 至 T-044 保持 `Done`，T-050 保持
  `Blocked`，不开始后续任务。

### Gate C Round 8 人工试玩步骤

在仓库根目录以中文启动：

```bash
export GODOT_BIN="/Applications/Godot.app/Contents/MacOS/Godot"
"$GODOT_BIN" --path . --language zh_CN -- --red-sand-route
```

1. 正常连续飞到阶段 6–8，打开 `H` 诊断层抽查高度仍为 `PROFILE / valid`，
   终端不应再出现阶段 6/7/8 的高度参考错误。
2. 阶段 8 保持在平台上方飞过，不接地并故意越过平台尾缘。越界时应立即显示
   “已错过着陆平台，正在返回进场检查点”，飞船停止，终端不得出现
   `RAY_PROFILE_MISMATCH`、`REFERENCE_BELOW_TERRAIN_PROFILE` 或高度不变量错误。
3. 等待自动重试或按 `R`，确认恢复到距中心约 `4.0 km / 600 m` 的阶段 8
   检查点，高度来源仍有效，平台宽度和实际接触区没有变长。
4. 从该检查点重新进场并完成着陆，确认仍可进入 `ARRIVAL`；如再故意漏过一次，
   每次都应只触发一次明确失败并可靠重试，不应在路线尾部继续飞行或刷错误。

本轮只需判断真实操作下的尾缘失败提示、无错误日志和重试闭环；已接受的入大气
视觉、雷达规则、路线节奏、燃料、Beam、Boost、雷暴与 T-036 手感不再重测。
当前仍是 T-045 强制人工验收节点，玩家确认前不开始 T-050。

### Gate C Round 8 人工试玩结论（2026-07-23）

**Result:** `Passed`

- 用户明确确认 T-045 人工验收通过，接受当前赤砂星从星系接近、连续入大气、
  低空管制雷达、高位准备、最终进场到着陆的完整 M0 路线基线。
- Round 8 重点复验的有限平台尾缘提示、无高度错误日志与阶段 8 重试闭环随本次
  验收关闭；此前已接受的路线节奏、燃料、Beam、Boost、雷暴、两层 HUD 和
  T-036 飞行手感继续保持，不再留在 T-045 等待复验。
- T-045 与阶段 E 标记为 `Done`。T-005、T-011 和 T-045 均已完成，因此
  T-050 与阶段 F 解锁为 `Ready`；本轮不开始 T-050 或任何其他任务。

### 建议提交

`完成赤砂星完整进入与降落试玩版本`

---

# F. 落地剧情、结算、存档与 M0 打磨

## T-050 — 赤砂星目的地场景与主要 NPC

**State:** `Done`\
**Priority:** P0\
**Goal:** 证明认识星球人物和文明是配送的核心回报。

### 范围

- 着陆后先进入场景式对话。
- 对话后开放一个约 1–2 屏的小型可步行区域。
- 一个主要 NPC、一个可观察物件和一个可选短对话。
- 文明冲突围绕公司采掘殖民地与当地劳动者展开，但保持 M0 可理解规模。
- 对玩家的进入风格、货物完整度和激光模块作出少量反应。

### 明确不做

- 不做完整城镇、战斗、支线网或复杂 NPC 日程。
- 不在 M0 决定最终宇宙秘密。

### 验收标准

- NPC 不只是“谢谢你的包裹”，至少提供一个人物动机和一个世界观信息。
- 可步行区域不会让玩家误以为是大型探索地图。
- 对话条件稳定读取订单结果。

### 依赖

T-045、T-011、T-005。

### 自动验收（2026-07-23）

- 着陆后先以场景对话承接 `entry_style`、货物完整度与激光模块结果；每类条件
  只选择一个互斥反应分支，完成后才解锁步行探索。
- 目的地保持为 `1.5` 屏、明确封闭的居民维修场，复用 `StationPlayer` 与
  `Interactable2D`；包含主要 NPC 伊娅、订单记录终端与一段可选短对话。
- 主对话说明伊娅维护聚居地冷却与净水系统的具体动机，并揭示公司工业配额记录
  与工人聚居地实际需求之间的轻微矛盾；稳定交互结果写入剧情标记和本次订单记录。
- Godot `4.7.1-stable` 导入、全部静态脚本解析、`47` 个自动测试套件、目的地
  直接启动及专项场景烟雾均通过；中文 `640×360` 可见录帧确认主对话、探索提示、
  玩家、NPC、记录终端和场地边界可读，因此按默认规则直接标记 `Done`。
- 当前为 M0 灰盒，伊娅仍是可替换的暂定工作名；本任务未实现结算、信用点、
  返航、存档、完整城镇、战斗或支线系统。长期产品规则未变化，无需新增决策记录。

### 建议提交

`实现赤砂星交付场景与首位星球角色`

---

## T-051 — 订单结算、信用点与一次升级反馈

**State:** `Ready`\
**Priority:** P0\
**Goal:** 完成订单后获得推进主线和改善家园的明确回报，但不引入刷取压力。

### 范围

- 结算展示基础报酬、货物完整度修正和叙事结果。
- 增加公司信用点。
- 解锁一个确定升级或快递站变化。
- 主线订单即使完成得很差也能继续推进。
- 返回快递站后老皮和场景状态更新。

### 明确不做

- 不做复杂商店、维修账单、贷款或随机奖励。
- 不要求重复刷单才能完成 M0。

### 验收标准

- 报酬计算可测试且不会为负。
- 差结果减少奖励，但不撤销主线剧情标记。
- 快递站至少有一个可见变化或新互动。

### 依赖

T-050。

### 建议提交

`实现首单结算与快递站升级反馈`

---

## T-052 — 版本化存档、继续游戏与安全恢复

**State:** `Blocked`\
**Priority:** P0\
**Goal:** 玩家可放心退出和继续，坏档不会轻易抹掉最后一次有效进度。

### 范围

- 保存：剧情标记、订单状态、信用点、飞船模块、站点升级、设置引用和已读对话。
- 存档包含 schema version。
- 写入采用临时文件/备份策略。
- 主菜单支持新游戏和继续游戏。
- 旧版或非法数据给出可读错误并回退到备份/新档选项。

### 明确不做

- 不做多存档 UI、云同步或跨平台同步。
- 不序列化场景节点实例。

### 验收标准

- 从返航后退出并重新进入，状态正确恢复。
- 自动测试覆盖保存、加载、缺字段默认值和坏 JSON/数据恢复。
- 设置存档与剧情存档职责清楚。

### 依赖

T-051、T-006。

### 建议提交

`实现版本化存档与安全继续游戏`

---

## T-053 — M0 UI、提示与公司警告首轮整合

**State:** `Blocked`\
**Priority:** P1\
**Goal:** 玩家无需查看调试数字也能理解订单、飞行资源、危险和下一步操作。

### 范围

- 统一大厅交互提示、订单界面、驾驶舱热点、飞行 HUD、着陆提示和结算。
- 公司管理层电话/终端警告用于货物完整度与风险反馈。
- 重要提示不依赖颜色单一通道。
- 确保高对比度地形和路线提示可用。

### 明确不做

- 不制作最终品牌动效。
- 不堆叠持续占屏教程文字。

### 验收标准

- 完整流程中没有必须打开调试 HUD 才知道的信息。
- 提示不会遮挡关键飞行区域。
- 公司警告有黑色幽默但信息明确。

### 依赖

T-052、T-044。

### 建议提交

`统一 M0 交互提示飞行 HUD 与公司警告`

---

## T-054 — M0 全流程稳定性与内容打磨

**State:** `Blocked`\
**Priority:** P0\
**Goal:** 将所有系统收敛为一段从头到尾可连续体验的 Demo 骨架。

### 范围

- 连接主菜单到返航后的完整流程。
- 修复场景切换、状态、音频、暂停、重试和存档问题。
- 清理明显占位调试文字；保留可切换开发 HUD。
- 确认一次正常流程和一次失败重试流程。
- 完善 README 运行说明。

### 明确不做

- 不加入新系统。
- 不为了“看起来完整”提前加入其他星球。

### 验收标准

- 新档流程连续完成。
- 失败后重试流程连续完成。
- 继续游戏流程连续完成。
- `./scripts/check_project.sh` 通过。
- Godot 输出无未解释错误。

### 依赖

T-053。

### 建议提交

`打磨并稳定 M0 首单完整流程`

---

## T-055 — M0 完整闭环人工验收

**State:** `Blocked`\
**Priority:** P0\
**Goal:** 判断项目是否具备进入四星 Demo 制作的资格。

### 范围

- 由主 Agent准备一份 20–40 分钟的试玩路径。
- 提供运行命令、已知问题和关注问题。
- 用户完成从新游戏到返航。
- 根据反馈只修复 M0 阻塞/高优先级问题。

### 最终验收问题

- 是否愿意继续了解老皮和下一颗星球？
- 从驾驶舱到星球着陆是否有明显仪式感？
- 俯冲与缓降是否都成立，而非一个明显错误玩法？
- 飞行是否有重量但不令人挫败？
- 赤砂星是否像一个有文明与冲突的地方，而非背景皮肤？
- 失败与重试是否足够快？
- 公司黑色幽默是否增强而非破坏情绪？

### 完成条件

- 用户明确接受 M0 或列出有限修复项。
- 修复项完成且检查通过。
- 本任务和 M0 阶段标记 `Done`。
- 创建 M0 标签或里程碑提交。
- 之后才允许把 M1 第一颗新增星球任务写入本文件。

### 依赖

T-054。

### 建议提交

`完成 M0 首个可玩骨架验收`

---

## 任务卡维护规则

- 不在 `Current State` 中堆叠长篇开发日志；实际历史交给 Git。
- 只有真实完成并通过测试后才能标记 `Done`。
- 需要人工判断的任务标记 `Human Check`，不能由 Agent 自行代替。
- 新任务必须说明玩家价值、范围、明确不做、验收和依赖。
- M0 之外的想法先记入 `Docs/04_DEMO_SCOPE.md` 或 Decision Log，不立即扩写实现任务。
