# AGENTS.md

本文件是仓库级长期指令。它应保持短、稳定、可执行。产品细节以 `CODEX_GOAL.md`、`M1_DEVELOPMENT_TASKS.md`、`Docs/` 和 `Docs/M1/` 为准。

## 1. 每次任务的必读顺序

开始修改前必须阅读：

1. `AGENTS.md`
2. `CODEX_GOAL.md`
3. `M1_DEVELOPMENT_TASKS.md`
4. 当前任务引用的 `Docs/*.md` 与 `Docs/M1/*.md`
5. `git status`、近期相关提交和当前实现

`DEVELOPMENT_TASKS.md` 是已经完成的 M0 历史任务账本，不再作为当前任务队列。不要只根据聊天提示工作，也不要跳过仓库现状检查。

## 2. 当前范围

- 当前里程碑是 **M1：Four-Planet Demo**。
- M0 已于 2026-07-24 完成，是受保护的回归基线。
- 只实现 `M1_DEVELOPMENT_TASKS.md` 中状态为 `Ready` 或由主 Agent 明确选定的一个任务。
- 一次只允许一个 `In Progress` 任务。
- 不提前实现 M2+、完整敌人战斗、多飞船购买、程序生成路线、多单同时配送、复杂贸易或开放宇宙。
- 每个任务都必须遵守其“明确不做”。

## 3. M0 基线保护

除非当前 M1 任务明确要求或修复真实回归，否则不得改变已经通过 Gate A–D 的以下基线：

- 大厅接单、老皮教程、工作台与模态 UI 优先级。
- 驾驶舱热点、旅行状态、对话跳过与场景交接。
- 飞船基础手感、有限倒车、重力补偿、Boost、持续激光与统一伤害顺序。
- 赤砂星完整路线、canonical 高度、雷达、着陆与检查点。
- 目的地剧情、结算、返站、版本化存档与继续游戏。

任何共享系统改动都必须运行完整 M0 闭环回归。不得以“泛化 M1 架构”为理由静默破坏赤砂星。

## 4. 不可擅自改变的产品事实

- 中文标题：`《我在宇宙送快递》`；英文工作名：`Universe Deliver`。
- 玩家是来自地球的人类，是银河外卖公司的快递员。
- 老皮是一名会说话、会直立行走的外星水豚，从开场起就是固定伙伴。
- 不同文明通过同声传译器交流。
- 快递站大厅为 2D 俯视角；星际旅行为 2D 第一人称可交互驾驶舱；星球进入与配送为横向侧视飞行。
- 星球进入从“飞入星系”开始，必须包含接近星球和可操控的大气层进入。
- 游戏是有明确主线与结局的单人叙事游戏，不是 Roguelike。
- 主线星球顺序基本线性，局部选择改变关系、对白、支线和结局权重，不制造指数级分支。
- 玩家长期使用同一艘飞船，通过数值、模块和剧情功能升级。
- M1 激光炮仍只用于陨石和明确环境障碍；星球地表剧情没有战斗。
- 主线失败不得造成经济死档；不设置阻止继续出发的强制维修费。
- 货物是剧情物件，不是随机词条集合。
- 不得自行把 PROVISIONAL 或 OPEN 内容升级为 LOCKED 正史。

## 5. M1 内容规则

- 主线工作顺序为：赤砂星基线 → 白噪星 → 赤砂星回访 → 穹林星 → 群潮星。
- 每颗新主线星球先完成 Planet Packet、Order Packet 和 Character Packet，再开始高成本关卡与资产制作。
- 每颗星球必须同时具备自然、人文、冲突、独特飞行、剧情货物、核心 NPC、到达场景、回访种子和独立视听身份。
- 新机制优先以可复用的小组件实现，但不得把星球差异抽象成同一套换皮参数。
- M1 至少实现一次有明显变化的赤砂星回访和一份正式低空投放订单。
- 白噪星是第一颗新增星球和第一个 M1 垂直切片。
- M1 暂定故事与角色名以 `Docs/M1/` 的 Canon Status 为准。

## 6. 技术基线

- Godot `4.7.1-stable`，升级必须是独立任务。
- GDScript；新增脚本默认使用静态类型。
- Compatibility renderer。
- 纯 2D、16:9、内部视口 640×360、`viewport + keep + fractional fit`；像素纹理 nearest。
- 首要平台：macOS / Apple Silicon；首要输入：键盘与鼠标。
- 输入必须通过 Input Map action，不在业务代码中散落物理按键判断。
- 飞船主体继续使用 `CharacterBody2D` 和显式速度积分。
- 只保留少量真正跨场景的 Autoload；不要创建万能 Manager 或默认全局 EventBus。
- 数据优先使用自定义 `Resource` 或可验证结构化文件。
- M1 存档增量必须提供从 M0 schema v1 的迁移和坏档安全恢复。

## 7. 代码与数据规范

- 文件、目录、类、变量、信号、资源 ID 使用英文 `snake_case` 或 Godot 约定命名。
- 类型名使用 `PascalCase`；常量使用 `UPPER_SNAKE_CASE`。
- 玩家可见文本使用本地化 Key；禁止把长段剧情硬编码进脚本。
- 中文是源语言；README 中英双语；开发文档中文为主。
- 单个脚本只承担清晰职责。优先组合、信号和小型 Resource。
- 不为尚未出现的需求搭建复杂框架。
- 可影响飞行手感和星球环境的关键值必须集中配置。
- 公开接口和非显然算法写简短注释；不要用注释重复代码。
- 不使用 `@warning_ignore` 掩盖真实问题。

## 8. 依赖与资产

- 默认不引入第三方插件、框架、SDK 或运行时服务。
- 引入前先在 `Docs/09_DEPENDENCY_LOG.md` 记录目的、候选、许可证、版本、替代方案和移除成本。
- 免费素材和生成式素材必须在 `ATTRIBUTION.md` 记录来源与许可。
- 生成式美术先作为占位或候选，不得无说明地视作最终资产。
- 不提交 `.godot/`、导出构建、临时缓存或本机设置。

## 9. 测试与完成条件

除纯文档任务外，完成前必须：

1. 运行当前任务专项测试。
2. 运行 `./scripts/check_project.sh`。
3. 确认 Godot 无报错导入。
4. 对受影响的可玩路径执行最短烟雾测试。
5. 运行完整 M0 回归，若修改共享状态、场景流、飞行、UI、存档或内容注册表。
6. 检查 `git diff --check` 和 `git status`。

自动测试重点覆盖：

- M0 存档迁移到 M1 schema。
- 多星球解锁、订单与通行许可状态。
- 数据定义和稳定 ID 引用。
- 关系、图鉴、纪念品与升级状态。
- 交付方式、支线计时和报酬。
- 飞行纯数学、环境参数和危险组件。
- M0 完整闭环不回归。

飞行手感、视觉节奏、对白和音频主要依赖人工试玩，不伪装成完全自动验收。

## 10. Git 规则

- 只由主 Agent 提交；子 Agent 不提交。
- 提交前必须通过适用测试。
- 一个提交对应一个完整、可解释的增量。
- 提交信息优先中文。
- 不修改、覆盖或删除用户未提交的工作。
- 不使用破坏性 Git 命令，除非用户明确要求。
- 不通过重写历史整理未经用户确认的提交。

## 11. M1 必须暂停人工试玩的节点

完成以下任务后必须停止扩范围：

- Gate E：白噪星第一颗新增星球垂直切片。
- Gate F：升级、支线、低空投放与赤砂星回访闭环。
- Gate G：穹林星自然/文明/武装态度差异。
- Gate H：群潮星高潮与中继网揭示。
- Gate I：M1 四星 Demo 完整闭环。

具体任务编号和验收问题见 `Docs/12_M1_ITERATION_GATES.md` 与 `M1_DEVELOPMENT_TASKS.md`。

## 12. 任务结束报告

每次完成任务后报告：

- 完成了什么。
- 修改了哪些关键文件。
- 运行了哪些命令及结果。
- 是否提交，以及提交摘要。
- M0 回归是否通过。
- 已知问题。
- 下一项 Ready 任务。
- 是否到达人工试玩节点。

如果测试或实现失败，明确说明实际状态，不得声称通过。