# AGENTS.md

本文件是仓库级长期指令，应保持短、稳定、可执行。产品细节以 `CODEX_GOAL.md`、`DEVELOPMENT_TASKS.md`、`Docs/M1/` 与长期 `Docs/` 文档为准。

## 1. 每次任务的必读顺序

开始修改前必须阅读：

1. `AGENTS.md`
2. `CODEX_GOAL.md`
3. `DEVELOPMENT_TASKS.md`
4. `Docs/M1/00_M1_ROADMAP.md`
5. `Docs/M1/01_M1_PRODUCT_SCOPE.md`
6. 当前任务引用的其他文档
7. `git status`、近期相关提交和当前实现

不要只根据聊天提示工作，也不要跳过仓库现状检查。

## 2. 当前范围

- 当前里程碑是 **M1：Four-Planet Demo**。
- M0 已完成并冻结为回归基线，归档见 `Docs/Archive/M0_MILESTONE.md`。
- 只实现 `DEVELOPMENT_TASKS.md` 中依赖满足、状态为 `Ready` 或由主 Agent 明确选定的一个任务。
- 存在 `In Progress` 或未处理的 `Human Check` 时，不领取新任务。
- 不跳过 M1-A 基础直接批量制作白噪、穹林或群潮内容。
- 不提前实现 M2+ 的完整结局、更多星球、完整战斗、多飞船或大型经营。
- 每个任务必须遵守其“明确不做”。

## 3. M0 回归保护

不得无理由破坏：

- 主菜单 → 大厅 → 驾驶舱 → 赤砂星 → 到达 → 结算 → 返站 → 保存/继续的完整流程。
- M0 v1 存档的读取与安全迁移。
- 已通过的飞行手感、检查点、护盾优先伤害、持续 Beam、主线安全网和赤砂星路线。
- 模态 UI、交互提示、对话整段跳过和场景输入恢复。
- Godot 4.7.1、Compatibility、640×360、16:9、nearest 与键鼠优先基线。

任务触及 `GameState`、`SaveService`、订单、导航、通用对话、飞船、路线模板、结算或站点状态时，必须运行 M0 完整闭环回归。

## 4. 不可擅自改变的产品事实

- 中文标题：`《我在宇宙送快递》`；英文工作名：`Universe Deliver`。
- 玩家是来自地球的人类，是银河外卖公司的快递员。
- 老皮是会说话、直立行走的外星水豚，从开场起是固定伙伴。
- 不同文明通过同声传译器交流。
- 快递站为 2D 俯视；星际旅行为 2D 第一人称可交互驾驶舱；星球进入为横向侧视飞行。
- 星球进入必须从飞入星系、接近星球和可操控大气层开始，不能直接传送到地表。
- 游戏是有主线与结局的单人叙事游戏，不是 Roguelike。
- 主线基本线性，局部选择改变关系、对白、许可、路线便利和结尾变量，不制造指数级分支。
- 玩家长期使用同一艘飞船，通过数值、模块和剧情能力成长。
- M1 激光炮仍只作用于陨石和明确环境目标；没有地表战斗。
- 主线失败不得造成经济死档；不设置阻止继续出发的强制维修费。
- 货物是剧情物件，不是随机词条集合。
- M1 主线顺序工作方案为赤砂 → 白噪 → 赤砂回访 → 穹林 → 群潮。
- 未锁定世界观必须遵守 `Docs/01_NARRATIVE_BIBLE.md` 的 Canon Status，不得静默升级为正史。

## 5. M1 内容规则

- 每颗新增主线星球先完成 Planet Packet、Order Packet 和 Character Packet。
- 星球差异必须同时体现自然、人文、人物冲突、货物和独特飞行机制。
- 共用飞船、HUD、检查点和交接合同可以抽取；星球专属逻辑不得强塞进通用基类。
- 白噪星是第一颗新增垂直切片；Gate E 通过前不批量制作后两颗星球的高成本路线。
- M1 至少实现一次明确回访和低空投放第二交付方式。
- 主线必需模块、许可和资源必须有确定获取路径，不允许随机刷取。
- 支线提供角色、探索、图鉴、许可或替代路径，不做随机合同池。

## 6. 技术基线

- Godot `4.7.1-stable`。
- GDScript；新增脚本默认静态类型。
- Compatibility renderer。
- 纯 2D、16:9、640×360、`viewport + keep + fractional fit`；像素纹理 nearest。
- 首要平台：macOS / Apple Silicon。
- 首要输入：键盘与鼠标；业务代码只读取 Input Map action。
- 飞船使用 `CharacterBody2D` 和显式速度积分，不把核心手感交给刚体涌现。
- 只保留少量真正全局 Autoload；不要创建万能 Manager 或全局 EventBus。
- 数据优先使用自定义 Resource 或可验证结构化文件。
- M1 存档使用 schema v2，并支持 M0 v1 → v2 迁移。

## 7. 代码规范

- 文件、目录、变量、信号、资源 ID 使用英文 `snake_case`；类型使用 `PascalCase`；常量使用 `UPPER_SNAKE_CASE`。
- 玩家可见文本使用本地化 Key，禁止把长段剧情硬编码进脚本。
- 中文为源语言；README 中英双语；开发文档中文为主。
- 单个脚本只承担清晰职责。优先组合、信号和小型 Resource。
- 不为尚未出现的需求搭建复杂框架。
- 影响飞行手感、危险和交付窗口的参数必须集中配置。
- 公开接口和非显然算法需要简短注释；不要用注释重复代码。
- 不使用 `@warning_ignore` 掩盖真实问题。

## 8. 依赖与资产

- 默认不引入第三方插件、SDK 或运行时服务。
- 新依赖先在 `Docs/09_DEPENDENCY_LOG.md` 记录目的、候选、理由、许可证、版本、替代方案和移除成本。
- 外部素材、字体和音频记录到 `ATTRIBUTION.md`。
- 生成式素材先标记为 placeholder/candidate，不得无说明视作最终资产。
- 不提交 `.godot/`、导出构建、临时缓存或本机设置。

## 9. 测试与完成条件

除纯文档任务外，完成前必须：

1. 运行 `./scripts/check_project.sh`。
2. 确认 Godot 无报错导入。
3. 运行当前任务专项测试。
4. 执行受影响路径最短烟雾。
5. 需要时运行 M0 完整闭环回归。
6. 检查 `git diff --check` 和 `git status`。

自动测试重点覆盖：

- 数据 ID 和引用。
- v1 → v2 存档迁移、备份和恢复。
- 章节、星球解锁、订单、许可、关系、图鉴与回访状态。
- 飞行数学、资源、伤害和危险。
- 低空投放和支线报酬。
- 新星球关键场景与完整路线烟雾。
- M0 完整闭环。

飞行手感、星球差异、视觉节奏、对白和音频主要依赖人工 Gate，不伪装成完全自动验收。

## 10. 强制人工 Gate

完成下列任务后停止扩展并等待玩家试玩：

- T-118：Gate E — 白噪星垂直切片。
- T-126：Gate F — 回访、支线与成长。
- T-138：Gate G — 穹林星与和平/武装反馈。
- T-147：Gate H — 跨星球成长整合。
- T-158：Gate I — 群潮星与 Demo 高潮。
- T-166：Gate J — M1 完整 Demo。

细则见 `Docs/M1/05_M1_GATES_AND_TESTING.md`。

## 11. Git 规则

- 只由主 Agent 提交；子 Agent 不提交。
- 提交前必须通过适用测试。
- 一个提交对应一个完整、可解释的增量。
- 提交信息优先中文。
- 不修改、覆盖或删除用户未提交的工作。
- 不使用破坏性 Git 命令，不重写未经用户确认的历史。

## 12. 任务结束报告

每次完成任务报告：

- 完成的任务和最终状态。
- 所属阶段状态。
- 修改的关键文件。
- 实际运行的命令和结果。
- commit hash 与提交信息。
- 已知问题和 M0 回归结果。
- 下一项可能解锁的任务。
- 是否到达人工 Gate；若到达，给出运行方式和关注问题。

任务失败或测试失败时必须说明真实状态，不得声称完成。
