# M1 Systems Delta / M1 系统增量

本文只描述相对 M0 的新增或扩展。M0 已通过的飞行、模态、伤害、检查点、存档备份与场景流合同默认保持。

## 1. 存档与进度

新增：

- schema v2 与 v1 迁移。
- `main_story_chapter`。
- `unlocked_planet_ids`。
- `planet_relation_values`。
- `planet_permission_ids`。
- `codex_entry_ids`。
- `souvenir_ids`。
- `completed_side_order_ids`。
- `station_state_level` / `station_upgrade_ids`。
- `ship_upgrade_ids`。
- `revisit_state`。
- `demo_ending_flags`。

不新增多存档槽、云存档或飞行节点序列化。

上述字段的运行时真相仍在 `GameStateModel`。章节、星球、关系、许可、图鉴、
纪念品和回访统一通过公开 API 修改；`M1ProgressRules` 只提供无状态白名单与准入
评估，`SaveService` 继续捕获同一批 schema v2 字段。

## 2. 多星球导航

- 导航由 PlanetDefinition、章节、许可和模块共同决定。
- 正常订单终端与驾驶舱导航从独立 `m1_data_registry.tres` 读取完整目录；
  `m0_data_registry.tres` 只保留为冻结回归夹具和显式兼容注入，不再是正常场景入口。
- `M1CatalogModel` 以无状态投影统一生成订单目录、星球目录、锁定原因和模块状态；
  UI 只保存当前选中项，不复制章节、解锁、订单或航线真相。
- 星球注册与路线可玩性分离：赤砂回访在 T-112 完成正式短路线闭环后为
  `PLAYABLE`；白噪在 T-125 完成“接单—配置—驾驶舱—独立路线—目的地选择—
  结算—返站—存档/继续”闭环后也为 `PLAYABLE`。穹林与群潮在各自正式订单
  流程完成前仍是 `REGISTERED_ONLY`，没有正常玩家可选场景路径，也不借用
  已完成星球的路线。隔离调试夹具不改变正式内容门禁。
- T-126 的白噪必做支线在白噪主单完成后才显示为可接取，并明确标注为自愿；
  它复用白噪主航路 `17000–34000 m` 后半段和同一目的地区域。该支线为
  `PLAYABLE`，但不是穹林主线的前置条件。
- 未解锁星球可以显示轮廓与解锁原因，但不能选择。
- 主线顺序默认固定：赤砂回访 → 白噪 → 穹林 → 群潮。
- 支线只在对应章节和关系条件满足时显示。
- 驾驶舱仍不自由驾驶星图。
- 当前白噪导航解锁要求精确完成赤砂屏蔽改装回访、到达白噪章节、赤砂已解锁且
  已拥有特高压电屏蔽罩；真正的出发资格还要求把模块安装到 Defense 槽。穹林与
  群潮各要求前序章节和前一颗星球，不允许越级解锁。
- T-114 将白噪准备状态集中为“前序未完成 / 模块未获得 / 已拥有未安装 /
  资格满足但正式航路未开放”四种只读投影。T-125 开放正式航路后，最后一种
  状态自然转为可接取/可出发；订单终端、模块工作台和驾驶舱仍共同消费同一
  结果，导航节点解锁本身仍不等于已经安装必需模块。

## 3. 订单模型

- 一次仍只允许一份 active order。
- `OrderDefinition` 增加 `MAIN / SIDE / REVISIT`、`LANDING /
  LOW_ALTITUDE_DROP`、章节与解锁条件、加急参数、关系/许可/图鉴/纪念品奖励及
  `UNIQUE / REPEATABLE / ARCHIVED_ONLY` 重复策略。
- 订单也使用 `REGISTERED_ONLY / PLAYABLE` 内容就绪状态。Packet 中尚未落地剧情
  与路线的 M1 订单可以进入开发目录，但不能接取、确认配置或开始旅行；玩家看到
  稳定的本地化获取路径，不看到内部 ID。
- 持久状态统一为 `AVAILABLE / ACCEPTED / COMPLETED / FAILED / ABANDONED /
  ARCHIVED`；只有 `ACCEPTED` 占用 active order 槽位，存档中也只允许一个
  `ACCEPTED`。
- 主线与回访订单不可误放弃；失败时保持 `ACCEPTED`，由既有检查点/重试流程继续，
  不制造主线死档。
- 支线可放弃、失败或以降低报酬的完成结果表达部分成功。`REPEATABLE` 只允许
  `FAILED / ABANDONED → ACCEPTED` 的显式重接；任何 `COMPLETED` 订单都不能再次
  接取或重复发奖。
- `ARCHIVED_ONLY` 用于只记录历史而不进入 active order 的条目，不是可接任务。
- 支线失败不撤销主线章节、模块或星球资格。
- 接受条件统一消费 T-102 的章节、星球、许可和模块状态；订单完成通过
  `GameStateModel` 的单一入口原子应用信用点、关系、许可、图鉴、纪念品、飞船
  模块所有权、导航节点、站点状态、章节、回访状态和完成标记，并写入奖励幂等
  账本。导航节点奖励使用 `planet_unlock_rewards`，只有预测结算后的全部准入
  条件成立时才允许事务提交。
- `required_completed_order_ids` 表达必须真实完成的前序订单，不允许只靠章节或
  剧情标记越级。赤砂回访同时要求 M0 实际订单 `order_red_sand_m0` 已完成。
- T-112 的赤砂回访使用统一结算入口的受校验附加参数：本地保留记录分支可增加
  一次赤砂关系奖励，剧情奖励模块可在同一事务中写入所有权并安装到合法槽位。
  附加关系、模块引用或槽位不合法时，结算在信用点和任何进度变化前整体拒绝。
- T-125 在同一结算入口增加受校验的附加图鉴和稳定结尾值：白噪三项选择只发放
  对应的一条异常图鉴，并写入唯一 `ending_archive_choice` 值。基础订单奖励、
  选择附加关系、档案许可、纪念品、白噪回访种子、穹林章节/导航与站点反馈
  仍在一次奖励账本事务中提交；重复结算或非法附加值不会产生部分奖励。
- T-126 继续使用同一原子结算入口：三项私人返件选择写入唯一
  `ending_white_noise_memory_return` 值，并让既有人物/货物图鉴按选择投影不同
  说明。货物完整度低于 `70%` 时附加白噪关系 `-1`，只抵消本单基础 `+1`；
  信用点继续使用统一货损公式。失败或放弃只终止这份 `UNIQUE` 支线，不回退
  白噪主单、穹林章节、导航、许可或模块。
- 章节、模块、站点和回访奖励在任何数值变更前统一校验；非法章节跳级或未知
  ID 会拒绝整次结算，不产生部分奖励。
- 订单终端按“当前主线 / 可选支线 / 下一步线索 / 历史”分层：active order 固定
  置顶，同一时刻最多一个当前主线，未发现的支线不泄露名称。`COMPLETED /
  ARCHIVED` 历史由同一注册表与订单状态投影为逐条可选的只读详情，不提供接取；
  若未持久化详细飞行结算，只显示已知订单资料、剧情选择摘要和“详细飞行结算
  未保留”。M0 兼容 alias 在投影前归一化，不生成重复历史。
- 不建立随机刷新、每日任务或无限委托板。
- 图鉴和纪念品奖励引用类型化 `CodexEntryDefinition` /
  `SouvenirDefinition`，注册表不再用平行手写 ID 列表维护同一内容。
- M0 文档 canonical ID `order_red_sand_cooling_core` 仅作为历史 alias 指向实际
  `order_red_sand_m0`，不会生成第二份可接订单。

## 4. 支线计时与报酬

加急支线可以配置：

```text
target_seconds
grace_seconds
minimum_reward_ratio
relation_bonus_on_time
```

规则：

- 只有 active 的加急订单累计 `OrderRunState.elapsed_time`；非加急订单不累计。
- `elapsed_time <= target_seconds` 时保留全额信用点及准时关系奖励；随后在
  `grace_seconds` 内线性降至 `minimum_reward_ratio`，继续超时也不低于该下限，
  不直接硬失败。
- 对话、帮助面板和系统暂停均暂停计时。
- 检查点重试保留同一计时；完成后由 `reward_applied_order_ids` 阻止重复增加奖励。
- 非加急订单不显示倒计时压力。

T-108 的运行时落地保持单一计时入口：

- `App/PersistentUI/ExpressOrderHUD` 是跨场景保留的唯一驱动；每帧只由组内权威
  实例调用一次 `GameStateModel.advance_active_order_time()`，阶段场景和各自 HUD
  不再另行累计。
- 驱动使用 `PROCESS_MODE_ALWAYS` 观察持久对话层、飞行帮助层和
  `SceneTree.paused`，冻结时仍可刷新“已暂停”反馈，但不增加 `elapsed_time`。
  对话与帮助打开时由模态面板内提示接管反馈并隐藏紧凑 HUD，避免边缘残片；
  单纯系统暂停时紧凑 HUD 保持可见。
- 场景切换、检查点与 `R` 重试只改变场景/飞行状态，不重置计时；schema v2
  继续保存该字段。读取存档后从已保存值继续，不根据系统时钟补算离线时间。
- 紧凑 HUD 固定为两行，区分全额、宽限、保底和暂停，并显示 `mm:ss` 与当前
  信用点比例；非加急订单整块隐藏。订单终端只对加急订单显示目标、宽限、最低
  比例及“超时不会直接失败”的说明。
- 结算严格按“基础信用点 → 货物完整度修正 → 加急时间修正 → 最终信用点”计算。
  `OrderSettlementCalculator` 生成完整结果，`GameStateModel` 只提交已经计算好的
  最终值，不再重复乘时间倍率；准时关系奖励仍由统一完成入口和奖励账本保证只
  发放一次。

群潮支线“风眼前的灯”在 T-108 仍为 `REGISTERED_ONLY`；上述流程只通过独立测试
夹具验证，不提前开放路线或玩家接单入口。

## 5. 第二种正式交付：低空投放

M1 的低空投放先使用固定、宽容接收区：

- 玩家进入高度、速度和水平位置窗口。
- 按统一 Input Map action 释放货物。
- 成功：落入核心接收区。
- 部分成功：落入外圈，降低报酬/关系。
- 失败：错过区域或货物损坏，可重试或放弃支线。
- 投放后同一货物不可重复释放。
- 不做军事投弹、复杂弹道或移动敌对目标。

T-107 的独立 `Delivery Lab` 先锁定以下可调原型基线：

- 有效高度 `90–220 m`，有效水平速度 `90–240 m/s`。
- 核心接收区为中心 `±110 m`，外圈为中心 `±220 m`。
- 货物以 `120 m/s` 的固定下降速度运动，继承飞船 `65%` 的水平速度；预计落点
  只由释放位置、高度和水平速度确定，不读取随机数或刚体碰撞。
- 外圈部分成功保留 `75%` 货物质量和 `65%` 报酬；核心成功为全额，外圈以外
  为可重试失败。
- 高度或速度不合法时货物不会被释放；合法释放后单货物门闩立即关闭，重复键鼠
  输入只给出原因，不产生第二件货物或第二次结算。
- `R` 从 Delivery Lab 检查点同时恢复飞船、货物、预计落点和投放结果。该场景
  不写入活动订单、信用点或穹林星正式路线状态。

首次正式使用：穹林星支线“孢子季急投”。

## 6. 关系、许可与玩家方式

关系值是小型叙事状态，不是可刷声望条。

影响：

- 后续对白。
- 部分支线和回访入口。
- 许可或便利路线。
- 图鉴文本。
- Demo 结尾变量。

来源：

- 主线局部选择。
- 支线结果。
- 武器封存/保留。
- 货物状态和交付方式。

不得通过重复同一订单无限刷关系。

M1 运行时关系范围集中为 `-2…+3`。每次正常关系变化携带唯一事件 ID，已应用事件
随 `story_flags` 保存；重复事件、到达边界后的无效变化和重复许可均不再次发出
持久化变更。

白噪主单目的地保存三个互斥局部选择之一：
`minimum_index / keep_sealed / local_custody`。选择完成标记、可选记忆持有人对白
和中继记录检查均使用稳定 `story_flags` / `optional_trigger_ids`，三路立即汇回
同一返航入口。T-125 的统一结算一次性应用基础白噪关系 `+1`、档案许可、
纪念品、订单完成、选择专属图鉴、结尾值、白噪回访种子和穹林前置信息；
保持封存/当地共同保管当前各有一项可调的额外关系 `+1`。目的地场景不提前发奖，
也不根据按钮分叉出多套主线关卡。

## 7. 飞船成长

### 特高压电屏蔽罩

- 白噪星主线资格模块。
- 降低电磁干扰和高压环境风险。
- 不替代普通护盾资源。
- 由赤砂回访确定获得。
- T-110 将它登记为赤砂回访唯一、免费且不可重复的主线奖励；T-111 已落地所有权
  与安装分离、schema v2 往返恢复、能力标签读取和飞船可见屏蔽轮廓。
- T-112 在赤砂回访正式结算时原子发放并自动安装该模块；玩家之后仍可在正常
  工作台卸下或重新安装，同时原子解锁白噪导航节点，不建立不可逆的隐藏配置。
- 模块必须实际安装在 Defense 槽后才生效；仅拥有模块只解锁后续目录/导航资格，
  不能通过白噪出发前检查。
- 当前可调工作基线为高压资源伤害乘数 `0.60`、电磁干扰乘数 `0.45`。高压伤害
  缩放后仍进入 M0 的普通护盾优先伤害入口，不增加第二条“特殊护盾”资源。
- 配置工作台会区分“尚未获得”“已拥有未安装”“已安装”，并明确剧情模块由
  对应主线固定获得，不提供购买、随机掉落或刷取路径。
- 槽位显示始终以真实已安装模块或当前可管理的已拥有模块为身份来源；“主线必需 /
  订单推荐 / 本次订单不要求”是独立订单角色，“已安装 / 已拥有未安装 /
  尚未获得”是独立状态。订单角色不得覆盖模块名称、说明或已拥有模块的装卸按钮，
  必需配置检查仍只消费订单 `required_modules`。

### 生物信号隔离舱

- 降低穹林星货物吸引生物的范围/频率。
- 不使玩家完全免疫生态风险。

### 横风稳定器

- 降低群潮星横风扰动。
- 不自动驾驶、不取消风向判断。

配置界面继续保持少量明确槽位；不引入随机品质与复杂科技树。

## 8. 快递站成长

### 档案终端

- 赤砂回访后解锁。
- 查看人物、星球、货物和异常记录。
- 旧机柜外壳从开局起始终可见；回访前屏幕、状态灯和正式标签断电，只能观察
  “旧档案接口尚未接入”，不能浏览。`station_state_archive_terminal` 精确 ID
  只控制通电内容与正式交互，不从摘要等级反推。
- 赤砂回访结算写入一次性待处理标记；玩家返站后由老皮说明他把回访记录与旧中继
  铭牌数据接入现有机柜，剧情结束后开放浏览。已经解锁的存档直接显示通电状态，
  不重播激活剧情。
- T-113 继续用同一设备根节点接入正式交互、浏览与焦点恢复；外壳可见不等于功能
  解锁。
- T-113 终端直接使用 v2 图鉴只读投影，显示已知的赤砂/白噪星、伊娅、剧情货物、
  旧中继铭牌与异常回波。图鉴“已知”和导航“已解锁”仍是两份独立状态；T-114
  只在赤砂回访结算的章节、完成记录、剧情标记和模块奖励全部成立时同步解锁后者。
- 老皮提供一次性白噪线索简报，只记录公开日志与旧铭牌断续噪声的相似性，不确认
  来源或含义。

### 生态角

- 穹林星后解锁。
- 提供生活感、角色对话和纪念品展示。

### 中继观测室

- 群潮星结尾解锁。
- 承担 Demo 最终中继信号和星图外坐标。

站点变化以可见场景和新互动为主，不发展成资源经营模拟。

T-106 只建立三个设施的隐藏场景根节点，不提前放置互动或剧情。功能解锁以
`station_upgrade_ids` 中的精确稳定 ID 为权威：

```text
station_state_archive_terminal
station_state_ecology_corner
station_state_relay_observatory
```

`station_state_level` 只取已解锁状态要求的最高等级，作为单调递增摘要；等级本身
不能反向推导或提前显示设施。重复解锁不发出第二次持久化变更，存档恢复只刷新场景
根节点，不重放升级事件。

## Gate E 玩家可见字体

- Gate E 的大厅、档案、订单终端、配置、驾驶舱、对话、赤砂回访 HUD、飞行帮助、
  抵达、结算与调试状态框共用 `data/ui/chinese_ui_font.tres`。
- 主字体候选保持 `PingFang SC → Noto Sans CJK SC → Noto Sans SC →
  sans-serif`，启用系统回退；另以 `Font.fallbacks` 提供逐字形平台回退，避免
  Godot 仅选中第一个可用系统字体后留下中文缺字。
- 该资源不打包第三方字体，不新增依赖；所有实际本地化中文字符由 Gate E 专项
  测试逐字验证，并以 640×360 非 headless 录帧确认没有十六进制缺字框、空白方块
  或乱码。

## 9. 星球独特机制扩展方式

核心飞船脚本不得堆叠 `if planet_id == ...`。

推荐：

- `FlightEnvironmentProfile`：重力、阻力、风、能见度。
- 小型危险组件：电磁干扰、生态吸引、横风区域。
- `FlightRouteDefinition`：阶段、检查点、背景和交付目标。
- 模块 capability tags：环境适应能力。
- 独立纯逻辑模型：可自动测试的危险与判定。

M0 的统一伤害、HUD、帮助、重试和 SceneRouter 继续复用。

## 10. 星球系统摘要

| 星球 | 核心环境 | 独特机制 | 关键模块 | 交付 |
|---|---|---|---|---|
| 赤砂回访 | 沙漠与维修场变化 | 短回访路线/剧情改装 | 取得屏蔽罩 | 着陆 |
| 白噪 | 强重力、电磁暴雪、低能见度 | 干扰与屏蔽 | 特高压电屏蔽罩 | 着陆 |
| 穹林 | 低重力、树洞、孢子、生物群 | 武器封存、货物吸引生态 | 生物信号隔离舱 | 着陆 + 低空投放 |
| 群潮 | 横风、海面、岛屿、天气塔 | 风向与塔控制区 | 横风稳定器 | 浮动平台着陆/简化对接 |

## 11. 图鉴与纪念品

基础类别：

- 星球。
- 人物。
- 货物。
- 纪念品。
- 异常信号。

原则：

- 未解锁条目不剧透。
- 图鉴记录人物和选择结果，而非只列百科。
- M1 不要求 100% 收集。
- 约 95% 隐藏结局属于完整游戏远期，不在 M1 实现。

T-106 基础实现：

- `CodexCatalogModel` 是无状态只读投影，输入为 M1 注册表与
  `codex_entry_ids / souvenir_ids`；分类、列表和详情 UI 不直接改写进度。
- `hidden_when_locked = true` 的条目完全不进入结果；允许保留锁定位的条目只显示
  通用“未知记录”，不暴露标题与说明。
- 图鉴中的纪念品条目与物理纪念品 ID 只投影为一条记录，避免旧中继铭牌重复。
- 快递站纪念品墙固定按 `GameDataRegistry.souvenirs` 顺序生成多槽位；未获得槽位
  只显示统一锁定占位。
- 纪念品详情使用 `StationModalCoordinator` 的阻塞模态，关闭后恢复玩家控制、
  交互提示和打开前的 UI 焦点。
- `CodexBrowserUI` 由 `StationArchiveTerminalController` 挂入正式档案终端，
  仍保持只读投影；它与纪念品详情共用 `StationModalCoordinator` 的世界输入锁，
  关闭后恢复原焦点、玩家控制和交互提示。
- 已完成赤砂回访的旧 schema v2 存档会幂等补齐 T-113 新增的白噪星与异常回波
  记录；未完成回访的存档不获得这些内容，也不会提前获得白噪导航许可。

## 12. M1 回归合同

每个重要提交至少保护：

```text
M0 新游戏与继续
M0 赤砂首单
v1 → v2 迁移
一次一单约束
模态 UI 与输入恢复
飞行伤害与检查点
赤砂 canonical AGL
存档主档/备份
```

每颗新星球应提供：

- 场景实例化测试。
- 路线阶段与检查点测试。
- 独特机制纯逻辑测试。
- 订单从接取到结算的烟雾。
- 直接调试入口。

T-109 建立统一的 `--m1-debug=<scenario_id>` 开发入口，目前覆盖赤砂回访、
白噪目录、白噪路线、穹林目录、群潮目录、低空投放、加急订单和 Gate E。配置集中声明章节、星球、
订单焦点、模块、许可、关系、目标 Stage 与预览限制，应用前必须通过注册表和
组合一致性验证。T-112 起赤砂入口接取正式 `PLAYABLE` 回访订单并直达复用后的
短路线；目录入口按注册表真实显示已开放与仍注册内容。白噪路线调试继续使用
独立深拷贝夹具，加急流程使用未开放订单夹具，低空投放继续使用既有 Lab；
三者均不改变正式内容就绪状态。

所有 M1 调试进程关闭自动保存，SaveService 不读取或写入正常主档、备份、临时档
和坏档保留文件，SettingsService 不写入玩家设置；`F6` 从内存初始快照重置并
重载目标场景。`scripts/check_m1_foundation.sh` 聚合 v1→v2 迁移、多星球进度、
一次一单与奖励恢复、注册表/本地化、目录导航、收藏、档案终端、Delivery Lab、
加急计时和 M0 完整闭环，并由项目总检查调用。

T-125 的生产闭环烟雾不使用目录预览夹具：它从正式 `PLAYABLE` 白噪订单开始，
依次经过正常驾驶舱旅行、精确白噪航路、着陆、档案选择、结算、返站、测试专用
存档和继续游戏，并断言重复结算不重复发奖、档案终端保持同步、穹林资格和
白噪回访种子往返一致。

T-126 的生产烟雾从 T-125 完成后的站点状态接取正式支线，依次覆盖自愿提示、
配置、独立驾驶舱内容、从 `17000 m` 开始的复用航段、同一冰下区域中的独立
三选一、结算、返站、测试专用保存与继续游戏。单元合同另覆盖 `50%` 货物完整度
得到 `64 / 80` 信用点与净 `0` 关系，以及失败/放弃均不改变穹林资格。

赤砂回访入口的基础快照必须已完成 M0 实际订单和首单剧情标记，但不得预发
`module_high_voltage_shielding`。T-112 启动时通过正式订单入口接取材料，
将回访状态推进到 `revisit_red_sand_materials_pending` 并进入 `26000 m` 安全
检查点；调试会话本身仍不读写正常存档。

T-112 的完整专项烟雾覆盖：三段短路线与本地 HUD、canonical AGL、服务航道和
最终进场检查点重试、伊娅/老皮对白及二选一、更新后的维修场互动、原子结算、
档案终端可见变化，以及测试专用临时路径上的保存、清空运行时状态和继续载入。

T-113 的专项烟雾继续覆盖：新档锁定、精确状态解锁、已知/隐藏图鉴边界、五类
键鼠浏览、640×360 区域不重叠、模态世界输入与焦点恢复、老皮一次性简报，以及
保存后重启继续游戏。可见录帧同时检查站内设备、异常详情和白噪线索对白。

T-114 的专项烟雾覆盖：赤砂回访完成奖励原子解锁白噪导航节点、旧 schema v2
完成档幂等补齐、四种资格状态、订单风险预览、工作台安装、驾驶舱目的地预览、
`REGISTERED_ONLY` 接单/配置/旅行三重守卫和 640×360 布局。可见录帧检查订单
风险、安装后飞船配置以及“资格已满足但正式航路未开放”的驾驶舱反馈。

T-120 的独立 `--m1-debug=white_noise_route` 入口从白噪章节、导航资格和已安装
屏蔽罩的确定快照开始，只在当前进程内接取 `debug_m1_white_noise_route`。
专项测试覆盖六段 `34000 m` 路线、`88 / 118 / 154 s` 节奏合同、1.28g 数据
对齐、三条冰裂谷局部通道与统一汇合、检查点恢复、有限着陆台、30/60/120 FPS
固定步长、640×360 HUD/高对比路线提示和正式订单/星球仍为
`REGISTERED_ONLY`。

T-121 在 `17000–23000 m` 原极光占位段接入
`WhiteNoiseStormProfile / WhiteNoiseInterferenceModel /
WhiteNoiseStormController`。危险按 `2.4 / 12.0 / 2.8 s` 最短时长与三个单调
路线门槛确定推进，活跃期每 `4.0 s` 通过既有高压、护盾优先入口结算一次
`18` 基础伤害。已安装屏蔽罩从模块 Resource 读取 `0.45 / 0.60`，未安装对照
保持 `1.0 / 1.0`；控制、Input Map、存档与核心 HUD 不受干扰修改。
`--m1-debug=white_noise_route` 保持已安装状态，新增隔离入口
`--m1-debug=white_noise_route_unshielded` 只移除当前夹具的安装项，不改变正式
Resource。路线提示、高对比和慢动作继续消费既有 SettingsService，帮助/系统
暂停、重试、F6、离开窗口与场景释放均清理计时、反馈和时间倍率。

T-122 用 `WhiteNoiseMainOrderContent` 把白噪主单的四套驾驶舱对白、旅行阶段
文案、公司终端备注、货舱封签和独立完成标记绑定到稳定订单 ID。驾驶舱只按当前
活动订单选择这组内容，不把白噪规则写入通用旅行控制器，也不复用 M0/赤砂回访
的对白已读标记。订单终端同时消费 `CargoDefinition.company_description_key`
与 `story_description_key`，把“公司记录”和“实际用途”并列显示；客户历史仍由
订单数据提供，不在 UI 脚本中硬编码。该阶段专项旅行烟雾只使用进程内深拷贝
夹具；正式订单和星球随后由 T-125 在完整闭环通过后开放。

T-123 将白噪路线灰盒提升为统一 production placeholder，但不改变 T-120/T-121
的路线、碰撞或危险数值。世界空间由 `WhiteNoiseRouteVisuals` 生成七层冰原、
裂谷、极光与档案结构；场景局部 `WhiteNoiseEnvironmentFeedback` 运行时合成
安全、暴雪、地下三层环境循环和短记忆动机。高对比辅助会同步降低雪粒、世界/
屏幕噪声线与脉冲闪光，同时保留航标、HUD 和碰撞上缘。全部来源为项目原创程序
内容与仓库既有粒子纹理，不增加第三方依赖；正式星球与订单仍为
`REGISTERED_ONLY`，直到 T-125 完成闭环后才升级为 `PLAYABLE`。

T-125 以 `WhiteNoiseSettlementContract` 连接正式路线与成长结果：

- 驾驶舱从活动目的星球读取精确 `flight_scene_path`，通过受阶段白名单约束的
  `request_stage_scene()` 进入 `white_noise_flight.tscn`；没有路径时只使用
  既有 M0 默认路线，不能让注册内容借道出发。
- 白噪路线在有限平台着陆后记录平稳/粗糙着陆与货损，再进入精确
  `white_noise_arrival.tscn`；返航升降梯仅在主对白与唯一选择均完成时请求
  `RESULTS`。
- 结算把固定订单奖励和选择附加奖励放入同一幂等奖励账本。`0%` 货物完整度
  只降低信用点，不撤销档案许可、白噪回访种子或穹林主线资格。
- 完成后旧档案终端显示白噪索引同步状态；保存、运行时重置和继续游戏都从
  schema v2 的同一组状态恢复，不根据场景重入再次发奖。

## 13. 不在 M1 的系统

- 完整战斗。
- 多飞船。
- 多单配送。
- 程序生成。
- 复杂贸易。
- 大型基地建设。
- Steam/云/成就。
- 最终结局系统。
