# AGENTS.md

本文件是仓库级长期指令。它应保持短、稳定、可执行。产品细节以 `CODEX_GOAL.md`、`DEVELOPMENT_TASKS.md` 和 `Docs/` 为准。

## 1. 每次任务的必读顺序

开始修改前必须阅读：

1. `AGENTS.md`
2. `CODEX_GOAL.md`
3. `DEVELOPMENT_TASKS.md`
4. 当前任务引用的 `Docs/*.md`
5. `git status`、近期相关提交和当前实现

不要只根据聊天提示工作，也不要跳过仓库现状检查。

## 2. 当前范围

- 当前里程碑是 **M0：Playable Spine**。
- 只实现 `DEVELOPMENT_TASKS.md` 中状态为 `Ready` 或由主 Agent 明确选定的一个任务。
- 不提前实现 M1 的另外三颗星球、完整战斗、程序生成、开放世界或复杂经营。
- 先完成赤砂星单订单完整闭环，再扩展广度。
- 每个任务都必须遵守其“明确不做”部分。

## 3. 不可擅自改变的产品事实

- 中文标题：`《我在宇宙送快递》`；英文工作名：`Universe Deliver`。
- 玩家是来自地球的人类，是银河外卖公司的快递员。
- 老皮是一名会说话、会直立行走的外星水豚，从开场起就是固定伙伴和前期教程角色。
- 不同文明通过同声传译器交流。
- 快递站大厅为 2D 俯视角；星际旅行为 2D 第一人称可交互驾驶舱；星球进入与配送为横向侧视飞行。
- 星球进入从“飞入星系”开始，必须包含接近星球和可操控的大气层进入，不能直接传送到地表。
- 游戏是有明确主线与结局的单人叙事游戏，不是 Roguelike。
- 主线顺序基本线性，局部选择可以改变人物关系与后续对话，但不得制造指数级分支。
- 玩家长期使用同一艘飞船，通过数值、模块和剧情功能升级。
- M0/M1 中激光炮仅用于轰击陨石；星球地表剧情没有战斗。
- 主线失败不得造成经济死档；不设置阻止继续出发的强制维修费。
- 货物是剧情物件，不是随机词条集合。
- 不得自行把暂定世界观写成永久正史。先检查 `Docs/01_NARRATIVE_BIBLE.md` 的 Canon Status。

## 4. 技术基线

- Godot `4.7.1-stable`。
- GDScript；新增脚本默认使用静态类型。
- Compatibility renderer。
- 2D、16:9、内部视口 640×360、`viewport + keep` 拉伸；精确倍数窗口自然保持整数比例，真全屏允许 fractional fit 以使用最大 16:9 区域；像素纹理 nearest filtering。
- 首要开发与验证平台：macOS，Apple Silicon。
- 首要输入：键盘与鼠标；输入必须通过 Input Map action，不在业务代码中直接判断具体按键。
- 飞船主体优先使用 `CharacterBody2D` 和显式速度积分。不要把核心手感交给不可预测的刚体模拟。
- 只保留少量真正全局的 Autoload；不要把所有系统塞进单例。
- 数据内容优先使用自定义 `Resource` 或可验证的结构化文件，场景脚本不硬编码大量剧情数据。

## 5. 代码规范

- 文件、目录、类、变量、信号、资源 ID 使用英文 `snake_case` 或 Godot 约定命名。
- 类型名使用 `PascalCase`；常量使用 `UPPER_SNAKE_CASE`。
- 玩家可见文本使用本地化 Key；禁止把长段中文剧情硬编码进脚本。
- 中文是当前源语言；README 中英双语；开发文档中文为主。
- 单个脚本只承担清晰职责。优先组合、信号和小型 Resource，不创建“万能 Manager”。
- 不为尚未出现的需求搭建复杂框架。
- 所有可影响飞行手感的关键值必须集中为可调配置，不能散落魔法数字。
- 公开接口和非显然算法需要简短注释；不要用注释重复代码。
- 不使用 `@warning_ignore` 掩盖真实问题，确需使用时写明原因。

## 6. 依赖与资产

- 默认不引入第三方插件、框架、SDK 或运行时服务。
- 引入任何新依赖前，先在 `Docs/09_DEPENDENCY_LOG.md` 记录：目的、候选、选择理由、许可证、锁定版本、替代方案和移除成本。
- 免费占位素材和生成式素材可以使用，但必须记录来源与许可到 `ATTRIBUTION.md`。
- 生成式美术先作为占位和风格探索，不得无说明地视作最终资产。
- 不下载或提交来源不明、授权不明的素材。
- 不提交 `.godot/`、导出构建、临时缓存或本机设置。

## 7. 测试与完成条件

除纯文档任务外，完成前必须：

1. 运行 `./scripts/check_project.sh`；若脚本尚未存在，当前基础任务必须创建它或明确说明临时验证方式。
2. 确认 Godot 无报错导入项目。
3. 运行与修改相关的自动测试。
4. 对受影响的可玩路径执行最短烟雾测试。
5. 检查 `git diff --check` 和 `git status`。

自动测试重点覆盖：

- 数据定义和 ID 引用有效性。
- 存档序列化、迁移和恢复。
- 订单状态流转。
- 飞行纯数学与环境参数组合。
- 关键场景可实例化且无缺失依赖。

飞行手感、视觉节奏、对白和音频主要依赖人工试玩，不伪装成完全可自动验收。

## 8. Git 规则

- 只由主 Agent 提交；子 Agent 不提交。
- 提交前必须通过适用测试。
- 一个提交应对应一个完整、可解释的可玩增量或基础能力。
- 提交信息优先使用中文，例如：`实现赤砂星大气层分区与重力过渡`。
- 不修改、覆盖或删除用户未提交的工作。
- 不使用破坏性 Git 命令，除非用户明确要求。
- 不通过重写历史来“整理”未经用户确认的提交。

## 9. 必须暂停人工试玩的节点

完成以下任务后不要继续扩范围，先报告并等待玩家试玩结果：

- 快递站接单与老皮教程闭环。
- 飞行基础手感与辅助驾驶。
- 赤砂星从星系接近到着陆的完整路线。
- M0 完整订单闭环。

暂停时提供：启动方式、测试路径、建议关注的 3–5 个手感问题、已知限制和最新提交。

## 10. 任务结束报告

每次完成任务后报告：

- 完成了什么。
- 修改了哪些关键文件。
- 运行了哪些命令及结果。
- 是否提交，以及提交摘要。
- 仍存在的已知问题。
- 是否到达人工试玩节点。

如果测试或实现失败，明确说明实际状态，不得声称通过。
